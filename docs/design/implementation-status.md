# 実装状況（Issue 単位の記録）

CLAUDE.md のステータス行から移設した Issue 単位の実装記録。新しい記録は末尾へ追記する。

workspace 雛形構築済み（TASK-66）。wire プロトコル層は実装済み（TASK-67・TASK-68・TASK-69・TASK-70・TASK-71）。SQL 表層は許可リスト検証（TASK-74）・束縛と実行計画（TASK-75）・取得モード切替構文（TASK-161）・`precision` モードの実行契約（TASK-162）・宣言的 UDF 呼び出し（TASK-79）・宣言的フィルタ API（TASK-147）まで実装済み。簡易クエリプロトコルを SQL 表層へ接続（TASK-73・WIRE-1）。`precision` モードの評価ハーネス実装済み（TASK-163。目標値の確定はユーザー判断待ち・`.github/workflows/recall.yml` 未接続）。モード切替・`precision` 契約の wire 経由 3 クライアント検証実装済み（TASK-165・SQL-12・SEARCH-9）。RLS 暗黙適用の全読み取り経路一般化検証を実施済み（TASK-138）。障害回復の `operation_id` 必須化ガード実装済み（TASK-92）。テーブル単位 `operation_id` 台帳実装済み（TASK-93）。台帳エントリへの内容照合ハッシュ（`operation_id` 再送時の内容一致/不一致判定。同一内容の再送は `23505`、内容不一致は `22023`。TASK-94・RECOVER-3 の重複拒否契約を包含する形で TASK-101・RECOVER-10 として実装済み）。集計関数（`COUNT`/`SUM`/`AVG`/`MIN`/`MAX`、TASK-166・SQL-13）・`GROUP BY`/`HAVING` 集計（TASK-167・SQL-14）まで実装済み。集計クエリの wire 経由 3 クライアント検証実装済み（TASK-168・SQL-13・SQL-14）。増分インデックス反映（TASK-120。ファイル形 `INSERT`（`path`/`body` 列指定）→ チャンク化 → 注入型 `Embedder` によるベクトル化 → 同一パス置換書き込み。外部埋め込みサービス実クライアントは未接続、注入点までが実装範囲）。増分インデックスの回帰テスト実装済み（TASK-121。本リポ独自の回帰基準による。Issue #281 で `t_full` の測定方式を見直し、一括投入 API による全体再構築計測・処理量カウント判定・規模スケーリング判定の 3 本柱へ再構成し、退行の実測モデルによる vacuous pass 防止テストを追加）。一括投入の処理量上限（TASK-122・INDEX-4。本リポ独自の実装上の上限を `batch_limits.rs` で判定し、`EngineCore::execute_insert_sql_batch` から複数ファイルのバッチ投入を受け付ける）実装済み。エラー契約 `wire_code` 写像の共通分類実装済み（TASK-152・ERR-2）。ErrorResponse への横断写像を `wire-server/src/error_response.rs` として実装済み（TASK-153・ERR-1・`RECOVER-5` (3) ポインタ）。バッチ検索の実 GPU バックエンド（`wgpu` =30.0.1・オーナー承認済み〔2026-08-26〕）を `gpu_batch.rs` として接続済み（TASK-128〜130・Issue #178。ベンチは CORE-6・CORE-16 とも実測経路へ配線済み〔CORE-16 は f32 常駐対照経路 `GpuF32ContrastBackend` を Issue #234 で追加〕）。辞書的情報源抽出パイプライン実装済み（TASK-109・PLAN-5）。LLM クエリプランニング（TASK-110・PLAN-1。常駐 LLM プロセス〔Ollama〕へのクエリ展開クライアントを `query_planner.rs` として実装。依存追加なしの自作 HTTP/1.1・JSON。辞書スナップショットを固定接頭辞として束ねる注入点は `EngineCore::with_query_planner`／`plan_query`。実 Ollama への疎通確認は対象外・プロセス内 TCP スタブで契約を固定）実装済み。ソフトブースト機構（TASK-111・PLAN-1。TASK-110 のヒント〔`path_hint`／`kind_hint`〕を RRF 融合後スコアへの加点として反映する `hybrid::apply_soft_boost`／`hybrid_search_boosted` をハードフィルタ化しない設計で `hybrid.rs` に追加。`sql/exec.rs` への結線は対象外）実装済み。クエリプランナーのモード推定（TASK-164・PLAN-11。`query_planner.rs` の展開結果へ `mode_hint` を追加し、`sql/mode.rs::resolve_mode_with_planner`／`EngineCore::plan_query_with_mode` として注入。解決契約の詳細は spec のビヘイビア定義（PLAN-11）を参照）実装済み。クエリ展開の受け入れ基準検証（TASK-112・PLAN-1・PLAN-2。`crates/engine/tests/query_planning_recall.rs` として、決定的コーパスと決定的スタブ `LlmClient` により「言い換え語彙のみのクエリ（intent）」「コーパス語彙と一致するクエリ（direct）」双方の展開なし/展開あり Recall@20 を回帰テスト化。テスト専任タスクにつき production コード無変更）実装済み。同ハーネスへ数万チャンク規模の `direct` カテゴリ限定 Recall 回帰を追加済み（TASK-113・PLAN-3。`intent` の大規模測定は対象外。層 B ゲート `QUERY_PLANNING_RECALL_MIN_R20_DIRECT_LARGE` は `.github/workflows/recall.yml` の environment `recall-gate` へ接続済み）。質問類型推定・ティアリング（TASK-115・PLAN-8。対話ティア／高精度ティアの判定を `tiering.rs` として実装し `EngineCore::with_tiered_query_planner`／`plan_query_with_classification` から接続。判定基準は本リポの実装既定値として差し替え可能にした。詳細は `docs/design/query-tiering-criteria.md` 参照）実装済み。再埋め込み規則（TASK-114・PLAN-10。`query_planner.rs`（`render_reembedding_text`／`reembed_expansion`）・`EngineCore::plan_and_embed_query` として実装。SQL 表層〔`USING PLAN`〕への結線は対象外）実装済み。`USING PLAN('<query>')` 専用構文パース・ディスパッチ（TASK-77・SQL-5。`ORDER BY` と相互排他の規範形〔括弧必須。`$n` パラメータ形式・括弧なし・重複は `42601`〕を許可リストで受理し、`sql::using_plan::bind_expansion` が TASK-110 の `plan_query` 展開結果を再埋め込みのうえ既存 C4 ハイブリッド実行形へ一意にディスパッチ。ソフトブースト〔`path_hint`/`kind_hint`〕の `sql/exec.rs` 結線は対象外）実装済み。`EXPLAIN` 構文結線（TASK-78・SQL-6。`sql::allowlist::Statement::Explain`・`sql::explain` として実装。詳細は spec のビヘイビア定義（SQL-6）を参照）実装済み。commit 成功境界と応答一意性（TASK-96・RECOVER-5。`recovery::commit_boundary`）実装済み。commit 成功境界を跨いだ panic の観測可能性側（TASK-97・RECOVER-6。panic フックによる緊急応答〔TASK-153・ERR-1・`RECOVER-5` (3) ポインタ〕の同期送出を `recovery::panic_hook` として実装。安全性側の abort は RECOVER-5 の既存ガードが引き続き担う）実装済み。障害回復の直近操作照会 API（TASK-98・RECOVER-7。`LastOperationLookup`）実装済み。ソフトブースト機構（TASK-111）を一般化するスコアリングブースト API（TASK-148・EXT-4。`scoring_boost.rs`。SQL 表層への構文露出は対象外）実装済み。内部エラーの fail-fast 統一契約（TASK-99・RECOVER-8。回復可能エラーは既存の ERR-1/`wire_code` 応答で処理継続、panic は経路・スレッドを問わずプロセス終了を `recovery::fail_fast` として実装。`wire-server::main` が `panic_hook::install_panic_hook` の直後に結線し TASK-97・RECOVER-6 の緊急応答経路を維持）実装済み。索引反映途中失敗の注入試験（TASK-100・RECOVER-9。`crates/engine/tests/index_failure_injection.rs` の commit 前失敗・commit 後の検索段失敗・再オープン系に加え、再構築処理そのものの途中失敗〔`crates/engine/src/arena.rs` の `build_filtered_with_limits_failure_mid_rebuild_leaves_committed_rows_intact_and_recovers`〕まで）実装済み。LLM クエリプランニングのソフトブーストヒントと RLS 事前フィルタの統合検証を実施済み（TASK-139。`crates/engine/tests/plan_rls_boost.rs`・`docs/design/plan-rls-boost-interaction.md`）。ティア別レイテンシ受け入れ基準の検証ハーネス実装済み（TASK-116・対象ビヘイビア PLAN-4, PLAN-6, PLAN-7〔判定内容・測定段階は spec 側が SSOT〕。`crates/engine/benches/tier_latency_bench.rs`・`benches/harness/tier.rs`・`tests/tier_latency_accept.rs`。常駐 Ollama 前提のため CI 経路は持たず（GitHub ホステッド runner に常駐 Ollama が無く self-hosted は組織承認済み例外の範囲外のため。PR #269 Codex 指摘）、`make bench-tier` を Actions 外の承認済み計測環境で運用者が直接実行する（README「ティア別レイテンシ受け入れ基準の実測手順」）。目標値の確定・実測はユーザー判断待ち・`docs/design/tier-latency-acceptance.md` 参照。LLM 不正応答（`PlanError::InvalidResponse`）試行の除外・上限ガードを追加済み〔Issue #316。`harness/protocol.rs::run_fallible`・`harness/tier.rs` の分類関数・既定除外上限で実装。除外対象は `InvalidResponse` のみで `Timeout` 等は従来どおり致命扱い。production コード無変更・テスト専任〕）。`USING PLAN` の wire プロトコル経由・実クライアント実行検証（TASK-117・PLAN-9 確定化の層 A。`wire-server` バイナリへの `--planner-endpoint`／`--planner-model`／`--embedder-hashing-dim` opt-in 注入 CLI と、`crates/wire-server/tests/wire_using_plan.rs` による決定的スタブ経由の成功系・fail-closed 系・RLS 不変検証。実 Ollama・実クライアント 3 種での PLAN-9 数値基準実測ハーネスは未整備・別タスク）実装済み。拡張構文（SQL-5〜7・9・10）の wire 経由・3 クライアント実行検証（TASK-82。セッション経由の SQL 実行経路〔`EngineCore::execute_sql_in_session`〕へ `INSERT` ディスパッチを追加し wire 経由での `INSERT` 受理へ切り替え〔読み取り可視性の既定は当時拡大せず、書いた本人も同一 wire セッションでは読み戻せない非対称を維持していた（RLS-11・TASK-195〔Issue #973〕でこの非対称は解消済み〔Issue #878 で判断記録を整理〕。詳細は `docs/design/three-client-e2e-harness.md`〕。層 A に `wire_hint_order.rs`・`wire_udf_call.rs`・`wire_insert_operation_id.rs` を追加し、層 B `crates/wire-server/tests/extended_syntax_e2e.rs`〔`#[ignore]`・`make e2e-three-client`〕を新設）実装済み。`PrefilterCache::insert` の世代競合を `DictionaryCache` と同じ fail-closed 契約〔`None` で拒否〕に統一（Issue #280）。テーブル単位世代カウンタの拒否精度は現状維持で確定（Issue #285・`docs/design/table-generation-rejection-granularity.md`）。回帰ベンチ・Recall ゲートの閾値 ↔ spec ポインタ対応表・設定手順を整理（Issue #286・`docs/design/ci-gate-variables.md`。実値設定・`workflow_dispatch` 疎通確認はマージ後の管理者作業として引き続き未実施）。閾値注入を Actions variables から secrets へ移行（Issue #286。`env:` ブロックがログへ値付きで印字される variables では閾値〔spec 由来の非公開数値〕が public な Actions ログへ漏えいするため。opt-in フラグ `BENCH_CORE6`／`BENCH_CORE16`／`BENCH_DEDICATED_ENV` は非機密として variables のまま維持）。さらに `bench.yml` の閾値 secrets は repo レベルから main 限定 Environment `bench-gate`（`recall-gate` と同方針）へ再移設（codex-review P0 指摘・PR #299。`workflow_dispatch` の任意 ref 書き換えで repo レベル secrets を任意処理へ渡せる経路を塞ぐ）。Recall 系閾値ゲートの実測値出力は `RECALL_VERBOSE` opt-in・`GITHUB_ACTIONS` 下拒否に限定（Issue #303）。CORE-7 ゲート（動的窓集約を経由する単発クエリ経路の p95 劣化上限）の測定方式を Issue #302 で単発クエリ p95 の A/B（複数試行中央値採用）へ再整合し、旧来の集約器 push/drain 単体比較は合否に数えない診断へ降格（`docs/design/core7-dynamic-window-gate.md`）。小規模段 Recall@20 ゲート未達の engine 側原因調査（Issue #307・SEARCH-1・SEARCH-3。詳細は `docs/design/hybrid-recall-regression.md`「小規模段ゲート未達の engine 側原因調査（Issue #307）」節参照）実施済み。hybrid Recall@20 ゲート（小規模・大規模）の engine 側改善（Issue #310・SEARCH-1・SEARCH-3。RRF 融合の同点順位規約 `hybrid.rs::TieRank`〔既定 `GroupEnd`〕・密プール境界の同点グループ完全化〔`complete_boundary_tie_group`〕。詳細は `docs/design/hybrid-recall-regression.md`「Issue #310: engine 側改善」節・`docs/design/rrf-tie-break-determinism.md` 参照。fixture パラメータ非変更・production 変更〔`TieRank::GroupEnd` 既定・境界同点グループ完全化〕はオーナー判断で採用済み）実装済み。`recall.yml` の 3 ゲート（hybrid・rerank・query-planning）を独立評価・AND 集約へ変更（Issue #311・`docs/design/recall-gate-independent-evaluation.md`）。`rerank.rs::LexicalOverlapReranker` の既定重みを等重み（1.0:1.0）から fused 優位（`fused_weight:lexical_weight` = 3.0:1.0）へ変更し、`rerank_recall.rs` 大規模段の非劣化アサーション `after_hits20 >= baseline_hits20` の red（Issue #310 対応後に発生した字句一致優先による正解脱落）を解消（Issue #310・`docs/design/rerank-recall-regression.md`「実測結果」節参照）。境界同点グループ再取得ループ（PR #320・Issue #310）の単発クエリレイテンシへの寄与を計測するベンチを追加（Issue #324・CORE-7・PLAN-4/6/7 関連ポインタ。`crates/engine/benches/hybrid_latency_bench.rs`・`benches/harness/hybrid_latency.rs`・`tests/hybrid_latency_accept.rs`。単一ビルド内の A/B〔通常コーパス vs プロトタイプクラスタによる同点誘発コーパス〕方式で `make bench-hybrid` から実行する手動専用ベンチ。CORE-7 は `hybrid_search` を通らない測定経路のため実測が構造的に不変であることを 1 回実測で確認し、`bench-tier`〔PLAN-4/6/7〕の前後比較は常駐 Ollama 前提のため運用者実測へ申し送り。詳細・実測値は `docs/design/hybrid-refetch-latency.md` 参照。production コード〔`hybrid.rs`〕は無変更）。CORE-16（f16 常駐 vs f32 常駐）ゲートの Apple GPU（Metal）fail 切り分け（Issue #313。本開発環境〔NVIDIA GeForce RTX 3060・Vulkan backend〕でのクリーンな単独計測〔方法 A〕により engine 側シェーダの性能不足〔H3〕は否定できたが、Apple GPU/UMA 環境要因〔H1〕は未検証、逐次測定ノイズ〔H2-逐次〕と並ぶ共同主因候補にとどまり、discrete GPU 実測のみで Metal 固有のシェーダ性能問題を断定的には否定できない（詳細・判断根拠は `docs/design/core16-f16-resident-gate.md`「判断」節参照）。規模点診断 `BENCH_CORE16_DIAG`／`BENCH_CORE16_DIAG_SCALE_INDEX` を `batch_bench.rs` へ opt-in で追加（1 プロセス = 1 規模点。複数規模点の同一プロセス内逐次測定は比較不能な証拠と判明したため撤去。PR #326）。詳細・環境別 pass/fail 表は `docs/design/core16-f16-resident-gate.md` 参照。`gpu_batch.rs` は無変更）実施済み。SQL 表層 C1 p95 の専有環境での僅差 fail への対応（Issue #314・TASK-83 条件7。SQL 表層 C1 経路の行数比例な固定コスト削減——`storage::decode_row_embedding_and_metadata_into` によるスクラッチバッファ再利用、`sql/exec.rs` の不要な `Vec<Value>` 確保省略、`core.rs::execute_validated_in_session` による二重パース排除）実装済み。専有環境での再実測・条件7 の最終判定はオーナー作業として引き続き未確定（`docs/design/c1-p95-dedicated-env-reverification.md` 参照）。`USING PLAN` が実 Ollama 経由で SQL エラーなしに 0 行を返す事象の調査（Issue #315・SEARCH-3・SEARCH-9・PLAN-11。決定的フィクスチャでの検証により、プランナー推定 `precision` の確信度ゲートによる契約どおりの空集合応答として再現可能であることを確認。ただし実 Ollama の `mode` 値・実埋め込み出力そのものは未確認であり、実事象がこの経路によるものかは原因候補・最有力仮説の位置づけ。`EXPLAIN` の `mode_source` による切り分け手順を含め `docs/design/using-plan-precision-empty-result.md` 参照。production コード無変更）実施済み。rerank 大規模段 improvement@20 ゲート未達の是正（Issue #330・SEARCH-7。`rerank.rs::LexicalOverlapReranker::rank_lexical`（字句一致順位）の同点規約を位置順位から `rank_fused` と同じ `GroupEnd` へ統一し、字句信号の到達上限まで改善幅を引き上げ（after hits20 388→389）。fixture パラメータ非変更。詳細・原因分析は `docs/design/rerank-recall-regression.md`「Issue #330」節参照。マージ後の `recall.yml` 実測・Issue 記録はオーナー／管理者作業として申し送り）実装済み。PR #331 で同点規約整合後もなお改善幅ゲート `RERANK_RECALL_MIN_R20_IMPROVEMENT` が絶対差基準ではプール外正解の到達不能分まで分母に含み過小評価する構造だったため、オーナー承認済みの spec 改訂（vector-db-spec#7）に合わせ判定基準を候補プール上限に対する相対比率（`(after − baseline) / (pool_ceiling_hits20 − baseline_hits20)`。改善余地が構造的にほぼ 0 の場合は非劣化のみで判定）へ再定義（Issue #330・`rerank_recall.rs::RerankRecallResult::improvement_ratio`。実測 2/9 ≈ 0.2222。詳細は `docs/design/rerank-recall-regression.md`「Issue #330 追記」節・`docs/design/ci-gate-variables.md` 参照。production コード〔`crates/engine/src/`〕は無変更・テスト専任）実装済み。クロスエンコーダ型リランカーの実 ONNX バックエンド接続と自然言語 fixture による効果実測（Issue #333・SEARCH-7 方式変更。`rerank.rs` の `CrossEncoderBackend` trait・`CrossEncoderConfig`・`CrossEncoderReranker`・`CrossEncoderError` に加え、オーナー承認済み〔2026-08-30〕で `deny.toml` へ `RUSTSEC-2024-0436`〔`paste`・unmaintained のみ〕の ignore を登録のうえ `ort` `=2.0.0-rc.13`／`tokenizers` `=0.23.2`〔いずれも optional・`cross-encoder` feature 限定〕を追加し、実 ONNX 推論バックエンド `OnnxCrossEncoderBackend`（`rerank/cross_encoder_onnx.rs`）を接続。自然言語 fixture（`tests/fixtures/nl_qa.rs`）でのクロスエンコーダ実測ハーネス（`tests/rerank_cross_encoder_recall.rs`・`make rerank-cross-encoder-eval`）を追加し実測を実施——SEARCH-7 相対基準〔ratio ≥ 0.6〕には未到達（fixture の文書生成構造〔正解概念が非流暢な追記フレーズに偏る〕が事前学習済みモデルの評価に適さないことが主因と分析。詳細・実測値・原因分析は `docs/design/rerank-recall-regression.md`「Issue #333 追記」節参照。到達可否は Issue #330 へ報告）。自然言語 fixture の再設計とクロスエンコーダ improvement_ratio の再実測（Issue #337・SEARCH-7。`tests/fixtures/nl_qa.rs` の文書テキスト生成方式を再設計し、正解概念の非流暢な追記フレーズを全廃して全概念を流暢な自然文へ埋め込む方式へ変更〔正解集合を決めるキーワード抽選用 rng ストリームとは分離し比較可能性を維持。`ceil20`＝79 で不変を実測確認済み〕。再設計後もクロスエンコーダは SEARCH-7 相対基準〔ratio ≥ 0.6〕に未到達（実測 `improvement_ratio`＝0.0000。原因は fixture の非流暢構造だけでなく、語彙規模に対する概念間の表層語重複・ドメイン語彙密度と汎用モデルの学習分布の乖離の可能性が残る、と分析を更新）。詳細・実測値・原因分析は `docs/design/rerank-recall-regression.md`「Issue #337」節参照。到達可否は Issue #330 へ報告。production コード〔`crates/engine/src/`〕は無変更・テスト専任）実施済み。2 方式（字句一致・クロスエンコーダ）× 2 fixture の全実測未達（0.222・0）を根拠に、オーナー承認済み spec 改訂（vector-db-spec#8・2026-08-31）で SEARCH-7 の改善幅相対基準を実コーパス評価まで informational（非ブロッキング・実測値の記録と報告のみ必須）へ降格し、本リポ側も `rerank_recall.rs` のブロッキング判定を「絶対下限（`RERANK_RECALL_MIN_R20_LARGE`）＋非劣化」のみへ変更（`RERANK_RECALL_MIN_R20_IMPROVEMENT` は不読・`recall.yml` の env からも除去。improvement_ratio は informational ログとして出力継続。実コーパス評価時にブロッキング化を再判定。`docs/design/ci-gate-variables.md`・`docs/design/rerank-recall-regression.md`「SEARCH-7 改訂（2026-08-31）」節参照）。式評価のステップ列コンパイル化（Issue #353。`WHERE` 事前/事後フィルタ・投影 `Computed` 列・集計 `ScalarExpr`・`GROUP BY` の `WHERE` が共有していた束縛済み式 `BoundExpr` の毎行再帰ツリーウォークを、束縛時に一度だけ平坦なステップ列へコンパイルし行ループでは明示スタックで線形実行する方式へ変更。`sql/expr_program.rs` を新設し `sql/exec.rs`・`sql/aggregate.rs`・`sql/group_by.rs` の行ループから再帰 `udf_call::eval` 呼び出しを撤去（`udf_call::eval` はセマンティクスの参照実装として残置）。行に依存しないスカラー算術・比較部分式の定数畳み込み（defer-on-error。畳み込み時にエラーになる部分式は畳み込まず実行時評価に委ね、可視行が 0 件ならエラーが発生しない既存契約を維持）を含む。詳細・ベンチ前後比較は `docs/design/expr-step-compilation.md` 参照）実装済み。hybrid 実行が参照する `SparseIndex` のテーブル世代整合キャッシュ（Issue #357。`sql/sparse_cache.rs::SparseIndexCache`。フィルタなし hybrid クエリに限り `(table, PolicyContext)` × テーブル単位世代でキャッシュし、`sql/exec.rs::execute_statement` の Hybrid 分岐へ結線。`PrefilterCache`/`DictionaryCache` の fail-closed 契約〔Issue #280〕を踏襲しつつ、insert 失敗時は呼び出し元が自身のクエリスナップショットから構築した索引をそのまま使う契約に意図的に差別化。詳細は `docs/design/sparse-index-cache.md` 参照）実装済み。hybrid_rrf クエリの段別内訳プロファイル切り分け（Issue #356・親 Issue #355。`crates/engine/benches/hybrid_profile_bench.rs`・`benches/harness/hybrid_profile.rs`・`tests/hybrid_profile_accept.rs`。`make bench-hybrid-profile` から実行する手動専用ベンチで `SparseIndex::build` が hybrid 経路上乗せコストの過半を占めることを実測確認。詳細・実測値は `docs/design/hybrid-rrf-latency-breakdown.md` 参照。production コード無変更）実施済み。スカラー列二次索引の設計検討 ADR（Issue #359・`docs/design/scalar-secondary-index.md`。データモデル・一貫性・RLS 境界・コスト見積り・実装タスク分解案を整理。ステータス Proposed・実装は ADR 承認後に別途タスク化）作成済み。スカラー列二次索引 ADR（Issue #359）を Issue #472 で候補 A（redb 永続二次テーブル）／候補 B（メモリ常駐・テーブル世代整合キャッシュ）の比較のうえ候補 B を採用案として確定し #473〜#476 へ対応づけ（オーナー承認コメントの有無に応じ Accepted／承認待ち）。スカラー列二次索引の構築とテーブル世代整合キャッシュ（Issue #473・親 #472・#359。`sql/scalar_index.rs::ScalarIndex`・`ScalarIndexCache`。`sql::arena_cache::SqlArenaSnapshot`〔#363〕から `TEXT` 列の等価・前方一致述語向け辞書索引と `id` 順序索引を構築し、`(table, PolicyContext)` × テーブル単位世代でキャッシュ〔`insert` は `core.rs::PrefilterCache`・Issue #280 と同じ fail-closed `None` 契約〕。`sql/exec.rs::execute_statement_with_cache` の SCALAR 事前フィルタ経路へ gated 構築のみ結線し、索引による候補削減はまだ行わない〔クエリ結果は本 Issue の前後で完全に不変〕。候補削減・`EXPLAIN` 露出・`ExecutionPlan` 統合は Issue #474 以降へ申し送り。詳細は `docs/design/scalar-index-generation-cache.md` 参照）実装済みproduction コード無変更。集計経路（TASK-166・TASK-167・SQL-13・SQL-14）の embedding 非参照デコードスキップと必要列限定デコード（Issue #350。`ReferencedColumns`・`DecodeTier` による 3 段階デコード〔ヘッダのみ／dim+マスク済みスカラー列／embedding 込み完全デコード〕を `sql/aggregate.rs`・`sql/group_by.rs` へ導入し、`row_codec::scan_scalar_columns_masked`・`storage::decode_row_dim_and_metadata_borrowed` を新設。RLS 可視性判定・TABLE-12 のキー/ヘッダ tenant 整合検査は全 tier で維持。詳細は `docs/design/aggregate-decode-skip.md` 参照）実装済み。SQL 表層 `SELECT`（`USING PLAN` 展開経由を含む）の `VectorArena` をテーブル単位世代整合キャッシュ化（Issue #363。`sql/arena_cache.rs::SqlArenaCache`。既存 `PrefilterCache`〔TASK-169〕と同型だが失効源泉をテーブル単位世代〔`catalog::table_generation_in_txn`〕とし、SCALAR 段が恒等写像の場合はキャッシュ済みスナップショットを直接借用する高速経路を持つ。`docs/design/sql-arena-generation-cache.md` 参照）実装済み。embedding チャンク連続格納レイアウトの設計 ADR（Issue #364・`docs/design/embedding-chunk-layout.md`。ステータス Proposed・production コード無変更・実装タスクは ADR 承認後に分解）作成済み。ANN 索引採否の設計検討（Issue #367・CORE-9/CORE-10・TASK-132。判断材料を `docs/design/ann-index-adoption.md` に整理）を経て、Issue #403 で B 案（条件付き opt-in 採用・自作 HNSW・依存追加なし・オーナー承認〔Issue #402〕）の判断記録・実装契約を追記しステータス Accepted へ更新（production コード無変更）。実装は Phase 3 タスク #404〜#413。エンジン選択の opt-in 経路（`SearchEngineKind::Hnsw` variant・`build` 結線・`EngineCore::open_with_engine`／`from_storage_with_engine` による構築時 opt-in。既定エンジンは不変。索引は本タスク時点は保持せず全件 brute-force フォールバック。詳細は `docs/design/hnsw-search-engine-wiring.md` 参照）を Issue #407 として実装済み。HNSW 索引のテーブル世代整合キャッシュと未索引分 brute-force 併用（Issue #408・親 #402・前提 #404〜#407。`sql::hnsw_cache::HnswIndexCache`。`(table, ctx)` × テーブル単位世代キーで `sql::arena_cache::SqlArenaCache`〔#363〕・`sql::sparse_cache::SparseIndexCache`〔#357〕と同型の fail-closed 契約を持ち、SQL 表層のフィルタなし `Ranking::Distance` クエリに限り `sql::exec::execute_statement_with_cache`・`core.rs::EngineCore`〔`SearchEngineKind::Hnsw` opt-in 構築時のみ〕へ結線。同一世代内は索引探索を再利用し、世代進行直後は索引済みノードと現行アリーナの差分〔新規・内容変更・削除/不可視化行〕を 1 回だけ計算したうえで未索引分のみ brute-force で補い Top-k をマージする〔差分比率が閾値を超えたら再構築。索引構築・再構築の失敗はこの世代のみ brute-force へ縮退し既存ベースは温存〕。索引ヒットのスコアは常に `kernel::dot` で再計算しスロット写像の妥当性検証も兼ねる。`SearchProvider` trait は無変更。詳細は `docs/design/hnsw-generation-cache.md` 参照）実装済み。疎索引キャッシュ（Issue #357）導入後の Recall 非劣化・RRF 同点順位規約の不変・前後比較検証（Issue #358・SEARCH-1・SEARCH-3 関連ポインタ。SQL 表層専用のcold/hot 等価性テスト `crates/engine/tests/sparse_cache_recall.rs` を追加し、既存Recall ゲート〔cache 非経由のため構造的に不変〕の green を確認したうえで`feature_bench` の `hybrid_rrf` フェーズ前後比較〔p50 約 2.5 倍改善〕を実測。`crates/engine/examples/feature_bench.rs` を本リポへ追跡化。詳細は `docs/design/sparse-index-cache-verification.md` 参照。production コード無変更・テスト専任）実施済み。距離計算と Top-k 選出の 2 段分離バッチ化の試作・前後比較実測（Issue #366。`kernel.rs::search_range_two_stage`〔ブロック単位・固定長スタックスクラッチ〕を試作し等価性は回帰テストで確認したが、共有計測環境での前後比較実測では変更を含まない区間の run-to-run 差分（min-of-N）が変更対象区間の観測差分と同程度かそれ以上あり、この環境・標本数では変更の効果を run-to-run 変動から切り分けて確認できなかったため production 変更を撤回。詳細・実測値・判断根拠は `docs/design/knn-two-stage-topk.md` 参照。production コード〔`crates/engine/src/`〕は無変更）実施済み。isa.rs dot カーネルの複数アキュムレータ化検討（Issue #365。前提 Issue #362。cache 常駐・arena 規模 × 複数次元のマイクロベンチ `crates/engine/benches/dot_kernel_bench.rs`〔`make bench-dot-kernel`〕・`tests/dot_kernel_accept.rs` を追加。ACC=2/ACC=4 いずれも交互実行 5 回実測で cache 常駐 dim100/dim128 が ノイズ帯を超えて悪化し決定規則の採用条件を満たさなかったため、`crates/engine/src/isa.rs` は無変更のまま現状維持で close 可と判断。実測表・判断根拠は `docs/design/dot-kernel-multi-accumulator.md` 参照。production コード無変更・テスト専任）実施済み。dim 768／1536 での前後比較・閾値候補（384）の輪番実測（Issue #519。`scripts/bench_dot_kernel_ab.sh`〔新規〕による本環境（共有 QEMU）の参考値実測。production `search_range` 経路の `dot_block4` に 8〜11% の退行所見あり。閾値 768 は据え置き・最終確定はオーナー実機〔Issue #530〕へ申し送り。詳細は `docs/design/dot-kernel-multi-accumulator.md`「Issue #519 追記」節参照。production コード無変更・テスト・docs 専任）実施済み。`search_within` の段別プロファイルと再取得ループ発火回数の計測点を `hybrid_profile_bench` へ追加（Issue #387。疎側再取得ループ本体を `hybrid.rs::sparse_refetch_loop`（private）へ抽出し、production 経路と テスト・ベンチ向け公開フック `engine::hybrid::sparse_refetch_observed` が 同一実装を共有する構成へ変更したことで、ベンチ側の疎側再取得スケジュールは 予測ではなく実観測になった（codex-review P1 指摘対応・PR #416）。基線は `docs/design/hybrid-rrf-latency-breakdown.md`「Issue #387」節）。自作 SHA-256 のストリーミング化（Issue #399。`recovery/content_hash.rs` の `HashInputBuilder` をバッチ全体の 2 回全量コピーを伴う一括処理版から、ブロック単位ストリーミング更新型 `Sha256`（16 語ローリングメッセージスケジュール）へ再構成。ハッシュ入力バイト列のレイアウト・既存 content_hash 値は不変（FIPS 180-4／NIST 公開テストベクタ・境界長網羅・分割 `update` 等価性テストで機械検証済み）。手動専用の直接比較実測（`content_hash.rs` 内 `#[ignore]` テスト）で参照実装（旧・一括処理版）以下の処理時間を複数回確認し採用。詳細は `docs/design/ingest-stage-profile.md`「Issue #399 追記」節参照）実装済み。redb `insert_reserve` によるゼロコピー行書き込みの試作・実測・採否検討（Issue #400。`Table::insert_reserve` は redb 4.2.0 内部で零埋め Vec をヒープ確保し通常の insert 経路へ渡す実装であり「ゼロコピー」にはならない構造だと静的解析で確認したうえで、`ingest_profile_bench.rs` へ `BENCH_INGEST_PROFILE_INSERT_MODE`（insert／reserve）の A/B 計測モードを追加して実測。I6（redb insert）段が `insert` 比で中央値約 +97%（dim=128・rows=1,000・交互 5 ペア）悪化することを確認し不採用（Rejected）と判断。詳細・実測表は `docs/design/redb-insert-reserve-zero-copy.md` 参照。production コード〔`crates/engine/src/`〕は無変更）実施済み。書き込み経路改善（Phase 2・親 Issue #395）の前後比較と Durability／バッチ上限の判断記録（Issue #401。`feature_bench` 13 フェーズと `bench-ingest-profile` の before〔61fc943〕/after 交互実測による前後比較表、棄却判断（RECOVER-5/6・RECOVER-8・TASK-96/97/99・TABLE-12・#397 ポインタ）、PostgreSQL `COPY` の件数/バイト二重基準と `batch_limits.rs`〔TASK-122・INDEX-4〕の対比および行形 `insert_rows` にバイト上限が無い点の申し送りを `docs/design/ingest-write-path.md` に記録。production コード無変更・docs 専任）実施済み。`SparseIndex` の term インターニング（Issue #388・親 Issue #386。`String` キーの `BTreeMap` を term 辞書（`String -> TermId(u32)`）と `TermId` キーの `Vec` へ置換し、クエリ語の辞書順走査を維持してスコアのビット一致を保った。公開 API 無変更・依存追加なし。`make bench-hybrid-profile` の前後比較実測（`sparse_build_total` 176.4ms→50.1ms・`hybrid_search_cached_index` 115.7ms→25.3ms 等）は `docs/design/hybrid-rrf-latency-breakdown.md`「Issue #388」節参照。後続 #389（posting list）・#390（可視ビットマップ＋posting 走査）は `TermId` を前提にする）実装済み。`SparseIndex` への転置索引（posting list）・`doc_len`／`doc_ids` 配列の構築（Issue #389・親 Issue #386。`TermId` 添字の posting list（`doc_idx` 昇順・順序構築。ソート不要）・`doc_len`／`doc_ids` 配列を追加し、`id_index` を `BTreeMap` から `HashMap` へ置換した。`search`／`search_within` は本 Issue では未参照のまま残置し（撤去・経路切替は #390）、公開 API・スコアのビット一致は不変。単体テストで posting list から復元した tf／df が `DocEntry` 経由の値と全件一致することを固定。`make bench-hybrid-profile` の build 所要時間・常駐メモリ（`approx_heap_bytes`・RSS）実測は `docs/design/hybrid-rrf-latency-breakdown.md`「Issue #389」節参照）実装済み。`SparseIndex::search`／`search_within` を可視ビットマップ（`VisibleBitmap`）＋posting 走査の 1 パス方式へ再実装（Issue #390・親 Issue #386。文書集合の全件線形走査・毎文書の `term_freq`（`DocEntry`）を撤去し、クエリ語ごとに `postings[t]` だけを辿って `doc_idx` 添字のスコアアキュムレータへ加算する共通コア `score_by_postings` を `search`（`ScoreScope::All`）・`search_within`（`ScoreScope::Visible`。可視集合を `doc_idx` ビットマップへ変換し統計の母数も同じ走査で確定させる）で共有。RLS 相当のテナント境界縮約契約〔`search_within` がインデックス全体の `doc_count`／`avg_doc_len`／`doc_freq` を一切参照しない〕・スコアの f64 ビット一致は不変（`crates/engine/tests/hybrid_profile_accept.rs::profile_sparse_index_replica_matches_real_search_within`・`tests/sparse_cache_recall.rs` cold/hot 等価性・`tests/hybrid_recall.rs` 層 A 固定値アサーションで検証済み）)実装済み。`make bench-hybrid-profile` 実測: `hybrid_search_cached_index`（実 hybrid 経路）25.3ms→9.7ms、`search_within_fetch_k=25000`（全可視集合）2.6〜3.4ms→1.27ms、`approx_heap_bytes()` 約18.65MiB→約11.59MiB（`docs: Vec<DocEntry>` 撤去分）。詳細・前後比較は `docs/design/hybrid-rrf-latency-breakdown.md`「Issue #390」節参照。`SparseIndex::score_by_postings` の BM25 文書長正規化項を文書長クラス別テーブル参照へ、Top-k 選出を 2k バッファ＋`select_nth_unstable_by` 型へ置換（Issue #391。式・演算順は旧実装と完全に同一のためスコアはビット一致。256 段ロッシー fieldnorm 量子化はビット一致契約に反するため不採用〔Rejected〕と判断。`make bench-hybrid-profile` 実測は`docs/design/hybrid-rrf-latency-breakdown.md`「Issue #391」節参照）実装済み。疎側再取得ループの再スコアリング回避（Issue #392。`hybrid.rs::sparse_refetch_loop` が各ラウンドで呼び直していた `SparseIndex::search_within` を、`sparse.rs::score_within`（新設）による 1 回のスコア計算＋`SparseScored::top`（新設）による前方一致な Top-k 拡張へ置換。融合結果・境界同点グループ・`fetch_k` スケジュールは不変であることを機械検証。`make bench-hybrid-profile` 実測は`docs/design/hybrid-rrf-latency-breakdown.md`「Issue #392」節参照）実装済み。転置索引化（Issue #386 Phase 1・#388〜#392）後の Recall 非劣化・cold/hot 等価・決定性の回帰検証（Issue #393。`crates/engine/tests/sparse_cache_recall.rs` へ RLS 部分可視・未知語のみクエリ・空クエリの cold/hot 等価性テストを追加し、統計縮約オラクル〔不可視行を含まない対照 DB との結果一致〕でテナント境界縮約の非漏えいを固定。`crates/engine/tests/sparse_determinism.rs` を新設し `docs/design/rrf-tie-break-determinism.md` の同点タイブレーク契約が `SparseIndex::search_within`/`score_within`/`SparseScored::top`・`hybrid_search` の疎側再取得ループでも維持されることを固定。3 Recall ゲート（hybrid・rerank・query-planning）の層 A 固定値アサーションは無変更のまま green、層 B 実測値の導入前〔`8bfaaa4`〕/導入後の前後比較は `docs/design/sparse-inverted-index-recall-verification.md` 参照。production コード〔`crates/engine/src/`〕は無変更・テスト専任）実施済み。疎索引転置化（Issue #386 Phase 1・#388〜#392）の設計判断・データ構造・維持契約・外部実装参照（tantivy〔MIT〕・qdrant〔Apache-2.0〕。手法名と採否のみ）と `feature_bench` 13 フェーズ／`bench-hybrid-profile` 段別の導入前（`8bfaaa4`）→導入後の通し前後比較（`hybrid_rrf` 13 フェーズ中で最大の改善対象。`feature_bench` p50 90,800us→11,507us・約 7.9 倍改善、他 12 フェーズは非退行〔比 0.98〜1.04x・ノイズ帯内〕。`hybrid_rrf`／`vector_knn` 比率は before 10.63x→after 1.38x）を `docs/design/sparse-inverted-index.md` に記録（Issue #394。production コード無変更・docs 専任）実施済み。HNSW 探索（ef ビーム探索・top-k）と brute-force 対照 Recall 単体テスト（Issue #405・親 Issue #402・前提 Issue #404。`hnsw.rs::HnswIndex::search`・`HnswSearchScratch`を追加し、上位層 `ef=1` 貪欲降下 → 層 0 を幅 `ef.max(k)` でビーム探索 → 上位 `k` 件を返す。visited 集合を `VisitedSet` trait でジェネリック化し、構築経路〔世代カウンタ方式 `VisitedScratch`〕・探索経路〔1 ノード 1 bit のビットマップ方式 `VisitedBitmap`〕の双方から `search_layer` が共有する構成へ変更。`crates/engine/tests/hnsw_search.rs` の層 A（常時実行・縮小フィクスチャ）・層 B（`#[ignore]`・`make hnsw-search-recall`・N=10,000・dim=128）で brute-force〔`kernel::CpuScalarProvider`〕対照の Recall@10 を実測——クラスタ構造ありフィクスチャ・既定パラメータで ef=64／256 いずれも 1.0000（受け入れ基準 0.95／0.99 を上回る）。同じ規模での一様乱数のみのコーパス（クラスタ構造なし・HNSW にとって最難条件の一つ）は informational 参考値として ef=64 で 0.6410・ef=256 で 0.9535 と明確に低下することもあわせて記録し、受け入れ判定はクラスタ構造ありフィクスチャの範囲に限定されることを明記した。同一索引・同一クエリでの結果再現性〔新規スクラッチ・再構築索引・重複ヘビーコーパスの同点 id 昇順を含む〕をあわせて固定。`search_engine.rs` 結線・並列構築・世代整合キャッシュ・RLS 統合・`EXPLAIN` 露出・Recall ゲート接続・永続化はいずれも別タスク〔#406〜#413〕の担当のまま。詳細は `docs/design/hnsw-search.md` 参照）実装済み。HNSW 構築の並列化（Issue #406・親 #402・前提 #404/#405。`hnsw.rs::HnswIndex::build_with_threads`／`build_parallel`・新設 `hnsw/parallel_build.rs`。pgvector の要素単位ロック・エントリポイント更新のみ排他というロック粒度設計、qdrant の逐次プレフィックス方式〔先頭 `SEQUENTIAL_PREFIX_NODES`＝256 件を単一スレッドで構築し孤立成分の発生を防ぐ〕を参考に、残りを `AtomicUsize` によるワークスティール方式で並列挿入する。ノード単位 `RwLock` で隣接リストを保護し、`parallel_search.rs::thread_count_for`／`WorkerBudgetGuard`〔`pub(crate)` 化・挙動不変〕を構築側とも共有。`threads==1` または `n<=SEQUENTIAL_PREFIX_NODES` では `build` と完全に同一のグラフを返す契約を機械検証。`score`／`select_neighbors_heuristic`／`shrink_links` の計算本体を逐次・並列で共有する純粋関数へ切り出し、逐次経路の挙動・既存テストは無変更のまま維持。実装中に発見した並列固有のレース（挿入中のノード自身の隣接リストが他ノードの逆方向リンクと同時に伸び次数上限を超え得る）を自己 `shrink_links` 呼び出しで解消。並列構築版でも `parallel >= sequential - 0.02` の Recall@10 同水準・探索の決定性契約は不変であることを固定。100k 点でのスレッド数ラダーベンチ（`make bench-hnsw-parallel-build`。手動専用・CI 非配線）でスレッド数に応じた構築時間短縮を実測。詳細は `docs/design/hnsw-parallel-build.md` 参照）実装済み。HNSW の RLS 事前フィルタとの統合（Issue #409・親 #402・前提 #408。候補マスク `crate::hnsw::NodeMask`・`HnswIndex::search_masked` を新設し、`hnsw.rs::search_layer` の受理判定をマスク対応化（`None` は既存 `search` とビット同一）。`sql::hnsw_cache::Overlay` へ `visible_mask`／`visible_in_index` を追加し、`search_with_overlay` を「可視候補数 ÷ 索引ノード数」の比 `HnswParams::full_scan_ratio`〔既定 1/10・整数比 `Ratio`〕による plain scan／マスク付き ANN 探索の切替へ書き換え、`k + stale_nodes` オーバーフェッチ方式〔#408 の既知の限界〕を撤去。SCALAR 事前フィルタ付き DISTANCE（`Subset` 形状）を `sql::hnsw_cache::search_subset_or_fallback`〔per-query 写像・キャッシュ非登録〕として新規結線し、`sql/exec.rs` の適用条件を `FullVisible`／`Subset` の 2 形状へ拡張。Rust API（`VectorCore::search`）を `rls.rs::PrefilterSnapshot::search_with_hnsw` として初めて HNSW キャッシュへ結線（既存テストの契約を「ANN 経路を使わない」から「既定エンジン対照 Recall@10 ≥ 0.9・可視外非混入」へ意図的に反転）。テナント境界は ADR〔`ann-index-adoption.md`〕の「事後フィルタ不採用」判断を維持し、索引は ctx 可視アリーナのみから構築（候補マスクはテナント境界ではなくクエリ時点の候補集合と索引ノードの差分を表す装置）。`EXPLAIN` 露出・hybrid 密側 ANN 化・Recall ゲート同一閾値検証は対象外（#410〜#412）。詳細は `docs/design/hnsw-rls-cardinality-switch.md` 参照）実装済み。フィルタ付き ANN の境界再取得（iterative scan 型）と fail-closed 検証（Issue #410・親 #402・前提 #408〜#409。DISTANCE 経路の「結果不足」（`masked_short`）は #409 以降構造的に到達不能と判明したため `ef` 倍増ループは追加せず、iterative scan の実体は hybrid 密側再取得ループ（`hybrid.rs::hybrid_search_boosted` の `dense_fetch_k` 倍増）とみなし、`sql/hnsw_cache.rs` の索引・オーバーレイ解決（`prepare_full_visible`／`prepare_subset`）と Top-k 探索（`search_prepared`）を分離したうえで、hybrid 密側専用アダプタ `sql::hnsw_hybrid::HnswDenseProvider`（`SearchInput` の ids／vectors が捕捉済みバッファと同一の場合のみ索引経路を使う fail-closed 受理条件）を新設し `sql/exec.rs` の `Ranking::Hybrid` 分岐へ結線。`hybrid.rs`・`SearchProvider` trait は無変更。停止性は既存の `dense_cap`／`MAX_FETCH_K` が provider 非依存に保証し、`k > MAX_EF` は `ef_cap_fallbacks` 統計を伴う brute-force 縮退で空集合の誤返却を防止。同点誘発コーパスでの停止性・決定性、既定エンジン対照 Recall@10 ≥ 0.9・可視外テナント非混入・非 vacuous（`hybrid_dense_searches > 0`）を固定するテスト（`crates/engine/tests/hnsw_cache.rs`・新設 `tests/hnsw_hybrid_refetch.rs`）を追加。詳細は `docs/design/hnsw-hybrid-iterative-scan.md` 参照。Phase B（再開型スキャン）は受け入れ基準が既存設計で満たされるため実装見送り（Rejected）と判断）実装済み。`EXPLAIN` への検索エンジン種別・ANN 静的適用判定の露出（Issue #411・親 #402・前提 #405〜#410。`sql/hnsw_cache.rs::classify_ann_plan`（`sql/exec.rs` の 4 ANN 適用条件と単一情報源を共有）・`sql/explain.rs::ExplainEngine`／`build_explain_result` を新設し、既存 6 行の `mode_source` 直後へ`engine:`（`parallel_brute_force`／`cpu_scalar_brute_force`／`hnsw`／`(custom_provider)`）・`engine: hnsw` のときのみ `hnsw_params:`（構築時静的パラメータのみ）・`ann_plan:`（`plain_scan_engine`／`plain_scan_precision`／`hnsw_full_visible`／`hnsw_subset`）を追記。既定エンジン時の出力は追記のみで後方互換。実行時縮退結果・可視カーディナリティ・行数・`full_scan_ratio` 等のテナント存在情報に繋がる値は非露出のまま、`EXPLAIN` は検索本体を実行しない契約（索引の `lookup`／`prepare_*` 不呼び出し）を維持。詳細は `docs/design/explain-search-engine-exposure.md` 参照）ANN opt-in 時の Recall ゲート同一閾値検証と TASK-121 系増分回帰の拡張（Issue #412・親 Issue #403。ADR `docs/design/ann-index-adoption.md` B 案の受け入れ条件「SEARCH 系 Recall ゲートを ANN 有効経路でも同一閾値で通過」「TASK-121 系増分回帰の ANN 対応」。SQL 表層のみが ANN の実 seam〔`sql::hnsw_cache`／`sql::hnsw_hybrid`〕へ到達できる production API であるため、3 つの Recall ハーネス〔`hybrid_recall.rs`・`rerank_recall.rs`・`query_planning_recall.rs`〕の層 B に `RECALL_ENGINE` 環境変数opt-in（既定 brute_force・`crates/engine/tests/fixtures/recall_engine.rs::RecallEngine`）を追加し、`hnsw` 指定時は `EngineCore::from_storage_with_engine` ＋ `ORDER BY HYBRID(...)` 経由で同一閾値を判定する。ローカル実測では全 6 測定点で brute_force と hnsw の Recall が完全一致（詳細は `docs/design/ann-recall-gate-verification.md` 参照）。`.github/workflows/recall.yml` へ `RECALL_ENGINE` を注入する経路として当初 `workflow_dispatch.inputs.recall_engine` を追加したが、schedule 実行では ANN 経路が未測定のままになる不整合（PR #438・codex-review 指摘）を受け、`strategy.matrix.recall_engine`（`[brute_force, hnsw]`）で両エンジンを trigger を問わず常時実行する方式へ変更（`workflow_dispatch.inputs.recall_engine` は撤去）。TASK-121 系（増分反映の初回構築・overlay・再構築の各状態）の ANN 対応検証を `crates/engine/tests/incremental_index_hnsw.rs` として追加。production コード〔`crates/engine/src/`〕は無変更・テスト専任）実施済み。ANN 導入（Phase 3・#404〜#412）の前後比較（Issue #413。親 #402／ADR `docs/design/ann-index-adoption.md` B 案の受け入れ基準「(B 案条件 1) 損益分岐点の実測」を事後確認。`feature_bench` 13 フェーズ〔25,000 行・100,000 行〕・`bench-knn-profile`（S0-cold／S0-hot）の前後比較を `docs/design/hnsw-index.md` に記録。既定エンジンは production コード〔`crates/engine/src/`〕無変更のため退行の定義自体が成立せず、hnsw opt-in 有効時（after/既定 vs after/hnsw の同一バイナリ比較）はフィルタなし DISTANCE・hybrid 密側・ホットパスで 6〜33% 高速化する一方、SCALAR 事前フィルタ付き DISTANCE〔`hnsw_subset` 経路〕は 37〜45% 悪化することを実測。25k〜100k の 2 規模点では明確な損益分岐点は観測されず、フィルタの選択性が主要因で行数規模は副次的という所見を得た。参照した外部実装（qdrant・pgvector・usearch）の既定値は上流ドキュメントで確認できた値のみ帰属。`crates/engine/benches/harness/bench_engine.rs`〔`BENCH_FEATURE_ENGINE`／`BENCH_FEATURE_SCALE`／`BENCH_KNN_PROFILE_ENGINE` の fail-closed パース〕を新設し、`feature_bench.rs`・`knn_profile_bench.rs` へ ANN opt-in・規模上書きの計測条件を追加。production コード〔`crates/engine/src/`〕は無変更）実施済み。HNSW 構築比較（Issue #406 追記）へ対照エンジン `hnsw_rs`（`=0.3.4`）を一時追加して 3 エンジン共通の L2 正規化コーパスで 100k 点の実測を行ったが（PR #451）、構築 3.4〜4.8 倍・探索約 5 倍遅く対照は usearch で足りるとのオーナー判断（2026-09-05）で撤去済み（`deny.toml` の bincode ignore と計測時間の倍増も解消。実測は履歴として `docs/design/hnsw-parallel-build.md`「hnsw_rs（`=0.3.4`）を加えた 3 エンジン比較」節に残置。`bench-hnsw-compare` は自作・usearch 2 エンジン・L2 正規化方式を維持）。パラメータ等価性の深掘りは Issue #446〜#450 のツリーへ申し送り。production コード〔`crates/engine/src/`〕は無変更）実施済み。他 DB との機能別横断ベンチと GPU 実機検証（PR #451 後続・Issue #406 関連。`scripts/crossdb_bench/`〔Python ハーネス・Cargo 依存追加なし〕で self〔wire 経由〕・pgvector・sqlite-vec・Qdrant・LanceDB・MySQL を 25,000 行・dim 128 で機能別に計測し、`crates/engine/benches/gpu_scaling_bench.rs`〔`make bench-gpu-scaling`〕で `engine::gpu_batch` の規模 × バッチ別スイープ、FAISS〔faiss-gpu-cu12〕・Qdrant GPU 索引構築との対照を RTX 3060 実機で実施。計測中に発見した wire-server の簡易クエリ応答 40ms 下限〔Nagle × delayed ACK〕を accept 直後の `TCP_NODELAY` 設定で解消〔`crates/wire-server/src/server.rs`・回帰テスト `tests/wire_nodelay_latency.rs`。production 変更〕。あわせて hnsw_rs 対照エンジンを撤去。広域取得 5 フェーズ〔Top-200/1000・`id`+`body`・ANN 候補幅 max(64, k) 統一〕を追加し、self のスカラー列投影時に `sql/exec.rs::on_visible_row` の全行 `scan_scalar_columns` による k 非依存の約 10 ms 固定コストを特定〔遅延デコードは Issue #453・広域取得モードの構文追加は Issue #454 へ起票〕。詳細・実測表は `docs/design/crossdb-bench.md` 参照）実施済み。性能判定の計測規約を `docs/design/benchmark-judgement-policy.md` として集約（Issue #462。交互 N≥5 ペア・per-run 生データ必須・min-of-N＋median 併記・固定 ±5% と参照区間実測の 2 種ノイズ帯・共有 QEMU 環境の証拠力区分・後続 perf Issue 向け受け入れ条件テンプレート。production コード無変更・docs 専任）実施済み。`vector_knn` 786µs（`docs/design/crossdb-bench.md`）の wire／SQL 表層／距離カーネル・Top-k 内訳を切り分ける計測ベンチを追加（Issue #463。`crates/wire-server/benches/knn_wire_profile_bench.rs`〔`make bench-knn-wire-profile`〕・`benches/harness/knn_wire.rs`・`tests/knn_wire_profile_accept.rs`。既定エンジン `ParallelSearchProvider` 上で wire e2e・SQL 表層〔`execute_sql_in_session`〕・単線/並列 provider・距離カーネル単体を交互計測し、共有開発環境の 1 回実測では SQL 表層区分〔T2−T1p〕が距離カーネル自体より支配的〔55.4% 対 29.6%〕、wire 区分〔T3−T2〕は 11.9% にとどまることを確認。spec 由来の閾値を持たない情報提供専用のため CI 非配線・`GITHUB_ACTIONS` 下は fail-closed 拒否。詳細・実測表は `docs/design/knn-wire-stage-profile.md` 参照。production コード〔`crates/engine/src/`・`crates/wire-server/src/`〕は無変更）実施済み。`agg_count`／`rls_isolation`／`vector_knn_where`（`docs/design/crossdb-bench.md` で self が最劣後する3フェーズ）の段別プロファイル計測を実施し、redb 全行走査・ヘッダデコード・RLS 判定・dim/metadata デコード・`WHERE` 述語評価の内訳と後続 Issue（#477・#471）への帰属を記録（Issue #464・親 #456・ルート #455。`crates/engine/benches/scan_stage_profile_bench.rs`・`benches/harness/scan_stage_profile.rs`・`tests/scan_stage_profile_accept.rs`・`make bench-scan-stage-profile`。詳細・実測表は `docs/design/scan-stage-profile.md` 参照。production コード〔`crates/engine/src/`〕は無変更・テスト・ベンチ・docs 専任）実施済み。`hybrid_rrf`（Issue #392 適用後）の最新基線を `bench-hybrid-profile`（engine 内 B0s〜B8）・`bench-hybrid-wire-profile`（engine/SQL 表層/wire。新設）で交互複数ラウンド再計測し、SQL 表層固定コスト・疎側再取得ループがほぼ同水準（37〜39%）で最大区分であることを確認、Phase 6（Issue #548）へ引き継ぎ（Issue #465・親 #456・ルート #455。`crates/engine/benches/harness/hybrid_profile.rs`〔`HybridProjection`・投影種別付き文組み立て・`bucket_diff`・帰属レンダリング〕・`hybrid_profile_bench.rs`〔ラウンド計測セクション〕・`crates/wire-server/benches/harness/hybrid_wire.rs`・`hybrid_wire_profile_bench.rs`〔いずれも新設〕。詳細・実測表・引き継ぎ内容は `docs/design/hybrid-rrf-latency-breakdown.md`「最新基線」節参照。production コード〔`crates/engine/src/`・`crates/wire-server/src/`〕は無変更・テスト・ベンチ・docs 専任）実施済み。チップ別手動計測手順と結果記録テンプレート（Issue #469・親 #456。`crates/engine/benches/chip_bench.rs`・`harness/chip.rs`・`make bench-chip`。dot_kernel／knn_profile／feature_bench〔dim 128・768〕を 1 ワークロード = 1 プロセスでラウンドロビン交互計測し CPU 情報・実行時検出 ISA・per-run 生データ・min/median/参照区間帯を `summary.json` へ出力。CI 非配線・手動専用。記録テンプレートは `docs/design/chip-kernel-guidelines.md` §7。production コード無変更）実施済み。自作 BM25 疎索引の score_by_postings が呼び出しのたびに新規確保していたスコアアキュムレータ（acc: Vec<f64>）を、索引（SparseIndex）の寿命内で再利用する有界スクラッチプールへ置換（Issue #546・親 #545。SparseIndexCache（Issue #357）が保持する Arc<SparseIndex> と寿命が一致する再利用バッファとし、&self シグネチャ・スコアの f64 ビット一致・RLS 相当のテナント境界縮約契約はいずれも不変。詳細は docs/design/hybrid-rrf-latency-breakdown.md「Issue #546」節参照。前後比較実測は Issue #547 の担当）実装済み。バッチ検索 GPU 経路のクエリタイル化（Issue #532。`gpu_batch.rs::DOT_SHADER_WGSL`/`DOT_SHADER_F32_WGSL` の dispatch 構造を「1 dispatch = 1 クエリ」から「`PolicyContext` 単位にグループ化したクエリを最大 `GPU_QUERY_TILE_MAX`〔実装既定値 16〕本まで 1 dispatch へタイル化」する方式へ変更し、常駐行列の行データ読み込みをタイル幅ぶんのクエリで償却する〔`workgroup` 共有メモリは使わない設計上の簡略化〕。結果の Top-k 選出は引き続き CPU 側 `TopKSelector` が担う。実測での前後比較は依存先 Issue #533 へ申し送り）実装済み。macOS 上の `is_aarch64_feature_detected!` 実効性を静的解析で確認済み（Issue #468。`crates/engine/examples/detect_features.rs`（`make detect-features`）・`.github/workflows/detect-features.yml`〔GitHub ホステッド `macos-latest`。情報提供専用〕。既存 NEON 経路は Apple ターゲットで常に有効と静的解析（`neon`/`fp16`/`fhm`/`dotprod` の コンパイル時 target_feature 化）で判明。実機（GitHub ホステッド `macos-latest`・オーナー実機）での実測は workflow run 反映待ち。詳細は `docs/design/chip-kernel-guidelines.md`「7. macOS 上の `is_aarch64_feature_detected!` 実効性」節参照。production コード無変更・テスト・docs 専任）実施済み。wgpu での部分 Top-k（bitonic／radix select）と SUBGROUP 可用性の設計 doc を `docs/design/gpu-batch-topk.md` に作成（Issue #535・親 #534・Phase 5 親 #460。#536 で実装する方式〔workgroup 内 bitonic 部分 Top-k＋SUBGROUP 有効時の subgroup shuffle 段＋CPU `TopKSelector` 最終マージ〕、SUBGROUP 非対応・`k` 上限超過時の全量 readback への fail-closed 縮退、naga 30.0.1 が `enable subgroups;` を拒否する制約、RTX 3060 の `adapter.features()` 実測〔SUBGROUP／SUBGROUP_BARRIER 有効・subgroup 32/32〕を記録。再現用の手動専用 example `gpu_adapter_info`〔`make gpu-adapter-info`〕を追加。production コード〔`crates/engine/src/`〕は無変更）実施済み。workgroup 内部分 Top-k シェーダ（共有メモリ上の bitonic ソート網。ADR 決定 1「候補 B」の subgroup shuffle 段は naga 30.0.1 がバリア・subgroup builtin の一様性を検証しない制約〔上記 doc §1.3〕を踏まえ意図的に見送り、候補 A 相当に統一）と CPU 側 `TopKSelector` 最終マージを実装（Issue #536。`gpu_batch.rs::DOT_SHADER_TOPK_WGSL`/`DOT_SHADER_TOPK_F32_WGSL`〔`topk_dot_shader!` マクロで S1 を両常駐形式が共有〕・`GpuReadbackMode`/`select_readback_mode`・`GpuTopKParams`・`plan_partial_topk_chunk_rows`・`merge_partial_topk_readback`・`dispatch_partial_topk`・`GpuBatchStats`/`stats()`。Top-k パイプライン生成失敗は既存の段階的 fail-closed 縮退〔`init_gpu_context`/`init_f32_contrast_pipeline` の独立 error scope〕で全量 readback へ吸収し、CORE-16 公平性のため f32 常駐対照経路 `GpuF32ContrastBackend` にも同型の Top-k パイプラインを用意。本開発環境〔RTX 3060〕実機での結合テストで、既定の部分 Top-k 経路と全量 readback 経路〔`GpuSearchTestOptions::force_full_readback`・`bench-internals` feature 限定〕の結果が `(id, score.to_bits())` 列としてビット同一であること、非有限スコア行の非混入、`stats()` の非 vacuous 性〔`partial_topk_dispatches > 0`〕を確認済み。詳細・実装スコープの申し送りは `docs/design/gpu-batch-topk.md`「実装記録」節参照。前後比較・readback バイト数の実測は Issue #537 の担当）実装済み。`search_range` の行ブロック（4 行）カーネルの NEON 版（Apple M／Graviton。Issue #511。AVX2+FMA／AVX-512F は Issue #510。1 行版とビット同一。前後比較は Issue #512／#530。詳細は `docs/design/dot-kernel-row-block.md` 参照）WHERE 事前フィルタの等価・範囲述語（`Equality`/`Prefix`/`id` 単純比較。宣言順の残余述語がある場合は索引を使わない狭義の対応形状に限る）を `sql::scalar_index::ScalarIndex`（Issue #473）の候補削減へ結線（Issue #474・親 #472・#359。`sql/scalar_plan.rs::classify_scalar_plan`／`id_bounds` の静的判定で `sql::exec`・`sql::explain`（`EXPLAIN` の `scalar_plan:` 行）が同一情報源を共有し、`ScalarIndex::resolve_candidates`・`VectorArena::build_from_cached_rls_rows_subset` により候補行のみへ `on_visible_row` を適用する〔索引は行を「絞る」ことしかできず「通す」ことはできない設計。索引↔スナップショット同一性ガード・選択度切替〔暫定既定 1/2〕付き〕。詳細は `docs/design/scalar-index-prune.md` 参照）実装済み。不適合ノードのリンクのみを 2-hop 中継点として参照する ACORN-1 方式のテナント境界契約整理とゲート条件を設計済み（Issue #500・親 #499。ベクトル非参照（I1・P0・不変）とリンク非参照（I2・実装上の不変条件）を分離し、per-`(table, ctx)` 索引には構築時点で ctx に不可視だった行の情報が含まれないことを根拠に I2 のみをゲート下で緩和する条件付き成立と判断。可視比率レジーム（PlainScan／OneHop／TwoHop）によるゲート条件設計・既定無効の方針を `docs/design/hnsw-rls-cardinality-switch.md`「Issue #500」節に記録。実装は #501、実測は #502 の担当。production コード無変更・docs 専任）実施済み。不適合ノードのリンクのみを 2-hop 中継点として使う ACORN-1 の実装（Issue #501・親 #500。`crate::hnsw::HopMode`〔`OneHop`／`TwoHop`〕・`ValidatedHnswParams::acorn_max_visible_ratio`〔既定 `None`＝無効・既存動作不変〕・`bridge_expand`〔`hnsw.rs`。ビーム探索・BFS 到達可能性検査〔`is_mask_fully_reachable_with`〕が共有する単一実装〕・`sql::hnsw_cache::TraversalRegime`〔`PlainScan`／`OneHop`／`TwoHop`。`search_with_overlay`・`Overlay::compute` の単一情報源〕を実装。統合テストでは選択率 20% の疎なマスクで既定（`OneHop`）だと `mask_splits_graph` が全クエリで発火する fixture が、`acorn_max_visible_ratio = 1/1` opt-in により全クエリ ANN 完走（既定エンジン対照 Recall@10 ≥ 0.9・tenant 非漏えい・非 vacuous）へ転じることを確認。`EXPLAIN` への `acorn_max_visible_ratio` 露出は `full_scan_ratio` と同区分として見送り。既定無効のため既存動作・既存テストは全てビット同一のまま無変更。詳細は `docs/design/hnsw-rls-cardinality-switch.md`「Issue #501」節参照。実測・既定値確定は #502 の担当）実装Phase 5（#532・#536・#539・#542）の通し前後比較・FAISS GPU 対照・Qdrant GPU 構築対照の更新・Apple UMA（Metal）ゼロコピー静的確認を実施（Issue #544。self（after）の `100,000×128×64` GPU f16 p95 min-of-5 は before比 0.20倍・約5倍高速化を6点中3点で確認、残り3点は共有環境ノイズにより判定不能。FAISS GPUとの差は約21〜26倍で横ばい、Qdrant GPU索引構築は引き続き優位性なし。UMA静的確認は wgpu 30.0.1が staging経由のため真のゼロコピーは不成立と判断・production変更なし。詳細は `docs/design/gpu-batch-phase5-before-after.md` 参照。production コード〔`crates/engine/src/`〕は無変更・テスト専任）実施済み。書き込みトランザクションの durability を起動時に選択する CLI opt-in（Issue #850・親 #849。`wire-server --durability immediate|none`。`crates/wire-server/src/durability_opt.rs` が閉じた語彙 2 値から `engine::storage::WriteDurability`（Issue #849）へ到達する唯一の入口。未指定は既存 `EngineCore::open` 経路のままビット同一・不正値/値欠落/重複指定は fail-closed。`none` 選択時は commit 成功応答が永続を保証しなくなる旨の警告を起動時に 1 行出力（詳細は `docs/design/ingest-write-path.md`「Issue #849 追記」節参照）。engine production コード無変更）実装済み。他は未実装（タスクは spec リポの `05-tasks.md`（TASK-66〜165）、マイルストーンは `06-roadmap.md`（MS-1〜6）参照）production コード無変更。集計経路（TASK-166・TASK-167・SQL-13・SQL-14）の embedding 非参照デコードスキップと必要列限定デコード（Issue #350。`ReferencedColumns`・`DecodeTier` による 3 段階デコード〔ヘッダのみ／dim+マスク済みスカラー列／embedding 込み完全デコード〕を `sql/aggregate.rs`・`sql/group_by.rs` へ導入し、`row_codec::scan_scalar_columns_masked`・`storage::decode_row_dim_and_metadata_borrowed` を新設。RLS 可視性判定・TABLE-12 のキー/ヘッダ tenant 整合検査は全 tier で維持。詳細は `docs/design/aggregate-decode-skip.md` 参照）実装済み。SQL 表層 `SELECT`（`USING PLAN` 展開経由を含む）の `VectorArena` をテーブル単位世代整合キャッシュ化（Issue #363。`sql/arena_cache.rs::SqlArenaCache`。既存 `PrefilterCache`〔TASK-169〕と同型だが失効源泉をテーブル単位世代〔`catalog::table_generation_in_txn`〕とし、SCALAR 段が恒等写像の場合はキャッシュ済みスナップショットを直接借用する高速経路を持つ。`docs/design/sql-arena-generation-cache.md` 参照）実装済み。embedding チャンク連続格納レイアウトの設計 ADR（Issue #364・`docs/design/embedding-chunk-layout.md`。ステータス Proposed・production コード無変更・実装タスクは ADR 承認後に分解）作成済み。ANN 索引採否の設計検討（Issue #367・CORE-9/CORE-10・TASK-132。判断材料を `docs/design/ann-index-adoption.md` に整理）を経て、Issue #403 で B 案（条件付き opt-in 採用・自作 HNSW・依存追加なし・オーナー承認〔Issue #402〕）の判断記録・実装契約を追記しステータス Accepted へ更新（production コード無変更）。実装は Phase 3 タスク #404〜#413。エンジン選択の opt-in 経路（`SearchEngineKind::Hnsw` variant・`build` 結線・`EngineCore::open_with_engine`／`from_storage_with_engine` による構築時 opt-in。既定エンジンは不変。索引は本タスク時点は保持せず全件 brute-force フォールバック。詳細は `docs/design/hnsw-search-engine-wiring.md` 参照）を Issue #407 として実装済み。HNSW 索引のテーブル世代整合キャッシュと未索引分 brute-force 併用（Issue #408・親 #402・前提 #404〜#407。`sql::hnsw_cache::HnswIndexCache`。`(table, ctx)` × テーブル単位世代キーで `sql::arena_cache::SqlArenaCache`〔#363〕・`sql::sparse_cache::SparseIndexCache`〔#357〕と同型の fail-closed 契約を持ち、SQL 表層のフィルタなし `Ranking::Distance` クエリに限り `sql::exec::execute_statement_with_cache`・`core.rs::EngineCore`〔`SearchEngineKind::Hnsw` opt-in 構築時のみ〕へ結線。同一世代内は索引探索を再利用し、世代進行直後は索引済みノードと現行アリーナの差分〔新規・内容変更・削除/不可視化行〕を 1 回だけ計算したうえで未索引分のみ brute-force で補い Top-k をマージする〔差分比率が閾値を超えたら再構築。索引構築・再構築の失敗はこの世代のみ brute-force へ縮退し既存ベースは温存〕。索引ヒットのスコアは常に `kernel::dot` で再計算しスロット写像の妥当性検証も兼ねる。`SearchProvider` trait は無変更。詳細は `docs/design/hnsw-generation-cache.md` 参照）実装済み。疎索引キャッシュ（Issue #357）導入後の Recall 非劣化・RRF 同点順位規約の不変・前後比較検証（Issue #358・SEARCH-1・SEARCH-3 関連ポインタ。SQL 表層専用のcold/hot 等価性テスト `crates/engine/tests/sparse_cache_recall.rs` を追加し、既存Recall ゲート〔cache 非経由のため構造的に不変〕の green を確認したうえで`feature_bench` の `hybrid_rrf` フェーズ前後比較〔p50 約 2.5 倍改善〕を実測。`crates/engine/examples/feature_bench.rs` を本リポへ追跡化。詳細は `docs/design/sparse-index-cache-verification.md` 参照。production コード無変更・テスト専任）実施済み。距離計算と Top-k 選出の 2 段分離バッチ化の試作・前後比較実測（Issue #366。`kernel.rs::search_range_two_stage`〔ブロック単位・固定長スタックスクラッチ〕を試作し等価性は回帰テストで確認したが、共有計測環境での前後比較実測では変更を含まない区間の run-to-run 差分（min-of-N）が変更対象区間の観測差分と同程度かそれ以上あり、この環境・標本数では変更の効果を run-to-run 変動から切り分けて確認できなかったため production 変更を撤回。詳細・実測値・判断根拠は `docs/design/knn-two-stage-topk.md` 参照。production コード〔`crates/engine/src/`〕は無変更）実施済み。isa.rs dot カーネルの複数アキュムレータ化検討（Issue #365。前提 Issue #362。cache 常駐・arena 規模 × 複数次元のマイクロベンチ `crates/engine/benches/dot_kernel_bench.rs`〔`make bench-dot-kernel`〕・`tests/dot_kernel_accept.rs` を追加。ACC=2/ACC=4 いずれも交互実行 5 回実測で cache 常駐 dim100/dim128 が ノイズ帯を超えて悪化し決定規則の採用条件を満たさなかったため、`crates/engine/src/isa.rs` は無変更のまま現状維持で close 可と判断。実測表・判断根拠は `docs/design/dot-kernel-multi-accumulator.md` 参照。production コード無変更・テスト専任）実施済み。dim 768／1536 での前後比較・閾値候補（384）の輪番実測（Issue #519。`scripts/bench_dot_kernel_ab.sh`〔新規〕による本環境（共有 QEMU）の参考値実測。production `search_range` 経路の `dot_block4` に 8〜11% の退行所見あり。閾値 768 は据え置き・最終確定はオーナー実機〔Issue #530〕へ申し送り。詳細は `docs/design/dot-kernel-multi-accumulator.md`「Issue #519 追記」節参照。production コード無変更・テスト・docs 専任）実施済み。`search_within` の段別プロファイルと再取得ループ発火回数の計測点を `hybrid_profile_bench` へ追加（Issue #387。疎側再取得ループ本体を `hybrid.rs::sparse_refetch_loop`（private）へ抽出し、production 経路と テスト・ベンチ向け公開フック `engine::hybrid::sparse_refetch_observed` が 同一実装を共有する構成へ変更したことで、ベンチ側の疎側再取得スケジュールは 予測ではなく実観測になった（codex-review P1 指摘対応・PR #416）。基線は `docs/design/hybrid-rrf-latency-breakdown.md`「Issue #387」節）。自作 SHA-256 のストリーミング化（Issue #399。`recovery/content_hash.rs` の `HashInputBuilder` をバッチ全体の 2 回全量コピーを伴う一括処理版から、ブロック単位ストリーミング更新型 `Sha256`（16 語ローリングメッセージスケジュール）へ再構成。ハッシュ入力バイト列のレイアウト・既存 content_hash 値は不変（FIPS 180-4／NIST 公開テストベクタ・境界長網羅・分割 `update` 等価性テストで機械検証済み）。手動専用の直接比較実測（`content_hash.rs` 内 `#[ignore]` テスト）で参照実装（旧・一括処理版）以下の処理時間を複数回確認し採用。詳細は `docs/design/ingest-stage-profile.md`「Issue #399 追記」節参照）実装済み。redb `insert_reserve` によるゼロコピー行書き込みの試作・実測・採否検討（Issue #400。`Table::insert_reserve` は redb 4.2.0 内部で零埋め Vec をヒープ確保し通常の insert 経路へ渡す実装であり「ゼロコピー」にはならない構造だと静的解析で確認したうえで、`ingest_profile_bench.rs` へ `BENCH_INGEST_PROFILE_INSERT_MODE`（insert／reserve）の A/B 計測モードを追加して実測。I6（redb insert）段が `insert` 比で中央値約 +97%（dim=128・rows=1,000・交互 5 ペア）悪化することを確認し不採用（Rejected）と判断。詳細・実測表は `docs/design/redb-insert-reserve-zero-copy.md` 参照。production コード〔`crates/engine/src/`〕は無変更）実施済み。書き込み経路改善（Phase 2・親 Issue #395）の前後比較と Durability／バッチ上限の判断記録（Issue #401。`feature_bench` 13 フェーズと `bench-ingest-profile` の before〔61fc943〕/after 交互実測による前後比較表、棄却判断（RECOVER-5/6・RECOVER-8・TASK-96/97/99・TABLE-12・#397 ポインタ）、PostgreSQL `COPY` の件数/バイト二重基準と `batch_limits.rs`〔TASK-122・INDEX-4〕の対比および行形 `insert_rows` にバイト上限が無い点の申し送りを `docs/design/ingest-write-path.md` に記録。production コード無変更・docs 専任）実施済み。`SparseIndex` の term インターニング（Issue #388・親 Issue #386。`String` キーの `BTreeMap` を term 辞書（`String -> TermId(u32)`）と `TermId` キーの `Vec` へ置換し、クエリ語の辞書順走査を維持してスコアのビット一致を保った。公開 API 無変更・依存追加なし。`make bench-hybrid-profile` の前後比較実測（`sparse_build_total` 176.4ms→50.1ms・`hybrid_search_cached_index` 115.7ms→25.3ms 等）は `docs/design/hybrid-rrf-latency-breakdown.md`「Issue #388」節参照。後続 #389（posting list）・#390（可視ビットマップ＋posting 走査）は `TermId` を前提にする）実装済み。`SparseIndex` への転置索引（posting list）・`doc_len`／`doc_ids` 配列の構築（Issue #389・親 Issue #386。`TermId` 添字の posting list（`doc_idx` 昇順・順序構築。ソート不要）・`doc_len`／`doc_ids` 配列を追加し、`id_index` を `BTreeMap` から `HashMap` へ置換した。`search`／`search_within` は本 Issue では未参照のまま残置し（撤去・経路切替は #390）、公開 API・スコアのビット一致は不変。単体テストで posting list から復元した tf／df が `DocEntry` 経由の値と全件一致することを固定。`make bench-hybrid-profile` の build 所要時間・常駐メモリ（`approx_heap_bytes`・RSS）実測は `docs/design/hybrid-rrf-latency-breakdown.md`「Issue #389」節参照）実装済み。`SparseIndex::search`／`search_within` を可視ビットマップ（`VisibleBitmap`）＋posting 走査の 1 パス方式へ再実装（Issue #390・親 Issue #386。文書集合の全件線形走査・毎文書の `term_freq`（`DocEntry`）を撤去し、クエリ語ごとに `postings[t]` だけを辿って `doc_idx` 添字のスコアアキュムレータへ加算する共通コア `score_by_postings` を `search`（`ScoreScope::All`）・`search_within`（`ScoreScope::Visible`。可視集合を `doc_idx` ビットマップへ変換し統計の母数も同じ走査で確定させる）で共有。RLS 相当のテナント境界縮約契約〔`search_within` がインデックス全体の `doc_count`／`avg_doc_len`／`doc_freq` を一切参照しない〕・スコアの f64 ビット一致は不変（`crates/engine/tests/hybrid_profile_accept.rs::profile_sparse_index_replica_matches_real_search_within`・`tests/sparse_cache_recall.rs` cold/hot 等価性・`tests/hybrid_recall.rs` 層 A 固定値アサーションで検証済み）)実装済み。`make bench-hybrid-profile` 実測: `hybrid_search_cached_index`（実 hybrid 経路）25.3ms→9.7ms、`search_within_fetch_k=25000`（全可視集合）2.6〜3.4ms→1.27ms、`approx_heap_bytes()` 約18.65MiB→約11.59MiB（`docs: Vec<DocEntry>` 撤去分）。詳細・前後比較は `docs/design/hybrid-rrf-latency-breakdown.md`「Issue #390」節参照。`SparseIndex::score_by_postings` の BM25 文書長正規化項を文書長クラス別テーブル参照へ、Top-k 選出を 2k バッファ＋`select_nth_unstable_by` 型へ置換（Issue #391。式・演算順は旧実装と完全に同一のためスコアはビット一致。256 段ロッシー fieldnorm 量子化はビット一致契約に反するため不採用〔Rejected〕と判断。`make bench-hybrid-profile` 実測は`docs/design/hybrid-rrf-latency-breakdown.md`「Issue #391」節参照）実装済み。疎側再取得ループの再スコアリング回避（Issue #392。`hybrid.rs::sparse_refetch_loop` が各ラウンドで呼び直していた `SparseIndex::search_within` を、`sparse.rs::score_within`（新設）による 1 回のスコア計算＋`SparseScored::top`（新設）による前方一致な Top-k 拡張へ置換。融合結果・境界同点グループ・`fetch_k` スケジュールは不変であることを機械検証。`make bench-hybrid-profile` 実測は`docs/design/hybrid-rrf-latency-breakdown.md`「Issue #392」節参照）実装済み。転置索引化（Issue #386 Phase 1・#388〜#392）後の Recall 非劣化・cold/hot 等価・決定性の回帰検証（Issue #393。`crates/engine/tests/sparse_cache_recall.rs` へ RLS 部分可視・未知語のみクエリ・空クエリの cold/hot 等価性テストを追加し、統計縮約オラクル〔不可視行を含まない対照 DB との結果一致〕でテナント境界縮約の非漏えいを固定。`crates/engine/tests/sparse_determinism.rs` を新設し `docs/design/rrf-tie-break-determinism.md` の同点タイブレーク契約が `SparseIndex::search_within`/`score_within`/`SparseScored::top`・`hybrid_search` の疎側再取得ループでも維持されることを固定。3 Recall ゲート（hybrid・rerank・query-planning）の層 A 固定値アサーションは無変更のまま green、層 B 実測値の導入前〔`8bfaaa4`〕/導入後の前後比較は `docs/design/sparse-inverted-index-recall-verification.md` 参照。production コード〔`crates/engine/src/`〕は無変更・テスト専任）実施済み。疎索引転置化（Issue #386 Phase 1・#388〜#392）の設計判断・データ構造・維持契約・外部実装参照（tantivy〔MIT〕・qdrant〔Apache-2.0〕。手法名と採否のみ）と `feature_bench` 13 フェーズ／`bench-hybrid-profile` 段別の導入前（`8bfaaa4`）→導入後の通し前後比較（`hybrid_rrf` 13 フェーズ中で最大の改善対象。`feature_bench` p50 90,800us→11,507us・約 7.9 倍改善、他 12 フェーズは非退行〔比 0.98〜1.04x・ノイズ帯内〕。`hybrid_rrf`／`vector_knn` 比率は before 10.63x→after 1.38x）を `docs/design/sparse-inverted-index.md` に記録（Issue #394。production コード無変更・docs 専任）実施済み。HNSW 探索（ef ビーム探索・top-k）と brute-force 対照 Recall 単体テスト（Issue #405・親 Issue #402・前提 Issue #404。`hnsw.rs::HnswIndex::search`・`HnswSearchScratch`を追加し、上位層 `ef=1` 貪欲降下 → 層 0 を幅 `ef.max(k)` でビーム探索 → 上位 `k` 件を返す。visited 集合を `VisitedSet` trait でジェネリック化し、構築経路〔世代カウンタ方式 `VisitedScratch`〕・探索経路〔1 ノード 1 bit のビットマップ方式 `VisitedBitmap`〕の双方から `search_layer` が共有する構成へ変更。`crates/engine/tests/hnsw_search.rs` の層 A（常時実行・縮小フィクスチャ）・層 B（`#[ignore]`・`make hnsw-search-recall`・N=10,000・dim=128）で brute-force〔`kernel::CpuScalarProvider`〕対照の Recall@10 を実測——クラスタ構造ありフィクスチャ・既定パラメータで ef=64／256 いずれも 1.0000（受け入れ基準 0.95／0.99 を上回る）。同じ規模での一様乱数のみのコーパス（クラスタ構造なし・HNSW にとって最難条件の一つ）は informational 参考値として ef=64 で 0.6410・ef=256 で 0.9535 と明確に低下することもあわせて記録し、受け入れ判定はクラスタ構造ありフィクスチャの範囲に限定されることを明記した。同一索引・同一クエリでの結果再現性〔新規スクラッチ・再構築索引・重複ヘビーコーパスの同点 id 昇順を含む〕をあわせて固定。`search_engine.rs` 結線・並列構築・世代整合キャッシュ・RLS 統合・`EXPLAIN` 露出・Recall ゲート接続・永続化はいずれも別タスク〔#406〜#413〕の担当のまま。詳細は `docs/design/hnsw-search.md` 参照）実装済み。HNSW 構築の並列化（Issue #406・親 #402・前提 #404/#405。`hnsw.rs::HnswIndex::build_with_threads`／`build_parallel`・新設 `hnsw/parallel_build.rs`。pgvector の要素単位ロック・エントリポイント更新のみ排他というロック粒度設計、qdrant の逐次プレフィックス方式〔先頭 `SEQUENTIAL_PREFIX_NODES`＝256 件を単一スレッドで構築し孤立成分の発生を防ぐ〕を参考に、残りを `AtomicUsize` によるワークスティール方式で並列挿入する。ノード単位 `RwLock` で隣接リストを保護し、`parallel_search.rs::thread_count_for`／`WorkerBudgetGuard`〔`pub(crate)` 化・挙動不変〕を構築側とも共有。`threads==1` または `n<=SEQUENTIAL_PREFIX_NODES` では `build` と完全に同一のグラフを返す契約を機械検証。`score`／`select_neighbors_heuristic`／`shrink_links` の計算本体を逐次・並列で共有する純粋関数へ切り出し、逐次経路の挙動・既存テストは無変更のまま維持。実装中に発見した並列固有のレース（挿入中のノード自身の隣接リストが他ノードの逆方向リンクと同時に伸び次数上限を超え得る）を自己 `shrink_links` 呼び出しで解消。並列構築版でも `parallel >= sequential - 0.02` の Recall@10 同水準・探索の決定性契約は不変であることを固定。100k 点でのスレッド数ラダーベンチ（`make bench-hnsw-parallel-build`。手動専用・CI 非配線）でスレッド数に応じた構築時間短縮を実測。詳細は `docs/design/hnsw-parallel-build.md` 参照）実装済み。HNSW の RLS 事前フィルタとの統合（Issue #409・親 #402・前提 #408。候補マスク `crate::hnsw::NodeMask`・`HnswIndex::search_masked` を新設し、`hnsw.rs::search_layer` の受理判定をマスク対応化（`None` は既存 `search` とビット同一）。`sql::hnsw_cache::Overlay` へ `visible_mask`／`visible_in_index` を追加し、`search_with_overlay` を「可視候補数 ÷ 索引ノード数」の比 `HnswParams::full_scan_ratio`〔既定 1/10・整数比 `Ratio`〕による plain scan／マスク付き ANN 探索の切替へ書き換え、`k + stale_nodes` オーバーフェッチ方式〔#408 の既知の限界〕を撤去。SCALAR 事前フィルタ付き DISTANCE（`Subset` 形状）を `sql::hnsw_cache::search_subset_or_fallback`〔per-query 写像・キャッシュ非登録〕として新規結線し、`sql/exec.rs` の適用条件を `FullVisible`／`Subset` の 2 形状へ拡張。Rust API（`VectorCore::search`）を `rls.rs::PrefilterSnapshot::search_with_hnsw` として初めて HNSW キャッシュへ結線（既存テストの契約を「ANN 経路を使わない」から「既定エンジン対照 Recall@10 ≥ 0.9・可視外非混入」へ意図的に反転）。テナント境界は ADR〔`ann-index-adoption.md`〕の「事後フィルタ不採用」判断を維持し、索引は ctx 可視アリーナのみから構築（候補マスクはテナント境界ではなくクエリ時点の候補集合と索引ノードの差分を表す装置）。`EXPLAIN` 露出・hybrid 密側 ANN 化・Recall ゲート同一閾値検証は対象外（#410〜#412）。詳細は `docs/design/hnsw-rls-cardinality-switch.md` 参照）実装済み。フィルタ付き ANN の境界再取得（iterative scan 型）と fail-closed 検証（Issue #410・親 #402・前提 #408〜#409。DISTANCE 経路の「結果不足」（`masked_short`）は #409 以降構造的に到達不能と判明したため `ef` 倍増ループは追加せず、iterative scan の実体は hybrid 密側再取得ループ（`hybrid.rs::hybrid_search_boosted` の `dense_fetch_k` 倍増）とみなし、`sql/hnsw_cache.rs` の索引・オーバーレイ解決（`prepare_full_visible`／`prepare_subset`）と Top-k 探索（`search_prepared`）を分離したうえで、hybrid 密側専用アダプタ `sql::hnsw_hybrid::HnswDenseProvider`（`SearchInput` の ids／vectors が捕捉済みバッファと同一の場合のみ索引経路を使う fail-closed 受理条件）を新設し `sql/exec.rs` の `Ranking::Hybrid` 分岐へ結線。`hybrid.rs`・`SearchProvider` trait は無変更。停止性は既存の `dense_cap`／`MAX_FETCH_K` が provider 非依存に保証し、`k > MAX_EF` は `ef_cap_fallbacks` 統計を伴う brute-force 縮退で空集合の誤返却を防止。同点誘発コーパスでの停止性・決定性、既定エンジン対照 Recall@10 ≥ 0.9・可視外テナント非混入・非 vacuous（`hybrid_dense_searches > 0`）を固定するテスト（`crates/engine/tests/hnsw_cache.rs`・新設 `tests/hnsw_hybrid_refetch.rs`）を追加。詳細は `docs/design/hnsw-hybrid-iterative-scan.md` 参照。Phase B（再開型スキャン）は受け入れ基準が既存設計で満たされるため実装見送り（Rejected）と判断）実装済み。`EXPLAIN` への検索エンジン種別・ANN 静的適用判定の露出（Issue #411・親 #402・前提 #405〜#410。`sql/hnsw_cache.rs::classify_ann_plan`（`sql/exec.rs` の 4 ANN 適用条件と単一情報源を共有）・`sql/explain.rs::ExplainEngine`／`build_explain_result` を新設し、既存 6 行の `mode_source` 直後へ`engine:`（`parallel_brute_force`／`cpu_scalar_brute_force`／`hnsw`／`(custom_provider)`）・`engine: hnsw` のときのみ `hnsw_params:`（構築時静的パラメータのみ）・`ann_plan:`（`plain_scan_engine`／`plain_scan_precision`／`hnsw_full_visible`／`hnsw_subset`）を追記。既定エンジン時の出力は追記のみで後方互換。実行時縮退結果・可視カーディナリティ・行数・`full_scan_ratio` 等のテナント存在情報に繋がる値は非露出のまま、`EXPLAIN` は検索本体を実行しない契約（索引の `lookup`／`prepare_*` 不呼び出し）を維持。詳細は `docs/design/explain-search-engine-exposure.md` 参照）ANN opt-in 時の Recall ゲート同一閾値検証と TASK-121 系増分回帰の拡張（Issue #412・親 Issue #403。ADR `docs/design/ann-index-adoption.md` B 案の受け入れ条件「SEARCH 系 Recall ゲートを ANN 有効経路でも同一閾値で通過」「TASK-121 系増分回帰の ANN 対応」。SQL 表層のみが ANN の実 seam〔`sql::hnsw_cache`／`sql::hnsw_hybrid`〕へ到達できる production API であるため、3 つの Recall ハーネス〔`hybrid_recall.rs`・`rerank_recall.rs`・`query_planning_recall.rs`〕の層 B に `RECALL_ENGINE` 環境変数opt-in（既定 brute_force・`crates/engine/tests/fixtures/recall_engine.rs::RecallEngine`）を追加し、`hnsw` 指定時は `EngineCore::from_storage_with_engine` ＋ `ORDER BY HYBRID(...)` 経由で同一閾値を判定する。ローカル実測では全 6 測定点で brute_force と hnsw の Recall が完全一致（詳細は `docs/design/ann-recall-gate-verification.md` 参照）。`.github/workflows/recall.yml` へ `RECALL_ENGINE` を注入する経路として当初 `workflow_dispatch.inputs.recall_engine` を追加したが、schedule 実行では ANN 経路が未測定のままになる不整合（PR #438・codex-review 指摘）を受け、`strategy.matrix.recall_engine`（`[brute_force, hnsw]`）で両エンジンを trigger を問わず常時実行する方式へ変更（`workflow_dispatch.inputs.recall_engine` は撤去）。TASK-121 系（増分反映の初回構築・overlay・再構築の各状態）の ANN 対応検証を `crates/engine/tests/incremental_index_hnsw.rs` として追加。production コード〔`crates/engine/src/`〕は無変更・テスト専任）実施済み。ANN 導入（Phase 3・#404〜#412）の前後比較（Issue #413。親 #402／ADR `docs/design/ann-index-adoption.md` B 案の受け入れ基準「(B 案条件 1) 損益分岐点の実測」を事後確認。`feature_bench` 13 フェーズ〔25,000 行・100,000 行〕・`bench-knn-profile`（S0-cold／S0-hot）の前後比較を `docs/design/hnsw-index.md` に記録。既定エンジンは production コード〔`crates/engine/src/`〕無変更のため退行の定義自体が成立せず、hnsw opt-in 有効時（after/既定 vs after/hnsw の同一バイナリ比較）はフィルタなし DISTANCE・hybrid 密側・ホットパスで 6〜33% 高速化する一方、SCALAR 事前フィルタ付き DISTANCE〔`hnsw_subset` 経路〕は 37〜45% 悪化することを実測。25k〜100k の 2 規模点では明確な損益分岐点は観測されず、フィルタの選択性が主要因で行数規模は副次的という所見を得た。参照した外部実装（qdrant・pgvector・usearch）の既定値は上流ドキュメントで確認できた値のみ帰属。`crates/engine/benches/harness/bench_engine.rs`〔`BENCH_FEATURE_ENGINE`／`BENCH_FEATURE_SCALE`／`BENCH_KNN_PROFILE_ENGINE` の fail-closed パース〕を新設し、`feature_bench.rs`・`knn_profile_bench.rs` へ ANN opt-in・規模上書きの計測条件を追加。production コード〔`crates/engine/src/`〕は無変更）実施済み。HNSW 構築比較（Issue #406 追記）へ対照エンジン `hnsw_rs`（`=0.3.4`）を一時追加して 3 エンジン共通の L2 正規化コーパスで 100k 点の実測を行ったが（PR #451）、構築 3.4〜4.8 倍・探索約 5 倍遅く対照は usearch で足りるとのオーナー判断（2026-09-05）で撤去済み（`deny.toml` の bincode ignore と計測時間の倍増も解消。実測は履歴として `docs/design/hnsw-parallel-build.md`「hnsw_rs（`=0.3.4`）を加えた 3 エンジン比較」節に残置。`bench-hnsw-compare` は自作・usearch 2 エンジン・L2 正規化方式を維持）。パラメータ等価性の深掘りは Issue #446〜#450 のツリーへ申し送り。production コード〔`crates/engine/src/`〕は無変更）実施済み。他 DB との機能別横断ベンチと GPU 実機検証（PR #451 後続・Issue #406 関連。`scripts/crossdb_bench/`〔Python ハーネス・Cargo 依存追加なし〕で self〔wire 経由〕・pgvector・sqlite-vec・Qdrant・LanceDB・MySQL を 25,000 行・dim 128 で機能別に計測し、`crates/engine/benches/gpu_scaling_bench.rs`〔`make bench-gpu-scaling`〕で `engine::gpu_batch` の規模 × バッチ別スイープ、FAISS〔faiss-gpu-cu12〕・Qdrant GPU 索引構築との対照を RTX 3060 実機で実施。計測中に発見した wire-server の簡易クエリ応答 40ms 下限〔Nagle × delayed ACK〕を accept 直後の `TCP_NODELAY` 設定で解消〔`crates/wire-server/src/server.rs`・回帰テスト `tests/wire_nodelay_latency.rs`。production 変更〕。あわせて hnsw_rs 対照エンジンを撤去。広域取得 5 フェーズ〔Top-200/1000・`id`+`body`・ANN 候補幅 max(64, k) 統一〕を追加し、self のスカラー列投影時に `sql/exec.rs::on_visible_row` の全行 `scan_scalar_columns` による k 非依存の約 10 ms 固定コストを特定〔遅延デコードは Issue #453・広域取得モードの構文追加は Issue #454 へ起票〕。詳細・実測表は `docs/design/crossdb-bench.md` 参照）実施済み。性能判定の計測規約を `docs/design/benchmark-judgement-policy.md` として集約（Issue #462。交互 N≥5 ペア・per-run 生データ必須・min-of-N＋median 併記・固定 ±5% と参照区間実測の 2 種ノイズ帯・共有 QEMU 環境の証拠力区分・後続 perf Issue 向け受け入れ条件テンプレート。production コード無変更・docs 専任）実施済み。`vector_knn` 786µs（`docs/design/crossdb-bench.md`）の wire／SQL 表層／距離カーネル・Top-k 内訳を切り分ける計測ベンチを追加（Issue #463。`crates/wire-server/benches/knn_wire_profile_bench.rs`〔`make bench-knn-wire-profile`〕・`benches/harness/knn_wire.rs`・`tests/knn_wire_profile_accept.rs`。既定エンジン `ParallelSearchProvider` 上で wire e2e・SQL 表層〔`execute_sql_in_session`〕・単線/並列 provider・距離カーネル単体を交互計測し、共有開発環境の 1 回実測では SQL 表層区分〔T2−T1p〕が距離カーネル自体より支配的〔55.4% 対 29.6%〕、wire 区分〔T3−T2〕は 11.9% にとどまることを確認。spec 由来の閾値を持たない情報提供専用のため CI 非配線・`GITHUB_ACTIONS` 下は fail-closed 拒否。詳細・実測表は `docs/design/knn-wire-stage-profile.md` 参照。production コード〔`crates/engine/src/`・`crates/wire-server/src/`〕は無変更）実施済み。`agg_count`／`rls_isolation`／`vector_knn_where`（`docs/design/crossdb-bench.md` で self が最劣後する3フェーズ）の段別プロファイル計測を実施し、redb 全行走査・ヘッダデコード・RLS 判定・dim/metadata デコード・`WHERE` 述語評価の内訳と後続 Issue（#477・#471）への帰属を記録（Issue #464・親 #456・ルート #455。`crates/engine/benches/scan_stage_profile_bench.rs`・`benches/harness/scan_stage_profile.rs`・`tests/scan_stage_profile_accept.rs`・`make bench-scan-stage-profile`。詳細・実測表は `docs/design/scan-stage-profile.md` 参照。production コード〔`crates/engine/src/`〕は無変更・テスト・ベンチ・docs 専任）実施済み。`hybrid_rrf`（Issue #392 適用後）の最新基線を `bench-hybrid-profile`（engine 内 B0s〜B8）・`bench-hybrid-wire-profile`（engine/SQL 表層/wire。新設）で交互複数ラウンド再計測し、SQL 表層固定コスト・疎側再取得ループがほぼ同水準（37〜39%）で最大区分であることを確認、Phase 6（Issue #548）へ引き継ぎ（Issue #465・親 #456・ルート #455。`crates/engine/benches/harness/hybrid_profile.rs`〔`HybridProjection`・投影種別付き文組み立て・`bucket_diff`・帰属レンダリング〕・`hybrid_profile_bench.rs`〔ラウンド計測セクション〕・`crates/wire-server/benches/harness/hybrid_wire.rs`・`hybrid_wire_profile_bench.rs`〔いずれも新設〕。詳細・実測表・引き継ぎ内容は `docs/design/hybrid-rrf-latency-breakdown.md`「最新基線」節参照。production コード〔`crates/engine/src/`・`crates/wire-server/src/`〕は無変更・テスト・ベンチ・docs 専任）実施済み。チップ別手動計測手順と結果記録テンプレート（Issue #469・親 #456。`crates/engine/benches/chip_bench.rs`・`harness/chip.rs`・`make bench-chip`。dot_kernel／knn_profile／feature_bench〔dim 128・768〕を 1 ワークロード = 1 プロセスでラウンドロビン交互計測し CPU 情報・実行時検出 ISA・per-run 生データ・min/median/参照区間帯を `summary.json` へ出力。CI 非配線・手動専用。記録テンプレートは `docs/design/chip-kernel-guidelines.md` §7。production コード無変更）実施済み。自作 BM25 疎索引の score_by_postings が呼び出しのたびに新規確保していたスコアアキュムレータ（acc: Vec<f64>）を、索引（SparseIndex）の寿命内で再利用する有界スクラッチプールへ置換（Issue #546・親 #545。SparseIndexCache（Issue #357）が保持する Arc<SparseIndex> と寿命が一致する再利用バッファとし、&self シグネチャ・スコアの f64 ビット一致・RLS 相当のテナント境界縮約契約はいずれも不変。詳細は docs/design/hybrid-rrf-latency-breakdown.md「Issue #546」節参照。前後比較実測は Issue #547 の担当）実装済み。バッチ検索 GPU 経路のクエリタイル化（Issue #532。`gpu_batch.rs::DOT_SHADER_WGSL`/`DOT_SHADER_F32_WGSL` の dispatch 構造を「1 dispatch = 1 クエリ」から「`PolicyContext` 単位にグループ化したクエリを最大 `GPU_QUERY_TILE_MAX`〔実装既定値 16〕本まで 1 dispatch へタイル化」する方式へ変更し、常駐行列の行データ読み込みをタイル幅ぶんのクエリで償却する〔`workgroup` 共有メモリは使わない設計上の簡略化〕。結果の Top-k 選出は引き続き CPU 側 `TopKSelector` が担う。実測での前後比較は依存先 Issue #533 へ申し送り）実装済み。macOS 上の `is_aarch64_feature_detected!` 実効性を静的解析で確認済み（Issue #468。`crates/engine/examples/detect_features.rs`（`make detect-features`）・`.github/workflows/detect-features.yml`〔GitHub ホステッド `macos-latest`。情報提供専用〕。既存 NEON 経路は Apple ターゲットで常に有効と静的解析（`neon`/`fp16`/`fhm`/`dotprod` の コンパイル時 target_feature 化）で判明。実機（GitHub ホステッド `macos-latest`・オーナー実機）での実測は workflow run 反映待ち。詳細は `docs/design/chip-kernel-guidelines.md`「7. macOS 上の `is_aarch64_feature_detected!` 実効性」節参照。production コード無変更・テスト・docs 専任）実施済み。零埋め固定長バッファによる分岐なし tail（TASK-156・CORE-14。AVX2／AVX-512／NEON。Issue #528。順序保存形のみを実装し `unsafe` 個数は不変・既定経路（`SimdKernel::dot`）は現行のスカラー tail のまま不変。詳細は `docs/design/dot-kernel-branchless-tail.md` 参照。既定切替の採否は Issue #529 へ申し送り）実装済み。wgpu での部分 Top-k（bitonic／radix select）と SUBGROUP 可用性の設計 doc を `docs/design/gpu-batch-topk.md` に作成（Issue #535・親 #534・Phase 5 親 #460。#536 で実装する方式〔workgroup 内 bitonic 部分 Top-k＋SUBGROUP 有効時の subgroup shuffle 段＋CPU `TopKSelector` 最終マージ〕、SUBGROUP 非対応・`k` 上限超過時の全量 readback への fail-closed 縮退、naga 30.0.1 が `enable subgroups;` を拒否する制約、RTX 3060 の `adapter.features()` 実測〔SUBGROUP／SUBGROUP_BARRIER 有効・subgroup 32/32〕を記録。再現用の手動専用 example `gpu_adapter_info`〔`make gpu-adapter-info`〕を追加。production コード〔`crates/engine/src/`〕は無変更）実施済み。workgroup 内部分 Top-k シェーダ（共有メモリ上の bitonic ソート網。ADR 決定 1「候補 B」の subgroup shuffle 段は naga 30.0.1 がバリア・subgroup builtin の一様性を検証しない制約〔上記 doc §1.3〕を踏まえ意図的に見送り、候補 A 相当に統一）と CPU 側 `TopKSelector` 最終マージを実装（Issue #536。`gpu_batch.rs::DOT_SHADER_TOPK_WGSL`/`DOT_SHADER_TOPK_F32_WGSL`〔`topk_dot_shader!` マクロで S1 を両常駐形式が共有〕・`GpuReadbackMode`/`select_readback_mode`・`GpuTopKParams`・`plan_partial_topk_chunk_rows`・`merge_partial_topk_readback`・`dispatch_partial_topk`・`GpuBatchStats`/`stats()`。Top-k パイプライン生成失敗は既存の段階的 fail-closed 縮退〔`init_gpu_context`/`init_f32_contrast_pipeline` の独立 error scope〕で全量 readback へ吸収し、CORE-16 公平性のため f32 常駐対照経路 `GpuF32ContrastBackend` にも同型の Top-k パイプラインを用意。本開発環境〔RTX 3060〕実機での結合テストで、既定の部分 Top-k 経路と全量 readback 経路〔`GpuSearchTestOptions::force_full_readback`・`bench-internals` feature 限定〕の結果が `(id, score.to_bits())` 列としてビット同一であること、非有限スコア行の非混入、`stats()` の非 vacuous 性〔`partial_topk_dispatches > 0`〕を確認済み。詳細・実装スコープの申し送りは `docs/design/gpu-batch-topk.md`「実装記録」節参照。前後比較・readback バイト数の実測を実施し ADR ステータスを Accepted へ更新済み（Issue #537。readback バイト数は 12.66〜12.79x 削減を確定的カウンタで確認、レイテンシは共有 QEMU 環境の参考値ながら 6 規模点中 5 点で一貫した改善方向。詳細は `docs/design/gpu-batch-topk.md`「前後比較実測（Issue #537）」節参照）実装済み。受理判定後 prefetch（Issue #490）の前後比較実測を実施し、8 規模点（10k／100k・dim 128／768・マスク有無）のうち 7 点は悪化方向への一貫したシグナルは無いが、1 点（10k×dim128・可視率50%）で両ノイズ帯を超える一貫した悪化が観測され、静的解析／実アセンブリの裏付けが無いため撤回条件は完全には満たさず保留（production 無変更・専有実機再実測をオーナーへ申し送り。Issue #491・`docs/design/hnsw-search.md`「Issue #491」節参照）実施済み。単文 INSERT の段別プロファイル（パース・台帳・content_hash・redb commit・wire 応答）を計測（Issue #484。親 Issue #483。crossdb ベンチ〔docs/design/crossdb-bench.md〕の `ingest_single_stmt` 経路を対象に、engine 側は `crates/engine/benches/ingest_profile_bench.rs`（`BENCH_INGEST_PROFILE_MODE=single` 新設）で P0 `parse_bind`（`validate_insert`→`get_table_schema`→`bind_insert_form`）・E0 `typed_row_api`（`tenant::insert_typed_row`）・S0 `sql_surface`（`EngineCore::execute_sql_in_session`）の 3 tier と、生 redb レプリカによる I1〜I8（既存 `StageId` 8 段を single モード向けに再利用）を計測。wire-server 側は新規 `crates/wire-server/benches/ingest_wire_profile_bench.rs`（`make bench-ingest-wire-profile`）で W0 `wire_roundtrip`（simple query 経由）・S0 `sql_surface` を交互ラウンド計測し wire ＝ W0 − S0 として帰属する。engine 内部段の計測フックは production〔`crates/engine/src/`〕へ追加せず公開 API・生 redb レプリカのみで構成する設計判断（`docs/design/ingest-stage-profile.md`「Issue #484 追記」節参照）。共有環境の 1 回実測では I8〔commit〕が engine 内部段の約 73% を占め、wire 区分〔W0−S0〕は W0 min-of-R の約 26%（実測参照区間帯 7.49% を上回る）——いずれも参考値で採否根拠にしない。production コード〔`crates/engine/src/`・`crates/wire-server/src/`〕は無変更・テスト・ベンチ・docs 専任）実施簡易クエリ応答（`DataRow` 群）を上限付きバッファへ組み立て 1 回の `write_all` で送出する方式へ変更（Issue #481。`crates/wire-server/src/response_buffer.rs`・`result_encoder.rs::encode_data_row_into`／`encode_ready_for_query`。上限超過時はフレーム境界で分割送出する fail-safe 設計。既存 wire テストは無変更のまま green。共有開発環境での before/after 実測は `docs/design/wire-response-buffering.md` 参照。専有環境再実測はオーナー作業）実装済み。凍結後 CSR 化（構築は可変長・凍結時に平坦化）の設計と決定性契約への影響を `docs/design/hnsw-index.md` §14 に整理（Issue #493・親 #492・#402・#403。実装は #494・前後比較実測は #495（3 ベンチ・N=5 交互計測で実施済み。共有 QEMU 環境の参考値。全区間ノイズ帯内で一貫した悪化は観測されず、`approx_heap_bytes` は §14.6 見積りと方向・オーダーが整合する削減を実測。詳細は `docs/design/hnsw-index.md` §14.13 参照。専有環境再実測はオーナー作業）。production コード無変更）作成済み。SQL 表層集計（`COUNT`/`SUM`/`AVG`/`MIN`/`MAX`）の `GROUP BY` なし・`WHERE` なし・`DecodeTier::Fast` に限り、`user_rows/{table}` の全行走査を省略する可視ビットマップの世代整合キャッシュ（Issue #478・`sql/visible_cache.rs::VisibleBitmapCache`。`SqlArenaCache`〔#363〕・`SparseIndexCache`〔#357〕と同型の fail-closed 契約でテーブル単位世代キーにより失効。ヒット時は ヘッダデコード・RLS 可視判定・TABLE-12 検査を一切行わず `visible_ids` を反復するのみで集計する。`DimAndScalar`/`Embedding` tier・`GROUP BY`・SELECT DISTANCE/hybrid 経路〔`SqlArenaCache` が既に担う〕への拡張はスコープ外。Issue #464 の実測環境での前後比較実測で `agg_count`／`rls_isolation` とも約 32 倍高速化を確認。詳細は `docs/design/visible-bitmap-cache.md` 参照）実装済み。前後比較実測は #495）作成済み。凍結時の CSR 平坦化と `search_layer` の CSR 参照を実装（Issue #494。`hnsw.rs::GraphBuilder`（可変長ビルダー表現。`insert_node`・`connect`・`shrink_links`・`repair_reachability`・`bfs_reachable`・`greedy_descend` を本体無変更で移設）・新設 `hnsw/csr.rs::CsrGraph`（exact-length・順序保存の CSR。`levels`／`node_base`／`offsets`／`links` の 4 配列）・共有インターフェース `hnsw.rs::Adjacency` trait（`level_of`／`neighbors`／`node_count`）を追加し、`search_layer` の本体を `search_layer_in<V, P, A: Adjacency>` へジェネリック化して構築中・凍結後の双方から共有する。`HnswIndex` は `nodes: Vec<Node>` の代わりに `graph: CsrGraph` を保持し、`build`・並列構築（`parallel_build::freeze`）はいずれも `repair_reachability` 完了後の最終段で `HnswIndex::freeze_from` により 1 回だけ CSR へ平坦化する（stage 順序 `level_assign → sequential_prefix → parallel_phase → freeze（assemble）→ repair_reachability → flatten`）。`HnswBuildProfile` へ `pub flatten` フィールドを追加（逐次縮退経路は `Duration::ZERO`）。CSR 化前後でグラフがビット同一であることを `tests/hnsw.rs::graph_fingerprint_is_stable_across_representation_change`（実装前コミット時点で採取した FNV-1a 64bit 固定値との照合）で機械検証し、既存の全 HNSW テストは無変更のまま green（`HnswIndex` を直接組む構造体リテラルのみ新設ヘルパ `index_from_nodes` へ呼び出し形を追随）。新規 `unsafe` なし。links 範囲 prefetch は申し送り（#489 系）。`serial_share` への `flatten` 加算・前後比較実測（Issue #495。`docs/design/hnsw-index.md` §14.13・`docs/design/hnsw-parallel-build.md`「Issue #495 追記」節）は実施済み。詳細は `docs/design/hnsw-index.md` §14.12 参照）実装済み。接続処理モデル（1 接続 1 スレッド ＋ 接続数上限。固定スレッドプールは不採用）の判断記録を`docs/design/wire-connection-model.md` に作成し、production 定数 `MAX_CONNECTIONS`（64）そのもので accept ループを通す上限ガード回帰テストと同時接続数 N 別スループット手動計測ハーネス（`make bench-wire-concurrency`）を追加（Issue #482・親 #480・#457・ルート #455。上限ガード自体は TASK-69・WIRE-5／WIRE-6 で既実装のため production コード無変更）実施済み。単文 INSERT 経路の上位段（スキーマ取得の一本化・tokenize 1 回化・embedding 複製排除）を Durability 契約不変のまま改善（Issue #485。詳細は `docs/design/ingest-stage-profile.md`「Issue #485 追記」節参照）実装済み。HNSW 索引ノードの f16 常駐表現と F16C／NEON fp16 デコード付き dot カーネル（Issue #514・親 #513・ADR #508 決定 1〜5 準拠。`isa.rs` の `F16cToken`／`NeonFp16Token`・`F16Kernel`、`hnsw.rs::ResidentPrecision`〔`ValidatedHnswParams::with_resident_precision` の opt-in・既定 F32〕、`f16.rs` 共有変換、`EXPLAIN` `hnsw_params:` への `resident=` 追記、`check_simd_codegen.sh` の `vcvtph2ps`／`fcvtl` 期待命令検査。最終スコアの `kernel::dot` f32 再計算契約は不変・既定エンジン無変更。詳細は `docs/design/hnsw-f16-resident.md`）実装済み。HNSW `repair_reachability`（到達不能ノード探索・再接続）の並列化（Issue #449・親 #402・前提 #447・#448。`bfs_reachable` の `HashSet` から `NodeMask`（ビットマップ）への置換・冗長 BFS の省略・フェーズ 1 最近傍探索の `nearest_reachable`／`repair_workers_for` による並列化を `hnsw.rs`・`hnsw/parallel_build.rs` に実装。`build`／`threads=1` のグラフはビット同一のまま不変（既存 `graph_fingerprint_is_stable_across_representation_change` で確認済み）。詳細・実測は `docs/design/hnsw-parallel-build.md`「Issue #449 追記」節参照。SEARCH 系 Recall・RLS 統合・`EXPLAIN` 露出はいずれも無変更）実装済み。repair 逐次段削減（#447〜#449）の 100k 点フルラダー前後比較実測・Recall 非劣化検証（Issue #450。`repair`（外側）段が threads=12 で約 12.5 倍短縮・並列由来の到達不能ノード増分が約 1/17（#448 受け入れ条件「1/10 以下」達成）を確認。3 Recall ゲート（hybrid・rerank・query-planning）は repair 削減前後で実測値が完全一致し非劣化。詳細は `docs/design/hnsw-parallel-build.md`「Issue #450 追記」節・`docs/design/ann-recall-gate-verification.md`「Issue #450 追記」節参照。production コード無変更・テスト・docs 専任）実施済み。可視比率（1/2・1/4・1/10・1/20・1/50）× 行数（25k・100k）での `hnsw_subset`（SCALAR 事前フィルタ付き DISTANCE）と plain scan の損益分岐点計測（Issue #487。`crates/engine/benches/knn_profile_bench.rs` へ `BENCH_KNN_PROFILE_VISIBLE_RATIO`／`BENCH_KNN_PROFILE_FULL_SCAN_RATIO`／`BENCH_KNN_PROFILE_SCALE` opt-in スイープモードを追加し、`make bench-knn-visible-ratio`〔`scripts/bench_knn_visible_ratio_sweep.sh`〕から交互 N ペアで実行する。共有 QEMU 環境・N=3 ペアでの実測では、本フィクスチャ〔均等分散マスク `id % N == 0`・一様乱数ベクトル〕は 1/2〜1/10 のいずれでも `ann_masked` ではなく `mask_splits_graph`（連結性検査による plain scan 縮退）を観測し、`full_scan_ratio` 既定値〔1/10〕の妥当性そのものは未確定のまま。詳細・実測表・判断根拠は `docs/design/hnsw-rls-cardinality-switch.md`「可視比率 × 行数の損益分岐点実測（Issue #487）」節参照。production コード〔`crates/engine/src/`〕は無変更・テスト・ベンチ・docs 専任）実施済み。ANN opt-in hybrid 密側再取得の再開型探索（pgvector `hnswscan.c` 型の破棄候補ヒープ保持）について、状態保持（クエリ単位の所有・寿命・メモリ上限・並行性）・決定性（現行の再実行型と bit 一致する範囲——ラウンド 1・exhaustive 終了時——と一致を保証しない範囲の等価クラス）・停止性（`dense_cap`／`MAX_FETCH_K` による有界性の維持）の契約を設計済み（Issue #504・親 #503。`docs/design/hnsw-hybrid-iterative-scan.md`「Phase B 再検討（Issue #504）」節。production コード無変更・docs 専任。実装は #505、前後比較実測は #506）。SHADER_F16 対応アダプタで f16 算術版 S0 シェーダを選択する経路を追加し、非対応時は unpack 版へ fail-closed に縮退（Issue #539。オーバーフローガード〔`select_dot_shader`〕・境界同点許容つき Recall の実機検証を含む。前後比較は #540。詳細は `docs/design/gpu-batch-f16-arith.md` 参照）実装済み。f16 算術版 S0 シェーダ（#539）の前後比較と CORE-16 ゲートへの影響を記録（Issue #540。`gpu_scaling_bench.rs` へ opt-in `BENCH_GPU_SCALING_QUERY_F16_EXACT`（クエリを f16 厳密往復可能な値へ丸める。`harness/gpu_scaling.rs::round_to_f16_exact`）を追加し、`f16_arith_dispatches`/`f16_arith_guard_fallbacks`（既存カウンタ）が非 vacuous に切り替わることを確認。CORE-16 ゲート本体・規模点診断（`batch_bench.rs`）へも `BENCH_VERBOSE=1` 時の同カウンタ出力を追加したところ、既定のクエリ生成（任意精度 f32）では f16 算術版シェーダが構造的に一度も選ばれないこと（`dispatches=0`・常に unpack 版へ縮退）を確認した。fixture のクエリを f16 厳密往復可能へ変更するかはゲート意味論の変更＝spec／オーナー判断として申し送り。詳細・レイテンシ参考値は`docs/design/gpu-batch-f16-arith.md`「8. 前後比較実測」節・`docs/design/core16-f16-resident-gate.md`「Issue #540 追記」節参照。production コード〔`crates/engine/src/`〕は無変更・テスト・ベンチ・docs 専任）実施済み。f16 f16 常駐時の Recall 3 ゲート同一閾値検証と可視外非混入テスト（Issue #515・親 #513。`crates/engine/tests/fixtures/recall_engine.rs::RecallEngine::HnswF16`（環境変数トークン `hnsw_f16`）を追加し、3 つの Recall ハーネス（`hybrid_recall.rs`・`rerank_recall.rs`・`query_planning_recall.rs`）の層 B を `hnsw_f16` でも測定できるよう結線（Issue #412 と同型の測定経路）。ローカル実測では brute_force／hnsw／hnsw_f16 の全 8 測定点で Recall 実測値が完全一致し、`f16_residency_fallbacks`（D6 自動縮退カウンタ）はいずれも 0 だった（詳細は `docs/design/ann-recall-gate-verification.md`「Issue #515 追記」節参照）。`tests/hnsw_cache.rs` の R4・hybrid・Rust API・`Subset` 形状・`full_scan_ratio` ANN 側の 5 テストを `run_*(precision)` 共有本体化し F32／F16 双方を検証する形へ拡張し、範囲外成分（`|x| > 65504.0`）による自動縮退（D6）が SQL 表層経由でも非漏えい・Recall を維持したまま fail-closed に働くことを固定する新規テストを追加。`tests/hnsw_hybrid_refetch.rs` の同点誘発コーパス停止性・決定性テストも F16 常駐版を追加。`.github/workflows/recall.yml` の `strategy.matrix.recall_engine` を `[brute_force, hnsw]` から `[brute_force, hnsw, hnsw_f16]` へ拡張。production コード〔`crates/engine/src/`〕は無変更・テスト・CI・docs 専任）実施済み。crossdb 横断ベンチ（2026-09-18・Apple M4 Max・NoSQL 対照追加）で劣後した 6 フェーズの原因分析と是正（Issue #843・#844・#845・親 #840・ルート #839。`sql/exec.rs` の `cache_fast_path_eligible`／`defer_projection` が hybrid を無条件除外し疎索引キャッシュヒット時も `VectorArena` を全行複製していた点を `(!is_hybrid || skip_sparse_accumulation)` へ緩和〔`hybrid_rrf`・`bulk_hybrid_k200`〕。スカラー列二次索引がヒットしても候補行へ `on_visible_row` を再適用する Issue #474 の多層防御を「索引で完全被覆された述語（`classify_scalar_plan != PlainScan` かつ残余述語なし・`resolve_candidates == Use`・索引↔スナップショット同一性ガード）に限り信頼する」形へオーナー承認〔2026-09-18〕のもと限定緩和し、`sql/exec.rs::mask_trusted_defer`・`sql/aggregate.rs::count_star_only`・`sql/group_by.rs::observe_group_count_only` として実装〔`vector_knn_where`・`bulk_knn_where_k200`・`where_compound_count`・`group_by_having`〕。テナント境界は索引が `(table, ctx)` 可視スナップショット由来のため不変。交互 5 ペア A/B〔min-of-N・p50〕で `hybrid_rrf` 4,641→1,471µs・`bulk_hybrid_k200` 6,185→1,517µs・`vector_knn_where` 998→246µs・`bulk_knn_where_k200` 1,603→389µs・`where_compound_count` 860→90µs・`group_by_having` 1,431→70µs、結果集合・Recall 層 B ゲート実測値は before と一致。`scan_where_nosort_k500`〔wire 分〕・`ingest_single_stmt`〔durability 契約差〕は対処せず。詳細は `docs/design/crossdb-loss-analysis-20260918.md` 参照）実装済み。バッチ検索の実 GPU バックエンドへ opt-in 専用の i8 パック常駐・`dot4I8Packed` シェーダ経路（`gpu_batch::packed_i8::GpuI8BatchBackend`。Issue #542・親 #541。行単位対称 SQ8 量子化・4 要素/u32 パック・候補生成専用〔最終スコアは `kernel::dot` の f32 再計算〕。`FallbackBatchEngine` の primary へは未接続・既定エンジン無変更。詳細は `docs/design/gpu-batch-i8-packed.md` 参照）実装済み。`bench-gpu-scaling` への i8 経路計測行追加・brute-force 対照 Recall・前後比較実測（Issue #543。`benches/harness/gpu_scaling.rs`・`benches/gpu_scaling_bench.rs`・`tests/gpu_batch_i8.rs`・`scripts/bench_gpu_scaling_ab.sh`。3 規模点 × N=5 ペアの全 run で Recall@10 劣化なし〔`i8_recall_at_k=1.0000`・`i8_mismatch=0`〕を確認した一方、クエリタイル化未実装〔#542 doc §6 の既存申し送り〕が原因と分析される速度低下〔既存 f16 常駐経路比で最大 19.4x 遅い・共有環境の参考値〕を実測。`DEFAULT_I8_OVERSAMPLE` は無変更のまま維持。詳細・実測表は `docs/design/gpu-batch-i8-packed.md`「前後比較実測（Issue #543）」節参照。production コード〔`crates/engine/src/`〕は無変更・テスト・ベンチ・docs 専任）実施`VisitedSet` trait へ第 3 実装 `VisitedSparse`（`HashSet<u32>`。faiss `VisitedTable` 方式を参考）を追加し、マスク付き探索（`HnswIndex::search_masked_with`）が可視候補数（`NodeMask::count_ones`。Issue #497 で O(1) 化）で`VisitedBitmap`／`VisitedSparse` を切り替える機構を実装（Issue #497。閾値 `ValidatedHnswParams::sparse_visited_max`〔`with_sparse_visited_max` opt-in・既定 0＝常に dense・既存動作不変〕・`EXPLAIN` `hnsw_params:` への `sparse_visited_max=` 追記・計測用 knob `BENCH_KNN_PROFILE_SPARSE_VISITED_MAX`〔`make bench-knn-profile`〕まで。閾値既定値の確定・可視比率別 before/after 実測は Issue #498 へ申し送り。詳細は `docs/design/hnsw-search.md`「visited 集合の 3 実装」節参照）実装済み。f16 常駐（`hnsw_f16`）と f32 常駐（`hnsw`）の前後比較・常駐メモリ実測（Issue #516・親 #513。`knn_profile_bench.rs` へ `BenchEngine::HnswF16`・hot-only モード（`BENCH_KNN_PROFILE_HOT_ONLY`）・索引単体メモリモード（`BENCH_KNN_PROFILE_INDEX_MEMORY`。子プロセス隔離）を追加し `make bench-knn-f16-resident`（`scripts/bench_knn_f16_resident_ab.sh`）から交互 N≥5 ペアで実行する。500k×768 の SQL 表層経路は `VectorArena` の 1 GiB 上限で構造的に到達不能なため索引単体メモリ計測のみで補う。詳細・実測値は `docs/design/hnsw-f16-resident.md`「Issue #516 追記」節参照。production コード〔`crates/engine/src/`〕は無変更・テスト・docs 専任）実施済み。SQL 表層集計・GROUP BY 経路（sql/aggregate.rs・sql/group_by.rs）をスカラー列二次索引（sql::scalar_index::ScalarIndex）の候補削減・GROUP BY キー列挙経路へ結線（Issue #475・親 #472・#359・前提 #473・#474。索引対応述語のみの WHERE を持つ集計・WHERE なしの GROUP BY（列挙形）・索引対応述語ありの GROUP BY（候補走査形）を対象とし、候補行には常に matches_all・式述語を再適用する多層防御を維持。SELECT を先に流さなくても集計クエリ自身の走査から piggyback で索引を構築（非 vacuous 化）。agg_count（WHERE なし COUNT(*)）は Issue #478 の VisibleBitmapCache 経路のまま無変更。25,000 行実測（交互 N=5 ペア・共有環境の参考値）で where_compound の p50 が約 9 倍、group_by_having が約 2.9 倍改善し、agg_count・参照区間（vector_knn・rls_isolation）は非退行。詳細は docs/design/scalar-index-aggregate.md 参照）実装済み。HNSW 索引ノードの対称 SQ8（i8）量子化常駐表現（Issue #521・親 #520。`crates/engine/src/sq8.rs` 新設。次元ごと min/max 由来のスケールで凍結時に1 回量子化する `ResidentPrecision::I8`〔既定 F32・opt-in〕。クエリ側は量子化せず f32 のまま扱う非対称設計〔`f16.rs` の昇格 dot と同型〕とし、整数 i8×i8 dot カーネルは対象外〔#522・#524〕。索引ヒットの最終スコアは常に `kernel::dot` の f32 再計算のまま不変。詳細は `docs/design/hnsw-sq8-resident.md` 参照。production コード〔`crates/engine/src/`〕は `hnsw.rs`・`hnsw/prefetch.rs`・`sql/hnsw_cache.rs` へ結線済み）実装済み。HNSW 索引ノードの整数 i8×i8 dot カーネル（VNNI 512bit／256bit・i16 widen フォールバック。Issue #522・親 #520・前提 #521。`crates/engine/src/isa.rs`〔`I8Kernel`／`AvxVnniToken`／`Avx512VnniToken`。`Avx2FmaToken` を widen 経路に再利用〕・新設 `crates/engine/src/isa/x86_i8.rs`・`crates/engine/src/sq8.rs`〔`I8_DOT_MAX_DIM`／`row_sums`／`Sq8QueryCodes`／`prepare_query`〕・新設 `crates/engine/src/hnsw/i8_query.rs`〔`PreparedI8Source`〕。クエリ側も量子化する二重量子化＋整数カーネルへ候補生成スコアを切替え、`hnsw.rs::search_masked_with_hop` が探索 1 回につき 1 回だけクエリを準備して `NodeSource` 経由で結線する（`NodeSource` trait 自体は無変更）。整数演算のため ISA 間ビット一致。`prepare_query` 失敗時は復号 dot（#521）へ fail-closed 縮退。索引ヒットの最終スコアは常に `kernel::dot` の f32 再計算のまま不変。`unsafe` は isa.rs 内のディスパッチ 3 箇所のみ（計 11 箇所）。`scripts/check_simd_codegen.sh` の `{vex}` encoding hint 接頭辞誤認バグ（AVX-VNNI の禁止命令・期待命令検査が素通りしていた）を修正のうえ i8 カーネル向け検査を追加。詳細は `docs/design/hnsw-sq8-resident.md`「Issue #522」節・`docs/design/simd-codegen-guard.md`「6.1」節参照）実装済み。aarch64 NEON dotprod（`vdotq_s32`）版の i8×i8 dot カーネル（`NeonDotprodToken`・`isa/neon_i8.rs::dot_i8_neon_dotprod`。Issue #525・親 #520・前提 #522。`unsafe` は `isa.rs` の `I8Kernel::dot_i8` `NeonDotprod` 分岐 1 箇所のみ〔計 12 箇所〕・レジスタ構築は `vsetq_lane_s8` 昇順連鎖〔`vcombine_s8`＋`vcreate_s8` は禁止パターン `mov v.d[1]` を残すため不採用と実測確認〕。`scripts/check_simd_codegen.sh` へ `19dot_i8_neon_dotprod`／`sdot v.4s` 検査を追加。1.98.0 toolchain での aarch64 cross-check・codegen 検査は green・x86_64 実行時証跡は「x86 版との整数一致」を既存の共有シードテストで機械検証。詳細は `docs/design/hnsw-sq8-resident.md`「Issue #525」節・`docs/design/simd-codegen-guard.md`「6.2」節参照）実装済み。`ORDER BY`／`USING PLAN` を伴わない広域取得（ソートなしのフィルタ取得。`SELECT ... [WHERE ...] LIMIT n`）を SQL 表層へ追加（Issue #454。`sql/allowlist.rs::Statement::Scan`〔BREAKING CHANGE〕・`sql/parser.rs::bind_scan`・新設 `sql/scan.rs::execute_scan`。`sql::aggregate::execute_aggregate` と同じく `VectorArena` を経由しない redb 直接走査で `VECTOR` 列を持たないテーブルでも動作し、可視かつ `WHERE` を満たす行が `LIMIT` 件集まった時点で走査を打ち切る〔早期終了〕。順序保証なし・取得モード〔`recall`/`precision`〕の適用対象外。`USING MODE`・`HINT ORDER`・`EXPLAIN` 前置は引き続き `42601`。契約は spec ビヘイビア ID（SQL-15・TASK-170・vector-db-spec#12）付与済みの実装既定値として `docs/design/wide-retrieval-scan.md` に記録。`crossdb_bench` の `scan_where_nosort_k500` は self でも受理されるようになった〔実測値の再取得はオーナー作業〕）実装済み。hybrid 密側再取得ループへ破棄候補ヒープ保持型の再開型探索を実装（Issue #505・親 #504・#458・#455。`hnsw.rs::ResumableMaskedSearch`・`HnswIndex::search_masked_resumable_start`／`search_masked_resume`（`HopMode::OneHop` 限定。`search_layer_in` 本体は不変・複製しない設計）。`sql::hnsw_cache::search_prepared_resumable`・`HnswResumeState`・診断用統計 `hybrid_resumed_rounds` を追加し、`sql::hnsw_hybrid::HnswDenseProvider` が `Mutex<Option<HnswResumeState>>` でクエリ単位に状態を保持して 2 ラウンド目以降を再開する。ラウンド 1 は既存単発経路とビット同一・exhaustive 終了時はブルートフォース対照と厳密一致することを機械検証済み。一方で同点誘発コーパス（Issue #410 fixture）での前後比較実測では、旧経路（毎ラウンド新規探索）が masked_short 0/12 だったのに対し再開型は 9/12 へ悪化する制約を確認したが、原因は「visited の単調性」ではなく候補復帰ループが自己昇格ノードを走査対象から取りこぼす実装バグであると判明し、fix 923efcf で修正済み（SQL クエリの正しさ・決定性は無変更のまま）。fix 後の再測定・性能上の採否は Issue #506 へ申し送り。詳細は `docs/design/hnsw-hybrid-iterative-scan.md`「実装記録（Issue #505）」「原因の訂正（fix 923efcf）」節参照HNSW 索引ノードの I8（SQ8）常駐（Issue #521）・整数 i8×i8 dot カーネル（Issue #522）opt-in の Recall 3 ゲート同一閾値検証と前後比較（Issue #523・親 #520。`crates/engine/tests/fixtures/recall_engine.rs::RecallEngine::HnswI8`（環境変数トークン `hnsw_i8`）を追加し、3 つの Recall ハーネス（`hybrid_recall.rs`・`rerank_recall.rs`・`query_planning_recall.rs`）の層 B を `hnsw_i8` でも測定できるよう結線（Issue #412・#515 と同型の測定経路）。ローカル実測では brute_force／hnsw／hnsw_f16／hnsw_i8 の全 8 測定点で Recall 実測値が完全一致し、`i8_residency_fallbacks`（D6 自動縮退カウンタ）はいずれも 0 だった（詳細は `docs/design/ann-recall-gate-verification.md`「Issue #523 追記」節参照）。`tests/hnsw_cache.rs` の R4・hybrid・Rust API・`Subset` 形状・`full_scan_ratio` ANN 側の 5 テストへ `i8_*` ラッパーを追加し、次元ごとスケールの f32 アンダーフローによる自動縮退（D6）が SQL 表層経由でも非漏えい・Recall を維持したまま fail-closed に働くことを固定する新規テストを追加。`tests/hnsw_hybrid_refetch.rs` の同点誘発コーパス停止性・決定性テストも I8 常駐版を追加。`tests/hnsw_i8_recall.rs`（新設）の brute-force 対照 `ef` 掃引（`ef ∈ {64, 128, 256}`）実測では、クラスタ構造ありコーパスで I8 の Recall@10 が F32 対比 `ef` を増やしても同じ約 9.5% のギャップで頭打ちになり oversampling（`ef` 引き上げ）では解決しないことを確認したが、Recall ゲート本体（実コーパス規模・hybrid 密側再取得ループ経由）では実害が及ばないため既定 `ef_search` は無変更のまま維持と判断（詳細は `docs/design/hnsw-sq8-resident.md`「Issue #523 追記」節参照）。`.github/workflows/recall.yml` の `strategy.matrix.recall_engine` を `[brute_force, hnsw, hnsw_f16]` から `[brute_force, hnsw, hnsw_f16, hnsw_i8]` へ拡張。production コード（`crates/engine/src/`）は無変更・テスト・CI・docs 専任）実施済み。`crossdb_bench` の `scan_where_nosort_k500` は self でも受理されるようになった〔実測値の再取得はオーナー作業〕）実装済み。同点誘発コーパスでの再取得発火時レイテンシ前後比較を記録（Issue #506。`hybrid_latency_bench.rs` へ `BENCH_HYBRID_LATENCY_ENGINE` opt-in の SQL 表層（hnsw opt-in）計測モードを追加し（既定モードの出力は不変）、`scripts/bench_hybrid_latency_ab.sh`（`make bench-hybrid-ab`）で before〔`838c53e`〕/after〔`4ceb6b5`〕をN=5 ペア交互実測。fix `923efcf` 後は同点誘発コーパスでも `masked_short=0`（旧経路と同水準まで回復）を確認したが、レイテンシは条件依存で改善（複数ラウンド発火時・約19%改善）・悪化（単発ラウンドでも生じる固定コスト・約8.6%悪化）の両方が観測された（共有 QEMU 環境の参考値。専有環境再実測はオーナー作業）。docs/spec 未チェックアウトのため Recall 3 ゲートは閾値未設定の明示 no-op を確認するに留まり、既存の #412/#515 記録（brute_force/hnsw/hnsw_f16 完全一致）がそのまま成立すると判断。詳細は `docs/design/hnsw-hybrid-iterative-scan.md`「前後比較実測（Issue #506）」節参照。Apple M 実機での i8／f16／f32 経路の前後比較の計測ドライバ・記録テンプレートを整備（Issue #526。`scripts/bench_knn_f16_resident_ab.sh` へ複数候補輪番 `AB_CANDIDATE_ENGINES`（`hnsw_f16 hnsw_i8`）と macOS 環境記録 fallback を追加し、`make bench-knn-precision-resident` から f32／f16／i8 の 3 精度を同一セッションで一括計測できるようにした。`knn_profile_bench.rs` の `run_hot_only`／`run_index_memory_mode` へ実行時ディスパッチされた 3 経路（`isa::current()`／`current_f16()`／`current_i8()`）の ISA を記録する `kernel_isa` 行（非 vacuous 証跡）を追加。手順・記録テンプレートは `docs/design/chip-kernel-guidelines.md` §7.7 に整理し、本環境（QEMU x86_64）でのスモーク実測（`kernel_isa dot=Avx2Fma f16=F16c i8=Avx2Widen`・全 run 完走）を x86 参考値として記録した。Apple 実機での実測値そのものはオーナー申し送り。production コード〔`crates/engine/src/`〕は無変更・テスト・ベンチ・docs 専任）実施済み。Phase 4（チップ最適カーネル群・親 #459・ルート #455）通しの着手前 SHA（`91f6a18`）／適用後 SHA（`6184491`）前後比較とチップ別最速判定を実施（Issue #530。`scripts/bench_chip_ab.sh`（新設）・`make bench-chip-ab` により `chip_bench`〔Issue #469〕を before/after 2 状態ディレクトリで交互 min-of-N 実行するドライバを整備し、本開発環境（共有 QEMU）で `dot_kernel`・`knn_profile` ワークロードを AB_PAIRS=5 で実測——dim≥768（ACC=4 ディスパッチ対象域）で dot カーネルが min-of-N 比 最大 47% 改善する一方、参照区間（`S1_redb_scan`／`S2_header_decode`）自体が実測ノイズ帯（reference_band。before+after 10 run プール）で 99.5%／159.9%（`benchmark-judgement-policy.md` §4）を示し本環境のノイズ床が固定 ±5% 帯を大きく超えることを確認した。3 チップ（Apple M／AMD Zen／Intel）実機の実測値そのものは引き続きオーナー申し送り。詳細は `docs/design/phase4-chip-before-after.md` 参照。production コード〔`crates/engine/src/`・`crates/wire-server/src/`〕は無変更・テスト・ベンチ・docs 専任）実施済み。Phase 3（ルート #455・親 #458。#486〜#507・#446 ツリー）通しの前後比較を実施し、`bench-hnsw-compare`（usearch 対照。N=5 ペア・rows=20,000 縮小構成）・`bench-knn-profile`（既定エンジン対照。共有環境のリソース逼迫により N=1 ペア）・`feature_bench`（hnsw opt-in 13 フェーズ・1 arm のみ N=3 ペア）・Recall 3 ゲート層 B（`RECALL_ENGINE` 切替。決定的コーパスのためペア測定対象外）を before（`4d2bd23`）/after（`799a7d8`）で対象別に異なる実施条件で実測した（Issue #507。`vector_knn`〔フィルタなし DISTANCE〕の 21.7 倍高速化という比率自体は 3/3 run で一貫するが、`ingest` 参照区間の実測帯（101.1%）を run1 込みの規約どおりの全幅で算出すると変化幅 95.4% はこれを超えず、`benchmark-judgement-policy.md` §4 の判定基準では両ノイズ帯を超える有効な変化と確定できない〔#563 投影遅延デコード（Phase 2）との交絡もあり Phase 3 単独の帰属もできない〕。共有 QEMU 環境の参考値・採否根拠にしない。詳細・実測表は `docs/design/hnsw-phase3-before-after.md`、要約は `docs/design/hnsw-index.md` §15 参照。production コード〔`crates/engine/src/`〕は無変更・テスト・ベンチ・docs 専任）実施済み。ScalarIndex 索引対象限定（Issue #632・#638）の crossdb 前後比較実測（Issue #633。`docs/design/scalar-index-generation-cache.md`「前後比較実測（Issue #633）」節。crossdb fixture の `body` 列実測平均〔126.3 バイト〕が除外閾値`MAX_SCALAR_INDEX_COLUMN_AVG_TEXT_LEN`〔128 バイト〕をわずかに下回るため本 fixture では除外が発火せず、`hybrid_rrf` の状態依存退行は本 fixture 上では未解消であることを実測で確認。production コード無変更・テスト・ベンチ・docs 専任）実施済み。ScalarIndex の TEXT 列除外閾値 `MAX_SCALAR_INDEX_COLUMN_AVG_TEXT_LEN` を 128 → 64 へ引き下げ（Issue #644・親 #630・前提 #632。crossdb fixture 相当〔平均 126 バイト〕で除外が発火し 32 バイト程度の短列は索引対象に残ることを単体テストで固定。判断基準・plain scan 縮退契約は不変。前後比較実測は後続 Issue の担当。詳細は `docs/design/scalar-index-generation-cache.md`「閾値の見直し（Issue #644）」節参照）実装済み。Issue #644 の効果を crossdb fixture 上で事後確認（Issue #645。除外発火を `warm_where_then_hybrid.rss_after_warm`（63.73→55.58 MiB）・`body_predicate` の plain scan 縮退（107.3→1765.9µs・ref 同水準）の 2 系統で非 vacuous に確認し、`hybrid_rrf`／`warm_where_then_hybrid` の p50 が ref 比 1.00〜1.04 まで収束したことを確認。共有 QEMU 環境の参考値のため両ノイズ帯を超える確定判定には至らず、専有環境再実測を条件に親 #630 は close 可能と判断。詳細は同 doc「前後比較実測（Issue #645）」節参照。production コード無変更・テスト・docs 専任）実施済み。複数行変更の operation_id 対応づけ設計 ADR（Issue #868・RECOVER-11。Proposed・docs 専任・production コード無変更）作成済み。述語つき UPDATE の行再エンコードを単一行 UPDATE と共有する merge_encode_scalar_columns 経路へ統一（Issue #996。tenant::merge_row_for_update。書き込みバイト列・エラー分類・台帳契約は不変。production コード〔crates/engine/src/tenant.rs〕・テスト・docs）実装済み。他は未実装（タスクは spec リポの `05-tasks.md`（TASK-66〜165）、マイルストーンは `06-roadmap.md`（MS-1〜6）参照）crossdb 横断ベンチの 2026-09-08 再計測（ext4 上・`qdrant/qdrant:latest`=1.19.1）と fixture FS 条件（tmpfs/ext4）による `ingest_single_stmt` の差（tmpfs 7,414〜7,714 対 ext4 約 2,000 rows/s。N<5 のため非退行の断定はできず、FS 条件差が低下要因という仮説を支持する参考値との位置づけ）・`point_where` 重複行の注記を `docs/design/crossdb-bench.md` に記録（Issue #636・親 #629。生データは `docs/design/bench-data/crossdb-20260908/`。production コード無変更・docs 専任）実施済み。検索エンジン選択の CLI opt-in（Issue #656。`wire-server --search-engine default|hnsw|hnsw_f16|hnsw_i8`。`crates/wire-server/src/search_engine_opt.rs` が閉じた語彙 4 値から `engine::search_engine::SearchEngineKind` へ到達する唯一の入口。未指定は既存 `EngineCore::open` 経路のままビット同一・不正値/値欠落/重複指定は fail-closed。選択結果は `EXPLAIN` の `engine:`／`hnsw_params:` 行で確認可能。engine production コード無変更。スカラー列二次索引の候補削減が消費された `VectorArena` 複製をなくし、候補スロットを直接マスクとして探索する経路（Issue #654・親 #650。`arena.rs::filter_cached_rls_rows_subset`・`kernel.rs::SearchProvider::search_subset`。hybrid・HNSW `Subset` 形状を除く DISTANCE 経路が対象。詳細は `docs/design/scalar-index-mask-search.md` 参照）実装済み。フィルタ付き ANN の探索パラメータ CLI opt-in（Issue #657。`--hnsw-full-scan-ratio`／`--hnsw-acorn-max-visible-ratio`／`--hnsw-sparse-visited-max`。`--search-engine` が `hnsw`／`hnsw_f16`／`hnsw_i8` のときのみ受理し `default`／未指定との組合せ・値欠落・形状不正・意味不正・重複指定は fail-closed。未指定時は既定値（`full_scan_ratio`=1/10・`acorn_max_visible_ratio`=none・`sparse_visited_max`=0）のまま不変。`full_scan_ratio`／`acorn_max_visible_ratio` は `EXPLAIN` の `hnsw_params:` 行へ非露出のまま維持（`sparse_visited_max=` は既存どおり露出）。engine production コード無変更・既定値の見直しは Issue #659 へ申し送り）実装済み。hybrid 経路の SQL 表層固定コスト（B1-B4）を S0〜S8 へ再分解する計測を `bench-hybrid-profile`（Issue #660・親 #652・ルート #649。`crates/engine/benches/hybrid_profile_bench.rs`・`benches/harness/hybrid_profile.rs`。パース・束縛・SCALAR 段行ループ構成要素・末尾処理の内訳と改善候補 P1〜P4 の起票案を `docs/design/hybrid-rrf-latency-breakdown.md`「Issue #660」節に記録。production コード無変更・テスト・ベンチ・docs 専任）実施済み。候補 id マスク経路（Issue #654）の前後比較実測と crossdb 再計測（Issue #655。段別プロファイル〔`scan_stage_profile_bench`〕で`e2e(vector_knn_where/W0-hot)` が min-of-N 比 0.5177（約 48% 高速化・参照帯超えで `improved` 判定）、crossdb self（`vector_knn_where`・`bulk_knn_where_k200`）でも min-of-N が改善方向（約 32%・28%）を示したが参照区間ノイズ帯が広く（共有 QEMU 環境・並行実行由来）両ノイズ帯判定での確定的な `improved` 判定には至らず、専有環境再実測をオーナーへ申し送り。詳細は `docs/design/scalar-index-mask-search.md`「前後比較実測（Issue #655）」節参照。選択率 33%（crossdb fixture・`lang='ja'`）での `ann_masked` 到達実測と既定値判断（Issue #659・親 #651。`crates/engine/tests/hnsw_crossdb_selectivity.rs`・`make hnsw-crossdb-selectivity`。in-process ハーネスのカウンタ実測で `default`〔既定 `full_scan_ratio=1/10`〕が `mask_splits_graph`（連結性検査による plain scan 縮退）へ到達することを確認し、Issue #658 の推測を確定。ACORN-1 opt-in （`acorn_max_visible_ratio=4/10`／`1/1`）は regime 判定式上は TwoHop に解決されるにもかかわらずこの分断を一切解消しないことも確認。`full_scan_ratio=2/5`〔fixture 選択率 33.1% を上回る閾値〕候補は wire A/B 参考値でも劣後幅の縮小（`vector_knn_where` 3.22x→1.57x・`bulk_knn_where_k200` 2.59x→1.48x）が一貫。共有 QEMU 環境のため既定値は据え置き、専有環境実測をオーナー・後続 Issue へ申し送り。詳細は `docs/design/hnsw-rls-cardinality-switch.md`「Issue #659」節参照。production コード〔`crates/engine/src/`〕は無変更・テスト・ベンチ・docs 専任）実施済み。HNSW `Subset` 形状の Overlay／BFS 世代整合キャッシュの設計 ADR（Issue #678・親 #673・ルート #672。`docs/design/hnsw-subset-overlay-cache.md`。ステータス Proposed・採否は Issue #677 の前後比較実測結果に条件付け。production コード無変更・docs 専任）作成済み。HNSW `Subset` 形状（SCALAR 事前フィルタ付き DISTANCE）が plain scan へ縮退する場合に限り、候補削減後（`filter_cached_rls_rows_subset` の `kept: Vec<u32>`）に `VectorArena` を複製する前に判定し、候補 id マスク経路（`SearchProvider::search_subset`。Issue #654）へ委譲する（Issue #676。`sql::hnsw_cache::SubsetSlotPlan`・`prepare_subset_from_slots`・`Overlay::compute_over_slots`〔`Overlay::compute` の一般化版〕を新設し `sql/exec.rs` の DISTANCE 段へ結線。ANN 探索へ進む場合は従来どおり複製する。新設カウンタ `subset_mask_scans`／`subset_arena_copies`。`EXPLAIN`・既存 Recall・テナント境界契約は不変。詳細は `docs/design/scalar-index-mask-search.md`「Issue #676」節・`docs/design/hnsw-rls-cardinality-switch.md`「Issue #676」節参照）実装済み。Issue #676（Subset 縮退時委譲）の前後比較を Issue #677 として実施済み（crossdb self exact/hnsw A/B は既定 `full_scan_ratio` で `vector_knn_where` 劣後幅が既存基線比 3.22x→2.62x・`bulk_knn_where_k200` が 2.59x→2.07x へ縮小、`full_scan_ratio=2/5` opt-in ではほぼ等速まで縮小。`scan_stage_profile_bench` へ `BENCH_SCAN_PROFILE_ENGINE`／`BENCH_SCAN_PROFILE_FULL_SCAN_RATIO` を追加し #676 の到達経路〔`plain_scan_mask_split`／`plain_scan_ratio`〕別のレイテンシ差を確認。いずれも共有 QEMU 環境の参考値。詳細は `docs/design/hnsw-rls-cardinality-switch.md`「Issue #677」節参照。production コード無変更・テスト・ベンチ・docs 専任）実施済み。HTTP 要求行パーサ（Issue #740・TASK-173・HTTP-2, HTTP-11。`crates/wire-server/src/http/request.rs`。`POST`／`HTTP/1.1` のみ受理・4 KiB 上限・違反は `08P01`）実装済み。`sql::exec::execute_insert`（SQL-10・TASK-80）・`sql::explain::build_explain_result`（SQL-6・TASK-78。入力の `ExplainEngine`・`AnnPlan`・`ScalarPlan`・`classify_ann_plan`・`classify_scalar_plan` を含む）を `BoundScan`（Issue #726）・`BoundAggregate`（Issue #727）と同じ作法で `pub(crate)` から `pub` へ昇格（Issue #730・TASK-186・NOSQL-6・NOSQL-10 の前提。`execute_insert` は `operation_id` 必須化ガードを関数内部で自己完結して適用するため公開可能と判断し、ファイル形 `execute_file_insert`〔ガードを呼び出し元に依存〕は対象外のまま `pub(crate)` を維持。`ExplainEngine` は `#[non_exhaustive] pub struct`＋`new`／アクセサーで構築し、`AnnPlan`／`ScalarPlan` も `#[non_exhaustive] pub enum` として `sql.rs` から再エクスポート。`crates/engine/tests/sql_insert_explain_public_api.rs` で insert 側の到達性・RLS 境界・`operation_id` 契約と、explain 側の到達性・SQL `EXPLAIN` との行単位完全一致を固定。`PlannedQuery::new`・`using_plan::pre_check_bindable`／`PreCheckShape` は対象外のまま。production の実行意味論・既存呼び出し元は無変更）実装済み。セッション発行・失効・上限のライフサイクル（HTTP-4／5／8）の層 A テスト群を追加（Issue #756。`crates/wire-server/tests/http4_session.rs`。トークン形式（43 文字・base64url アルファベット限定・32 バイト復号・`expires_in` 一致）・TTL 失効（wire 経由の `28000` 収束・`issue` 時一括回収）・同時有効数 256 上限（257 件目 `53300`・接続数上限 64 とは独立）・close ライフサイクルの一連遷移を、既存 `http4_session_issue.rs`・`http5_query_bearer.rs`・`http8_session_close.rs` と重複しない範囲で固定。production コード無変更・テスト専任）実施済み。NoSQL 表層のエンドポイント限定（`/v1/session`・`/v1/session/close`・`/v1/query` のバイト厳密一致のみ受理し、未知パス・非 POST を `08P01` で拒否）を production ルータへ結線（Issue #758・TASK-179・NOSQL-1。`http::router.rs::resolve_target` が単一情報源。`crates/wire-server/tests/nosql1_endpoint_routing.rs`。op 許可リストの正式化は Issue #759 以降の担当）実装済み。`POST /v1/query` の `op` 名を閉じた語彙 4 値（`search`／`scan`／`aggregate`／`insert`）へ分類する許可リストと、語彙外（DDL・UDF 呼び出し・トランザクション制御・UPDATE／DELETE 相当を含む）の `0A000` 拒否を実装（Issue #759・TASK-179・NOSQL-1・NOSQL-9。`wire-server/src/http/query/op.rs::Op`・`classify_op`。`gate.rs` の判定順序を op 許可リスト → スキーマ検証の順に固定）。NoSQL 表層 `POST /v1/query`（`op: aggregate`。`group_by`／`having`／`explain: true` を除く単一行集計）を SQL テキストを組み立てずに `engine::sql::parser::BoundAggregate`／`BoundAggregateItem`（Issue #768・TASK-186・NOSQL-4 で公開）へ直接束縛・実行する経路を実装し、SQL 表層と同一結果（NULL・オーバーフロー `22003`・`VECTOR` 列拒否を含む）を返すよう `wire-server/src/http/query/aggregate.rs`・`ident.rs` を追加、`gate.rs`・`router.rs::Router::with_engine`・`main.rs` を結線済み（Issue #768・TASK-177・NOSQL-4）。`POST /v1/query`（`op: aggregate`）の `group_by`（単一列限定）・`having`（`=`/`<`/`<=`/`>`/`>=` の 5 演算子）を SQL-14 の `GROUP BY` 実行計画へ写像し実行済み（Issue #769・TASK-177・NOSQL-5。`engine::sql::parser::BoundAggregate::new_grouped`〔TASK-186・NOSQL-5。列名解決・HAVING 対象の型検査を SQL テキスト経由の `bind_group_by_clause` と共有〕・`engine::sql::group_by::MAX_GROUPS`（10,000）を `pub` 化し、`wire-server/src/http/query/aggregate.rs` から到達。グループ数・グループキー累計バイト・`HAVING` 述語数の各上限超過は `54000`、`group_by` 要素数不正・`having` 単独指定・非有限値・識別子形状不正は `42601`、`GROUP BY` 列が `TEXT` でない・`HAVING` 参照不整合は `22000` を返す）。`search` op の JSON クエリオブジェクト（`vector`／`plan` 排他・`hybrid`・`mode`・`columns`）を SQL 表層と同一形の `engine::sql::parser::BoundStatement` へ束縛（Issue #763・TASK-175・NOSQL-2。`wire-server/src/http/query/search.rs::bind_search`。投影・ベクトル値・本文列規約・`LIMIT` 範囲の束縛は `engine::sql::parser` の公開ヘルパーへ委譲し第 2 の実行器を作らない。`plan` 指定は LLM 展開が engine 内部 I/O を要するため束縛済み部品のみの `PlanSearch` に留め、`BoundStatement` までの完成・実行結線・`gate.rs` 暫定応答の置換は後続 Issue の担当）実装済み。NoSQL 表層 `POST /v1/query`（`op: aggregate`。`group_by`／`having`／`explain: true` を除く単一行集計）を SQL テキストを組み立てずに `engine::sql::parser::BoundAggregate`／`BoundAggregateItem`（Issue #768・TASK-186・NOSQL-4 で公開）へ直接束縛・実行する経路を実装し、SQL 表層と同一結果（NULL・オーバーフロー `22003`・`VECTOR` 列拒否を含む）を返すよう `wire-server/src/http/query/aggregate.rs`・`ident.rs` を追加、`gate.rs`・`router.rs::Router::with_engine`・`main.rs` を結線済み（Issue #768・TASK-177・NOSQL-4）。`op: "scan"` を束縛済み実行計画（`BoundScan`）へ写像し `EngineCore::execute_bound_scan_in_session` で実行して `{"columns":...,"rows":...,"row_count":...}` を返す NoSQL 表層 `scan` 写像を実装（Issue #766・TASK-176・NOSQL-3。`wire-server/src/http/query/scan.rs`。`vector`／`plan`／`mode`／`hybrid` の付与・`explain: true` はいずれも `42601`。`search`／`aggregate`／`insert` は引き続き暫定 `0A000`／501。第 2 の実行器は作らず `engine::sql::parser::bind_projection`／`validate_search_limit` を `pub` 昇格して共有）。NoSQL `insert` op を SQL-10 と同一の engine 書き込み契約（`operation_id` 必須化 `23502`・台帳照合による再送判定 `23505`／`22023`・INDEX-4 上限 `54000`）へ写像（Issue #771・TASK-178・NOSQL-6。`sql::exec::execute_insert_batch`・`EngineCore::execute_bound_insert_in_session`・`wire-server::http::query::insert`。`gate.rs` への結線は対象外・#772 の担当）実装済み。NoSQL 表層 `POST /v1/query`（`op: search`）の `vector`／`plan` 双方を `EngineCore::execute_bound_search_in_session`／`execute_bound_plan_search_in_session`（TASK-186・NOSQL-2）へ結線し、SQL 表層の検索 `SELECT`／`USING PLAN` と同一の fail-closed 判定順序・precision 確信度ゲート（SEARCH-9）・RLS 暗黙適用（RLS-7）で実行するよう `gate.rs`・`wire-server::http::query::search` を実装（Issue #764・TASK-186・NOSQL-2・SEARCH-9・RLS-7。`USING PLAN` 経路の I/O 前後の fail-closed 手順は `core.rs::EngineCore::run_using_plan_select` へ抽出し SQL テキスト経由・束縛済み計画経由の双方で共有〔第 2 の実行器を作らない設計〕。`explain: true` は `vector` 指定 `42601`・`plan` 指定 `0A000`〔NOSQL-10・Issue #765 の担当〕で拒否。詳細は `docs/design/bound-plan-session-entry.md` 参照）実装済み。NoSQL 表層 `op: search`・`explain: true` を検索本体を実行せず SQL 表層 `EXPLAIN SELECT ... USING PLAN(...)` と同一内容の `{"explain":[...]}` 応答へ写像（Issue #765・TASK-186・NOSQL-10。`core.rs::EngineCore::explain_bound_plan_in_session`（`Statement::Explain` アームと私的ヘルパー `run_explain_plan` を共有）・`sql::explain::ExplainShape`（旧 `sql::using_plan::PreCheckShape` を公開型へ昇格）・`wire-server::http::query::explain`。`vector` 指定・`plan` 未指定はいずれも `42601`、`aggregate`／`scan` の `explain: true` も同じ `42601` へ統一。`gate.rs` は `explain: true` を通常の `search` 実行より必ず先に判定する match の腕順序で結線）実装済み。`insert` op の成功応答 `{"inserted","operation_id"}` と `gate.rs` 結線（Issue #772・TASK-178・NOSQL-6・TABLE-12・RLS-9。`insert::handle`・`InsertSuccess`・`encode_success_body`。他テナント保持 id／未存在 id の応答同一性と同一テナント重複 `23505` を `crates/wire-server/tests/nosql6_tenant_row_id_scope.rs` で固定。engine production コード無変更）実装済み。`insert` 写像の契約全体（`operation_id` 必須化・台帳照合による再送判定・INDEX-4 処理量上限・TABLE-12 同一テナント内 `id` 衝突・RLS-9 秘匿）の層 A 結合テスト群を実装（Issue #773・TASK-178・NOSQL-6。`crates/wire-server/tests/nosql6_insert.rs`。production ルータ経由の生バイトクライアントで `23502`（欠落・`null`・空文字）・`23505`／`22023`（表層を跨いだ再送判定の一致を含む）・INDEX-4 の 4 上限（①は SQL 経路とのパリティを含む。③④は SQL 表層に複数行 `INSERT` 構文が無いため共有 Rust 入口を経由した HTTP 契約としてのみ検証。②は行形の TEXT／VECTOR 合計バイト長へも同じ per-file 上限が適用されるため境界値で検証）・束縛エラー（空 `rows`・バッチ内 `id` 重複・未存在テーブル・判定順序）・TABLE-12・RLS-9（重複拒否応答バイト列が他テナント行の有無で完全一致）を固定。詳細は `docs/design/nosql-insert-mapping.md` 参照。production コード無変更・テスト・docs 専任）実装済み。 ERR-4 分類境界 (a)〜(f) 全類型の `wire_code` と HTTP ステータス射影を層 A で網羅検証済み（Issue #775・TASK-180。`crates/wire-server/tests/err4_http_projection.rs`。`42501`・`P0002` は NoSQL 表層の実要求からは構造的に到達不能なため production の応答エンコーダ経由の射影検証に限定。production コード無変更・テスト専任）。NoSQL 表層の 3 クライアント統合ハーネスの骨格と curl ランナー（Issue #776・TASK-183／HTTP-13。`crates/wire-server/tests/three_client_http_e2e.rs`・`make e2e-three-client-http`〔opt-in・`ci` 非包含〕。urllib／fetch・失効後 `28000`・SQL 経路パリティは #777〜#779 の担当。production コード無変更）実装済み。同ハーネスへ urllib.request／fetch クライアントスクリプトとランナーを追加（Issue #777・TASK-183・HTTP-13。`crates/wire-server/tests/three_client_http/{urllib_client.py,fetch_client.js}`〔標準ライブラリのみ・依存追加なし〕・`spawn_script_client`／`urllib_post`／`fetch_post`（`PYTHON_BIN`／`NODE_BIN` で解決）・`HttpClient` enum。テスト本体を `run_session_search_close_scenario` へ抽出し curl／urllib／fetch の 3 テストが共有。`make e2e-three-client-http` は 3 テストを一括実行する。production コード無変更・テスト専任）実装済み。同ハーネスへ実行記録出力（`[e2e-record]`。クライアント版・各段の観測要点。秘密情報非含有を機械検証）を追加し `make e2e-three-client-http` を `--nocapture --test-threads=1` へ変更、記録様式・運用手順を `docs/design/three-client-e2e-harness.md` へ整理（Issue #778・TASK-183・HTTP-13。production コード無変更・テスト・docs 専任）実装済み。同ハーネスへ SQL 経路（psql）と NoSQL 経路の結果一致比較（search-1〜5・scan-1・agg-1〜4 の 10 ケース × alice／bob／carol の 3 テナント。列名・型・行集合の一致と Private 行〔id=11／12〕非混入を検証）を追加（Issue #779・TASK-183・HTTP-13。`crates/wire-server/tests/three_client_http_e2e.rs::run_sql_nosql_parity_scenario`。redb 単一ライター制約により seed 一回→SQL 表層起動・採取→`stop_and_drain`→同一 DB で NoSQL 表層起動・採取の順次 2 プロセス方式。型は同一 SQL 表層プロセスへの生 simple query の `RowDescription` から取得（psql は拡張クエリプロトコル未対応のため）。production コード無変更・テスト・docs 専任）実装済み。是正（#843・#844・PR #853）取り込み後の crossdb 再計測（Issue #848。self・Elasticsearch を含む対照 DB 全系統を交互 N=5 ラウンドで同一セッション再取得。新設 `scripts/bench_crossdb_ab.sh`・`scripts/bench_crossdb_ab_summarize.py`・`make bench-crossdb-ab`。確定的な負けは `scan_where_nosort_k500`（測定条件の非対称）の 1 件のみへ縮小し、`hybrid_rrf`／`bulk_hybrid_k200` は Redis との差が run-to-run ノイズ帯内の「僅差」へ後退。診断用 `CROSSDB_REDIS_HYBRID_WINDOW` opt-in で Redis 側 `WINDOW` を self の pool_depth（200）に揃えても Redis 側レイテンシはほぼ不変であることを実測し、従来の「プール深さ差が主因」という説明は本診断では支持されないと記録。共有機（他 Issue エージェント並行実行下）の参考値。詳細は `docs/design/crossdb-bench.md`「再計測（Issue #848）」節・生データは `docs/design/bench-data/crossdb-20260918T142251Z-ab/` 参照。production コード無変更・テスト・docs 専任）実施済み。`ingest_single_stmt` の durability 別実測（Issue #851・親 #849・#850。`crates/engine/benches/ingest_profile_bench.rs` の single モードへ `BENCH_INGEST_PROFILE_DURABILITY=immediate|none` opt-in を追加し、`scripts/bench_ingest_durability_ab.sh`・`scripts/fsync_probe.py`（新規。macOS `F_FULLFSYNC`/`F_BARRIERFSYNC`/`fsync(2)` 原始操作プローブ）で N=5 ペア交互実測。I8〔commit〕median が `immediate` 約4.58ms→`none` 約8.6µs（ratio≈0.0019）まで縮小し、同一ボリュームの `F_FULLFSYNC` p50〔約4.35ms〕とほぼ同水準であることを確認。crossdb ベンチ本体〔wire 経由〕・Linux ext4 の実測は本 Issue の時間内では未実施のまま `scripts/crossdb_bench/self_durability.py`（`CROSSDB_SELF_DURABILITY` opt-in 結線）を用意し申し送り。詳細は `docs/design/crossdb-bench.md`「Issue #851」節・`docs/design/ingest-write-path.md`「10. Issue #851 追記」節参照。production コード〔`crates/engine/src/`・`crates/wire-server/src/`〕は無変更・テスト・ベンチ・docs 専任）実施済み。UPDATE 文（単一行・id 指定）の許可リスト・束縛を実装済み（Issue #864・TASK-191・SQL-17。実行結線は Issue #865 が対象）。UPDATE（単一行）の実行結線と operation_id 契約を実装済み（Issue #865・TASK-191・SQL-17。`tenant::update_row_columns_unchecked`〔read-merge-write を単一 write トランザクション内で行う列指定の書き込み入口。`update_row_unchecked` と同一の書き込みプリミティブを共有〕・`recovery::content_hash::for_update_columns`〔専用 `OpTag`〕・`sql::exec::execute_update`／`execute_update_with_schema`〔`SqlOutcome::Update`〕・`core.rs::execute_update_sql`／`execute_update_form`（`INSERT`・`TRUNCATE` と同型の先頭トークン覗き見分岐）として実装。0 行更新（他テナント行・未存在 id・RLS 可視集合外のいずれも区別しない）でも台帳記録・テーブル世代進行・commit を必ず行う非対称設計は `truncate_table_unchecked` と同じ。wire 応答は pg 互換 `CommandComplete` タグ `UPDATE <n>`。詳細は `docs/design/update-single-row.md` 参照。NoSQL 表層は対象外〔`op` 許可リストに `update` を含めない〕）実装済み。述語つき `DELETE ... WHERE` の許可リスト・束縛を実装済み（Issue #870・TASK-192・SQL-19。単一行・`id` 完全一致形〔SQL-18・`sql::allowlist::ValidatedDelete`〕とは別型 `sql::allowlist::ValidatedPredicateDelete`・`DeleteStatement` として追加し、`SELECT`／集計／広域取得（scan）と共有する述語表現〔`WherePredicate`・`bind_where_predicates`〕をそのまま再利用。`sql::parser::BoundPredicateDelete`・`bind_predicate_delete`・`DEFAULT_MAX_DML_AFFECTED_ROWS`・`check_affected_row_count`。既存の単一行形・`validate_delete` の受理範囲・挙動は無変更。実行結線・影響行数上限の実測判定・台帳照合は Issue #871 が対象）。`TRUNCATE TABLE <table> USING OPERATION_ID '<id>'` 構文（Issue #874・TASK-195・SQL-22。テーブル定義は残したまま自テナント所有の全行〔`Public`／`Private` を問わない〕を単一 write トランザクションで削除する書き込み系操作。`sql::allowlist::ValidatedTruncate`・`sql::exec::execute_truncate`〔削除件数を一切返さない `TruncateOutcome`〕・`tenant::truncate_table_unchecked`〔`redb::Table::retain_in` によるテナント名前空間レンジ削除〕・`recovery::content_hash::for_truncate`〔入力なし・同一 `operation_id` 再送は常に `23505`〕として実装し、`core.rs::execute_sql_in_session` へ `INSERT` と同型の先頭トークン覗き見分岐で結線。0 行 TRUNCATE でも台帳記録・テーブル世代進行が必ず発生する契約を含め `crates/engine/tests/truncate_table.rs` で検証済み。詳細は `docs/design/truncate-table.md` 参照。NoSQL 表層は対象外〔`op` 許可リストに `truncate` を含めない〕）実装済み。RLS-11（TASK-195。read-your-writes）の engine API・wire・HTTP・表層横断の層 A 結合テスト（`crates/engine/tests/rls11_read_your_writes.rs`・`crates/wire-server/tests/rls11_read_your_writes.rs`）と、3 クライアント統合テスト（`three_client_e2e.rs`・`extended_syntax_e2e.rs`・`three_client_http_e2e.rs`）の seed・期待値を読み取り可視性拡大後の前提へ更新（Issue #974。production コード無変更・テスト・docs 専任）実装済み。SQL 表層 複数行 INSERT（SQL-16・TASK-190）の実行本体結線（`execute_insert_batch` 経由・INDEX-4 上限適用）は先行 PR（#978）で実装済みだったため、Issue #863 では受入基準 4 項目（原子性・INDEX-4 4 上限・バッチ内 id 重複拒否・台帳キー空間共有）を固定する層 A 残差テスト（`crates/engine/tests/insert_multi_row.rs`・`crates/wire-server/tests/wire_insert_operation_id.rs`・`nosql6_insert.rs` の SQL⇄HTTP 複数行パリティ）と doc 整合（`docs/design/sql-multi-row-insert.md`）を追加（production コード無変更・テスト・docs 専任）実施済み。`DELETE FROM <table> WHERE id = <n> USING OPERATION_ID '<id>'`（SQL-18・TASK-191・#867。単一行・`id` 等価指定形）の実行結線と台帳照合を実装（`sql::exec::execute_delete`・`core.rs::execute_delete_sql`／`execute_delete_form`・`execute_sql_in_session` の先頭トークン `DELETE` 覗き見分岐。既存の `tenant::delete_row_unchecked`〔TASK-95・RECOVER-4〕へ委譲し、対象行が他テナント所有／未存在のいずれでも区別せず `DeleteOutcome { rows_affected: 0 }` の成功へ写像〔台帳非記録・テーブル世代不変。RLS-9・RLS-10〕。`wire-server::simple_query` は `CommandComplete` タグ `DELETE <rows_affected>` へ写像。詳細・設計判断は `docs/design/sql-delete-single-row.md` 参照。NoSQL 表層・述語つき `DELETE`・`RETURNING` は対象外）実装済み。UPSERT（`INSERT ... ON CONFLICT (id) DO NOTHING | DO UPDATE SET ...`。SQL-20・TASK-193 の UPSERT 部・Issue #872。字句解析へ `Token::QualifiedIdent`〔BREAKING CHANGE〕を追加し `EXCLUDED.<col>` を受理。衝突判定スコープは物理キー `(tenant_id, id)` の所有〔RLS 可視性ではない〕とし、行制約由来 `23505`〔`IdConflict`〕は構造的に発生させず台帳由来の `23505`〔`DuplicateOperationId`〕のみへ収束させる。`recovery::content_hash::for_typed_upsert`〔`OpTag::Upsert = 7`〕・`tenant::upsert_typed_rows_unchecked`〔read-merge-write〕・`sql::exec::execute_upsert`／`execute_upsert_with_schema` として実装し `core.rs::execute_insert_form` の `BoundInsertForm::Upsert` 分岐へ結線。`DO UPDATE` で行を実際に変更した場合のみテーブル世代を進行させる（全行 `DO NOTHING` は世代不変）。NoSQL 表層・`RETURNING`・`UNIQUE` 制約対象への拡張は対象外。詳細は `docs/design/sql-upsert.md` 参照）実装済み。NoSQL `op` 語彙へ `update`／`delete` を追加（Issue #875・TASK-179・NOSQL-1／NOSQL-9／NOSQL-12 ポインタ。`FieldType::AnyObject`・`UPDATE_SCHEMA`／`DELETE_SCHEMA`。束縛・実行結線は Issue #876、パリティ検証は Issue #877）実装済み。NoSQL `update`／`delete` op の `where`（単一行・`id` 完全一致形）を束縛済み計画で SQL 表層と同一の実行器（`engine::sql::exec::execute_update_with_schema`／`execute_delete`）・同一の台帳キー空間へ結線（Issue #876・TASK-186・NOSQL-6・NOSQL-12。`core.rs::EngineCore::execute_bound_update_in_session`／`execute_bound_delete_in_session`・`wire-server::http::query::{dml_target,update,delete}`。`filter`（述語形）は実行器未接続（Issue #871 の担当）のため `0A000`／501 のまま。複数列 `SET` の宣言順が `content_hash` に影響する既知の制約あり（詳細は `docs/design/nosql-update-delete-mapping.md` 参照）実装済み。述語つき UPDATE/DELETE の実行結線（Issue #871・TASK-192・SQL-19。候補行列挙（テナント所有スコープ）・1 write トランザクション一括適用・内容照合ハッシュ（RECOVER-11。ADR `docs/design/multi-row-dml-operation-id.md`〔Issue #868・ステータス Proposed〕を作業前提として実行結線）・影響行数上限（1,000）超過時の副作用ゼロ拒否を `recovery/content_hash.rs`（`OpTag::UpdateWhere`/`DeleteWhere`）・`tenant.rs`（`delete_rows_where_unchecked`/`update_rows_where_unchecked`）・`sql/exec.rs`（`execute_predicate_delete`/`execute_predicate_update`）・`core.rs` として実装。単一行 UPDATE（SQL-17・Issue #865・PR #989）と `SqlOutcome::Update` を共有し、`core.rs::execute_predicate_update_form` が `ValidatedUpdateForm` の `Single`／`Predicate` 腕で実行本体を振り分ける（`Single` 腕は PR #989 の単一行実行結線〔`sql::exec::execute_update_with_schema`〕へディスパッチ）。詳細は `docs/design/predicate-dml-exec.md` 参照）実装済み。UPDATE/DELETE の SQL・NoSQL パリティ・RLS-9 応答同一性・台帳のプロセス・表層横断永続を実バイナリ・無改造クライアント（psql・curl・urllib・fetch）経由で検証する層 B テストを追加（Issue #877・TASK-183・HTTP-13。`crates/wire-server/tests/three_client_http_e2e.rs::run_sql_nosql_dml_parity_scenario`。詳細は `docs/design/three-client-e2e-harness.md`「DML パリティ（Issue #877）」節参照。production コード無変更・テスト・docs 専任）実施済み。`ColumnType` へスカラー型を追加する基盤整備（Issue #880。カタログ v2〔`v1` は fail-closed に拒否〕・型タグ↔`ColumnType` 往復の集約・未知型タグの確保前拒否。詳細は `docs/design/column-type-extension.md` 参照）実装済み。簡易クエリのセミコロン区切り複数文実行（Issue #938・WIRE-16・TASK-219。`crates/engine/src/sql/statement_splitter.rs::split_statements`／`classify_statement`／`check_write_placement`〔非空文上限 16。超過は `54000`〕による分割・文種別分類を engine 側へ実装し、`wire-server/src/simple_query.rs` の `'Q'` オーケストレーションから呼ぶ。明示 `BEGIN`（SQL-31・RECOVER-12。未実装）を前提にせず、書き込み系文が複数文メッセージの最後の 1 文にのみ許可される制約〔違反は `0A000`〕の下で WIRE-16 の原子性要件を構造的に満たす設計。各文の応答を順に送出し `ReadyForQuery` は最後の文にのみ付ける。途中の文が失敗した場合は `SET`／`CREATE FUNCTION` によるセッション状態をメッセージ受信前へ巻き戻す。詳細は `docs/design/wire-multi-statement.md` 参照）実装済み。`ColumnType` へ `REAL`／`DOUBLE PRECISION` 列型を追加（Issue #882・TABLE-13・TASK-196。`row_codec::Value::Real/Double`・`scalar_float.rs`〔解析・整形・順序規約〕・content_hash タグ Real=5／Double=6。`WHERE`・集計・スカラー索引・NoSQL JSON 束縛への対応は #891〜#896 へ申し送り、暫定 fail-closed 拒否のみ実装。`row_codec::scan_scalar_columns` 系の戻り値を `ScalarRef<'a>` へ変更〔BREAKING CHANGE〕。詳細は `docs/design/float-column-types.md` 参照）実装済み。`ColumnType::Array` による可変長スカラー配列列（TABLE-14・TASK-198、Issue #888。要素型は TEXT／BOOLEAN。二次索引化の対象外とし、既定エンジンでの KNN が配列列の有無でビット同一であることを確認済み。詳細は `docs/design/array-column-type.md` 参照。NoSQL 表層の JSON 配列束縛・SQL `CREATE TABLE` 構文露出は対象外）実装済み。`ColumnType` へ `BYTEA` 列型を追加（Issue #886・TASK-197。TABLE-13・WIRE-13・NOSQL-17。新設 `engine::bytea`（`\x` hex テキスト解析・出力）・`row_codec::Value::Bytes`／`ScalarRef::Bytes`・`sql::exec::Cell::Bytes` を行エンコーダ・SQL 表層（INSERT/UPDATE/UPSERT/RETURNING/`COUNT`）・wire テキスト表現（`\x` 小文字 16 進）まで結線。NoSQL 表層は標準 base64（新設 `wire-server::http::query::base64_std`）を JSON 表現として採用し、`insert`／`update` op から到達可能（型不一致・不正 base64 は `42601`・長さ超過は `54000`。SQL 表層の型不一致 `22000` とは意図的に異なる暫定分類）。`WHERE` 述語・二次索引・式評価・`CREATE TABLE` 構文は対象外。詳細は `docs/design/column-type-extension.md`「#886 追記」節参照）実装済み。`ColumnType` へ `JSON`／`JSONB` 列型を追加（Issue #889・TASK-198。TABLE-14・NOSQL-8・NOSQL-17。共有 JSON パーサー `engine::json::parse_json`（TASK-172）を格納時検証の単一チョークポイント（`row_codec` の encode 系）で使い、`JSON` 列は入力テキスト保持・`JSONB` 列は正規化再シリアライズ（`json::write_canonical`）した文字列を格納する共通の `row_codec::Value::Json`／`ScalarRef::Json` 表現で結線。SQL 表層（INSERT/UPDATE/UPSERT/RETURNING/`COUNT`）・wire テキスト表現（格納テキストをそのまま出力）・NoSQL 表層（`insert`／`update` op で JSON オブジェクト／配列を正規化テキスト化して受理、応答は native JSON 値として再シリアライズ出力）まで結線。`WHERE` 述語・二次索引・式評価・パス演算子（`->`）・`CREATE TABLE` 構文は対象外（JSON パス参照 API は engine の Rust API 限定で追加。SQL/NoSQL への構文露出は spec ID 付与後の別 Issue）。JSON 型は SQL・NoSQL 表層間で `operation_id` 再送判定の非対称（JSONB は正規化のため一致、JSON は入力テキスト保持のため空白差で内容不一致になりうる）が既知の設計判断として存在する。詳細は `docs/design/column-type-extension.md`「#889 追記」節参照）実装済み。SQL 表層に `INTEGER`／`BIGINT` 列型を追加（Issue #881・TABLE-13・TABLE-6・TASK-196。`catalog::ColumnType`・`row_codec::Value`／`ScalarRef`・`sql::exec::Cell::SignedInteger` へ変更し、固定幅バイト表現・数値リテラル束縛・範囲外 `22003`・`WHERE`／集計／NoSQL SET 等の未対応経路の fail-closed 拒否〔`22000`〕を実装。詳細は `docs/design/column-type-integer.md` 参照）実装済み。

`NUMERIC(precision, scale)` 列型（TABLE-13〔検討中〕・TASK-197、Issue #885。自作十進固定小数表現〔`unscaled: i128` + `scale: u8`〕・カタログ型タグ `"numeric"`（`param` は `"p,s"`）・行バイト表現（presence + `i128` LE 16 バイト固定。`scale` は行に持たずカタログの列型のみを正とする）・INSERT/UPDATE/UPSERT リテラル束縛（数値・文字列の両方を受理・half away from zero 丸め・桁あふれ `22003`）・負数リテラル受理（`sql::allowlist::Parser::expect_literal`。非 NUMERIC 列への `-5` は `42601`→`22000` へ変化）・`COUNT` 集計・content_hash（新規タグ 14。origin/main への rebase 時点で ARRAY のタグ 10 と衝突するため採番し直した）・wire/HTTP 正規テキスト出力を一貫して実装。外部クレート非使用（オーナー判断）。WHERE/式評価・SUM/AVG/MIN/MAX・スカラー二次索引・`ALTER COLUMN TYPE`・SQL `CREATE TABLE` 構文は対象外（#891〜#897・#901 へ申し送り）。詳細は `docs/design/column-type-extension.md`「#885 追記」節参照）実装済み。

`ColumnType` へ `ENUM` 列型を追加（Issue #890・TASK-198。TABLE-14・WIRE-13・NOSQL-17。名前付き型（`Storage::create_enum_type`／`get_enum_type`／`alter_enum_type_add_value`／`drop_enum_type`。新設 redb テーブル `enum_types`）としてカタログに登録し、`ColumnType::Enum(Arc<EnumTypeDef>)`（`Copy` 除去）・`row_codec::Value::Enum`／`ScalarRef::Enum`（TEXT と同じ行フレーム）まで結線。語彙外ラベルは新設 `ErrorClass::InvalidTextRepresentation`（`22P02`）で書き込み前に拒否（`sql::parser::bind_enum_literal`・`row_codec` encode 時の二重防御）。`ALTER TYPE ... ADD VALUE` は末尾追記のみ許可し削除・並べ替えは提供しない。`WHERE` 等価述語・`COUNT` はスカラー列二次索引を TEXT と共有（`ScalarRef::as_dictionary_text`）して受理し、`SUM`/`AVG`/`MIN`/`MAX`・`LIKE`・式評価・`USING PLAN`・scoring_boost への露出は BYTEA と同じく拒否。投影・`RETURNING` は既存の `Cell::Text` へ写像。NoSQL 表層は `insert`／`update` でラベルの生 JSON string を受理し同じ `22P02`／`42601` 契約を共有。詳細は `docs/design/column-type-extension.md`「#890 追記」節参照）実装済み。`ColumnType` へ `UUID` 列型を追加（Issue #887・TASK-197。TABLE-13〔検討中〕・WIRE-13。128bit 識別子を `crates/engine/src/uuid.rs::Uuid`（RFC 4122 ネットワークバイトオーダー・厳密な `8-4-4-4-12` 文法検証）として自作実装し、カタログ・行バイト表現（presence + 16 バイト固定）・SQL 表層（INSERT/UPDATE/UPSERT/RETURNING/`COUNT`）・content_hash（タグ 15）・wire/HTTP 正規テキスト出力（小文字整形）まで一貫して結線。厳密文法違反は `22P02`（ENUM と同じ発生経路を共有）。WHERE 述語・式評価・二次索引・`SUM`/`AVG`/`MIN`/`MAX`・NoSQL `insert` op は対象外。詳細は `docs/design/column-type-extension.md`「#887 追記」節参照）実装済み。拡張クエリプロトコル Bind／Execute／Sync／Close／Flush（Issue #934・TASK-71・WIRE-11。`crates/wire-server/src/extended_query.rs` に `ExtendedQueryState`（`PreparedStatementStore`・`PortalStore`・`ignore_till_sync`）を実装し、Bind が対象 statement を Describe 相当で確定して portal（Bind 時点の `ParsedSql` スナップショット）を保持、Execute が `max_rows` による分割送出（`PortalSuspended`）・完了（`CommandComplete`。副作用は再実行しない）を管理する。エラー後は Sync まで後続メッセージを破棄してから `ReadyForQuery` を返し接続を維持する同期回復（フレーム自体が壊れている場合は従来どおり切断）。`crates/wire-server/src/simple_query.rs` から `execute_with_emergency_registration`／`map_outcome`（`SqlOutcome` → 応答形の写像。簡易クエリの応答バイト列は完全に不変）を切り出し Execute と共有（第 2 の実行器を作らない設計）。結果 format code（WIRE-14・#936 のエンコーダ層）を `result_encoder::ResultFormats::resolve`／`validate_binary_formats` により列ごとに解決・事前検査し、Describe(Portal) の `RowDescription`（`encode_row_description_with_formats`）・Execute の `DataRow`（`encode_data_row_into_with_formats`）が Bind 時点で確定した同じ値を参照するよう結線（`TEXT` 列は binary 対応・`id`／`VECTOR`／`Computed`／`BOOLEAN`／`BYTEA` は非対応として `0A000`）。`$n` 束縛・型 OID 推論は WIRE-12・#935、暗黙トランザクションブロックは #942・RECOVER-12・SQL-31、ReadyForQuery の状態バイトは #943・WIRE-19 の担当のまま。詳細は `docs/design/wire-extended-query-bind-execute-sync.md` 参照）実装済み。
新スカラー型（`INTEGER`/`BIGINT`/`REAL`/`DOUBLE PRECISION`/`NUMERIC`/`DATE`/`TIMESTAMP`）の `SUM`/`AVG`/`MIN`/`MAX` 集計・`22003` オーバーフロー契約を実装（Issue #892・TABLE-13・SQL-13・SQL-14・TASK-166・TASK-167・TASK-196・TASK-197。`sql::parser::AggregateInput` へ `IntegerColumn`/`BigIntColumn`/`RealColumn`/`DoubleColumn`/`DateColumn`/`TimestampColumn` を追加し `NumericColumn` を `{index, precision, scale}` へ拡張。`sql::aggregate::Accumulator` へ `IntSum`/`IntAvg`/`IntMin`/`IntMax`/`NumericSum`/`NumericAvg`/`NumericMin`/`NumericMax`/`DateMin`/`DateMax`/`TimestampMin`/`TimestampMax` を追加し、`finish` を `Result<Cell, SqlSurfaceError>` へ変更して `SUM(INTEGER/BIGINT)` の `i64` 範囲・`SUM`/`AVG(NUMERIC)` の 38 桁上限を確定時に検査する（部分和の一時的な超過は許容）。`AVG(NUMERIC)` の結果 scale は `max(s, min(16, 38-(p-s)))`（`numeric::avg_unscaled` による桁ごとの長除算・half away from zero）。`HAVING`（`sql::parser::check_having_target_is_numeric`）は `NUMERIC` 型集計結果・`DATE`/`TIMESTAMP` の `MIN`/`MAX` を `22000` で拒否し、`Cell::SignedInteger` は `sql::group_by::cmp_signed_to_literal` で厳密比較する。`GROUP BY` の `ORDER BY`（`cmp_cell_values`）へ `SignedInteger`/`Numeric`/`Date`/`Timestamp` の腕を追加。デコード tier（Issue #350）は全新型が `DecodeTier::DimAndScalar` に着地し RLS・TABLE-12 検証は不変。`WHERE`・式・`GROUP BY` キー列としての新型直接参照は Issue #891 の担当のまま。NoSQL 表層は `BoundAggregate`/`BoundAggregateItem` の共有により production コード変更なしで新型集計を受理する。詳細は `docs/design/aggregate-scalar-types.md` 参照。production コード〔`crates/wire-server/src/`〕は無変更）実装済み。
新スカラー型（INTEGER/BIGINT/REAL/DOUBLE/DATE/TIMESTAMP/NUMERIC/UUID）のスカラー列二次索引対応（Issue #893・親 #472・#359。TABLE-13・INDEX-5・INDEX-6・TASK-199 ポインタ。`sql::scalar_index::OrderedColumnIndex`〔`TextColumnIndex` の順序キー版。`I64`／`F64Sortable`／`I128`／`U128` の 4 variant〕・`ScalarIndex::typed_columns`・`candidates_typed_range` を追加し、`INTEGER`／`BIGINT`／`DATE`／`TIMESTAMP` は `i64` へ昇格、`REAL`／`DOUBLE` は全順序比較可能な `u64` ビット表現、`NUMERIC` は列固定 `scale` のもとでの `unscaled`（`i128`）、`UUID` はネットワークバイトオーダーの `u128` を、それぞれキーとする順序索引で候補削減できるようにした。`BIGINT`／`TIMESTAMP` は絶対値が `2^53` を超える値を 1 件でも含む列を列単位で索引対象外へ縮退する。`BOOLEAN`／`BYTEA`／`JSON`／`JSONB`／`ARRAY`／`VECTOR` 列は等価・前方一致・範囲述語を持たない、または値域が 2 値であるとして索引対象外のまま据え置く判断を確定。着手時点では新スカラー型の `WHERE` 述語（レーン B: `DATE`／`TIMESTAMP`／`NUMERIC`／`UUID`／`BYTEA` の等価・範囲比較。Issue #891・TASK-199）が未マージだったため一旦 production 未接続のまま実装したが、codex-review 指摘（PR #1032）を受け同一 PR 内で production 結線まで完了させた（レーン A: `INTEGER`／`BIGINT`／`REAL`／`DOUBLE` の算術式・式評価経由の `WHERE` は別 Issue のまま未着手・未索引）。`ScalarIndex::build` は `DATE`／`TIMESTAMP`／`NUMERIC`／`UUID` 列を常に構築し（レーン A 列は述語表現が無く一度も照会されないため構築自体を遅延し続ける。未接続列だけが原因で既存の `TEXT`／`ENUM` 索引を巻き添えにしないための区分）、`sql::scalar_index::ScalarIndex::candidates_for` が `declarative_filter::FilterOp::TypedCompare`（`sql::exec`／`sql::aggregate`／`sql::group_by` の `metadata_filters` に混在したまま渡ってくる。`resolve_candidates` 側の型ごとの振り分けは不要）を直接消費する形で production 結線した。`NUMERIC` はリテラルが列と異なる `scale` を持ちうるため `numeric::rescale_bounds_for_column`（`cmp_exact` と同じ「丸めない」比較意味論を保った境界導出）を新設。`sql::scalar_plan::ScalarPlan::IndexTypedRange`・`EXPLAIN` の `scalar_plan:` トークン `index_typed_range` を追加し、索引で完全被覆された述語として既存の信頼マスク（`mask_trusted_defer`／`count_star_only`／`observe_group_count_only`）にも自然に合流する（`BYTEA` は `OrderedColumnIndex` に対応 variant を持たず引き続き `PlainScan`）。詳細は `docs/design/scalar-index-prune.md`「Issue #893」節参照。production コード〔`crates/wire-server/src/`〕は無変更）実装済み。
新スカラー型（INTEGER〜ENUM。TABLE-13・TASK-199）の 3 段階デコード tier（`DecodeTier::Fast`/`DimAndScalar`/`Embedding`。Issue #350）対応を精査（Issue #894。production の実装は #881〜#892 で先行済みだったため、本 Issue は網羅性・fail-closed 性の確認と回帰テストの固定が中心。`sql::aggregate::execute_aggregate_with_cache` のインライン tier 選択 if 連鎖を `select_decode_tier`（純関数・挙動不変）へ抽出し、`ReferencedColumns::derive`＋`select_decode_tier` の組み合わせで新型列参照が一律 `DimAndScalar` に着地し embedding をデコードしないことを固定するユニットテストを追加。`row_codec.rs` へ全新型（ENUM を除く。`Storage::create_enum_type` を要するため対象外）を含む「可変長・固定長交互」スキーマでのマスク付き走査の単一ビット総当たりテスト、および非要求列（マスク false）でも REAL の NaN・BOOLEAN の未知バイト・NUMERIC の precision 超過・INTEGER の途中打ち切り・末尾余剰バイトを fail-closed に拒否することを固定するテストを追加（ENUM の同種契約は既存 `tests/enum_column.rs` で固定済み）。`sql/scan.rs::decode_tier_for`（純関数）・`sql/group_by.rs`（`TEXT` キー `GROUP BY` ＋新型集計）についても tier 選択とキー/ヘッダ tenant 不一致（TABLE-12）の fail-closed 契約を新型スキーマで固定。`crates/engine/tests/decode_tier_scalar_types.rs`（新設）で `EngineCore::execute_sql` 経由の `COUNT(*)`/`COUNT(<新型列>)`/`MIN(dt)`/`SUM(i)`/`GROUP BY lang`/広域取得 `SELECT id, i, u, dt ... LIMIT` を可視行のみのオラクルと突合し、他テナント行の非混入を確認。production のホットループ（行走査本体）自体は無変更のため性能実測は必須化せず、構造的な非退行根拠（tier 選択が 1 クエリ 1 回の呼び出しのまま）を記録するに留めた。詳細は `docs/design/aggregate-decode-skip.md`「#894 追記」節参照。申し送り: `row_codec::scalar_refs_as_text`（`pub`・呼び出し元ゼロ・fail-open の危険が doc に明記済み）の削除検討、Issue #891（`BoundExpr` への新型スカラー列参照追加）時に `ReferencedColumns::derive`／`decode_tier_for` の式経由参照も `scalar_mask` へ反映する必要がある点、GROUP BY キーの新型拡張、二次索引経路の新型対応（#893）はいずれも別 Issue の担当。

`RowDescription` 型 OID 写像の拡張（WIRE-13・TASK-200、Issue #895。`result_encoder::WireType` へ `Bool`/`Float4`/`Float8`/`Date`/`Timestamp`/`Bytea`/`Uuid`/`Json`/`Jsonb` を追加し、`BOOLEAN`（OID 16）/`REAL`（OID 700）/`DOUBLE PRECISION`（OID 701）/`DATE`（OID 1082）/`TIMESTAMP`（OID 1114）/`BYTEA`（OID 17）/`UUID`（OID 2950）/`JSON`（OID 114）/`JSONB`（OID 3802）を PostgreSQL 組み込み OID・typlen で公告するよう `column_wire_type` を拡張。`WireType::ALL`（variant 数と一致させた固定長配列）を単一情報源として `from_oid` の逆引きを導出し、逆引き専用の第 2 の写像表を持たない。`id`（`numeric`・OID 1700）・`TEXT`/`VECTOR`（OID 25）の既存公告は不変（`u64` 全域を表現できない `int8` へは変更しない据え置き判断）。`ARRAY`/`ENUM`/`Computed` も専用 OID を持たない据え置き判断により `text` のまま。バイナリ形式（WIRE-14）は新型いずれも spec 側で未策定のため `supports_binary`/`column_binary_support` が fail-closed に非対応のまま（対応拡大は WIRE-14・TASK-218 の後続へ申し送り）。層 A 結合テスト `crates/wire-server/tests/wire13_type_oid.rs` で実 wire 経路（`RowDescription` バイト列・`DataRow` 値の不変・他テナントからの RLS 境界）を固定。層 B（psycopg・node pg の opt-in 型 OID 観測）は対象外のまま次 Issue へ申し送り。詳細は `docs/design/wire-type-oid-mapping.md` 参照。engine production コード無変更）実装済み。

TLS 1.3 サーバー側自作実装の transcript hash と Finished（Issue #964・TASK-228・WIRE-9・HTTP-10 ポインタ。親 Issue #941 の分解 13/20）を実装済み。`tls/transcript.rs::Transcript`（ハンドシェイクメッセージ列の累積 SHA-256。更新順序の単一情報源として `expected_next()` を提供し〔poison 後は `Err(OutOfOrder)`〕、HelloRetryRequest 時は RFC 8446 §4.4.1 の `message_hash` 合成メッセージへ 1 回目 ClientHello を置換する）・`tls/finished.rs`（`compute_verify_data`・`build_server_finished`・`verify_client_finished`。`hkdf::ct_eq` のみによる定数時間検証・不一致は `AlertDescription::DecryptError`〔51〕へ写像）として実装し、`record.rs` へ `AlertDescription::DecryptError` を追加。RFC 8448 §3（Simple 1-RTT）・§5（HelloRetryRequest。いずれも IETF の公開文書由来）の実バイト列を用いた公開 API のみの結合テスト（`tests/tls_transcript_finished_rfc8448.rs`）で、server／client Finished の verify_data 一致と `message_hash` 置換後の transcript hash 一致を固定した。詳細は `docs/design/tls-transcript-finished.md` 参照。alert の実送出・状態機械（#965）・接続への結線（#966・#968）は対象外。
