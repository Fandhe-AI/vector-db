# samples

| Name | Description | Path |
| --- | --- | --- |
| Metal Compute Kernel Dispatch | Run a compute-only round trip on the GPU: device, library, function, pipeline state, command buffer, encoder, dispatch, and readback. | [compute-kernel-dispatch.md](./compute-kernel-dispatch.md) |
| Non-Uniform Threadgroup Dispatch Sizing | Derive a threadgroup size from the pipeline state's execution width, then dispatch across a 2D grid using both the non-uniform (`dispatchThreads`) and uniform (`dispatchThreadgroups`) forms. | [nonuniform-threadgroup-dispatch.md](./nonuniform-threadgroup-dispatch.md) |
| Shared Storage Mode Buffer on Unified Memory | Allocate a buffer with `MTLResourceStorageModeShared` so the CPU and GPU read and write the same physical memory on Apple silicon, then access it from the CPU through `contents()` with no explicit copy. | [shared-storage-mode-buffer.md](./shared-storage-mode-buffer.md) |
| Resource Group Argument Buffer for a Compute Function | Bind several resources (a texture, a buffer, and a constant) to a compute function through a single argument buffer instead of one bind call per resource. | [resource-group-argument-buffer.md](./resource-group-argument-buffer.md) |
| CPU-Encoded Indirect Command Buffer (Compute) | Encode a compute indirect command buffer (ICB) once on the CPU and re-execute it across multiple frames, instead of re-encoding the same dispatch every frame. | [cpu-encoded-indirect-compute.md](./cpu-encoded-indirect-compute.md) |
| Combining Blit and Compute Operations in a Single Pass | Metal 4's compute command encoder can issue both texture-copy ("blit") commands and a compute dispatch, so two source textures can be composited and then processed by a kernel without leaving the encoder. | [blit-and-compute-single-pass.md](./blit-and-compute-single-pass.md) |
| MPSGraph Matrix Multiplication | Build a small graph with placeholders and variables, connect them with `matrixMultiplication`, and execute it against a command buffer with `MPSGraphTensorData`. | [mpsgraph-matrix-multiplication.md](./mpsgraph-matrix-multiplication.md) |
| MPSGraph Training Loop Step | Wire up placeholders, trainable variables, a loss, its gradients, and SGD update ops into a single graph, then encode one training step per batch with double buffering. | [mpsgraph-training-loop.md](./mpsgraph-training-loop.md) |
| MPS Image Filter with In-Place Fallback | Encode a built-in `MPSImageGaussianBlur` kernel into a command buffer, letting it try to run in place and falling back to a fresh texture through a copy allocator when in-place isn't possible. | [mps-image-filter.md](./mps-image-filter.md) |
| MLX Array Creation and Lazy Evaluation | Create `mx.array` values and control when the compute graph they build actually runs, via `mx.eval` and the implicit-evaluation triggers (`print`, `.item()`, `numpy` conversion). | [mlx-array-lazy-evaluation.md](./mlx-array-lazy-evaluation.md) |
| MLX Unified Memory Across CPU and GPU Streams | Run the same array through CPU and GPU streams without moving or copying it, and let MLX's scheduler insert dependencies automatically when one stream's output feeds another. | [mlx-unified-memory.md](./mlx-unified-memory.md) |
| MLX MLP Training Loop | Define a multi-layer perceptron as an `nn.Module`, get a combined loss-and-gradient function with `nn.value_and_grad`, and run an SGD training loop with `optim.SGD.update`. | [mlx-mlp-training-loop.md](./mlx-mlp-training-loop.md) |
| MLX Composable Function Transforms | Compose `mx.grad`, `mx.value_and_grad`, and `mx.vmap` — each transform takes a function and returns another function, so transforms can be nested and reused arbitrarily. | [mlx-function-transforms.md](./mlx-function-transforms.md) |
