//! 自作 TLS 1.3 サーバー側実装の入口（TASK-228・WIRE-9・HTTP-10。
//! ポインタ: `docs/spec/05-tasks.md` TASK-228）。
//!
//! 親 Issue #941 の判断（オーナー判断 2026-09-22。外部 TLS クレートへ依存せず
//! 自作する）に基づき、実装を 2 時間単位の sub-issue（#952〜#971）へ分割して
//! 進めている。本モジュールはそのうち #952（レコード層。RFC 8446 §5.1）の
//! 入口であり、現時点では以下の接続経路へは**未接続**のまま維持する:
//!
//! - `handshake.rs` の `SSLRequest` 応答（`'N'` を返す既存挙動）はビット単位で
//!   不変。TLS の実接続組み込みは #966・#968 が担当する
//! - ハンドシェイクメッセージ層（#953）・バージョン交渉（#954）・
//!   レコード保護／暗号化（#959）・状態機械と alert 送出（#965）は
//!   いずれも別 Issue の担当であり、本モジュールはそれらに先行して
//!   レコード層のみを提供する
//!
//! [`record`] モジュールはヘッダ検証・parse/serialize・受信バッファの組み立てを
//! 担い、鍵・暗号・状態を一切持たない（`record` のドキュメンテーションコメント
//! を参照）。後続 sub-issue は `pub mod` を 1 行ずつ追加していく想定であり、
//! 並列開発時のコンフリクトを避けるため本ファイルは意図的に最小限にとどめる。

pub mod record;
