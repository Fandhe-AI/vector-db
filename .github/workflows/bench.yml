name: Bench

# TASK-127（性能・Recall 受け入れ基準の回帰テスト化）の実行。crates/engine/benches/simd_bench.rs
# は時間依存の測定値を含むため PR CI（ci.yml）のアサーションには混ぜず、本 workflow を
# 独立実行する。private submodule（docs/spec）は取得しない（README「開発環境構築」:
# ビルド・テストは spec 抜きで成立する方針）。
#
# schedule トリガ（毎週月曜 03:00 UTC。CI 混雑を避けるため PR/push トリガーとは
# 独立した時間帯にする）は #168 で再追加済み。BENCH_MAX_P95_MS・BENCH_MIN_RECALL・
# BENCH_BATCH_MAX_DEGRADATION_PCT の repo variables 未設定のまま schedule 実行された
# 場合は `simd_bench.rs` / `batch_bench.rs` 側の fail-closed により run が red に
# なる（「一度も評価していない run」が green として埋もれることはない）。CORE-5
# （対照エンジンに対する p95 レイテンシ比率の判定。ポインタ:
# docs/spec/04-behavior/core-engine.md CORE-5）は対照エンジンの接続が未完了のため
# 引き続き Issue #176 で追跡し、`BENCH_CORE5` repo variable が未設定（既定）の間は
# 「対象外」として合否判定から明示的に除外する opt-in 方式を維持する（`simd_bench.rs`
# 冒頭コメント参照）。CORE-6（GPU vs CPU-SIMD）・CORE-16（f16 常駐 vs f32 常駐）も
# 実 GPU バックエンド未接続のため Issue #178 で追跡し、同様に opt-in のまま維持する
# （`batch_bench.rs` 冒頭コメント参照）。
#
# bench-c1（TASK-83・Conditional Go 条件7）は workflow_dispatch 限定とし、上記
# schedule には含めない。GitHub ホステッド runner は共有 2 vCPU の非専有環境であり、
# `crates/engine/benches/sql_c1_bench.rs` が測る SQL 表層（毎クエリ約 300MB の候補
# デコードを伴う）の p95 を恒常的な回帰ゲートとして schedule 実行すると、専有環境
# 前提の基準に対して恒常的に red になりかねない（`sql_c1_bench.rs` 冒頭コメント参照。
# `BENCH_DEDICATED_ENV` は GitHub ホステッド runner が専有環境でないため注入しない）。
on:
  workflow_dispatch: {}
  schedule:
    - cron: "0 3 * * 1"

permissions:
  contents: read

concurrency:
  # workflow_dispatch は任意の ref を選択して起動できる（デフォルトブランチ固定では
  # ない）ため、workflow 名のみで group を一意化すると別 ref 向けの実行が先行実行を
  # キャンセルしてしまう。ref を group に含め、同一 ref の重複実行のみ
  # キャンセル対象にする（ci.yml の PR 番号切り分けと同種の意図）。
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # --------------------------------------------------
  # bench-simd（TASK-127 回帰ベンチ実行）
  # --------------------------------------------------
  # 生の steps を持つ独立ジョブとして定義するため、第三者 action（actions/checkout）は
  # ci.yml の crash-test 系ジョブと同一の SHA 固定で参照する（AGENTS.md「CI・ワークフロー
  # の改変（P1）」。`@latest` が許されるのは Fandhe-AI/actions のみ）。
  bench-simd:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      - name: Install Rust toolchain (rust-toolchain.toml)
        run: rustup show
      - name: Run bench-simd
        # 数値基準（p95 上限・Recall 下限）は spec（TASK-127）が SSOT のため本ワークフロー
        # にはハードコードしない。リポジトリの Actions variables（`vars.*`）から注入する
        # （spec の定量値を secrets 化してまで隠す運用は取らないが、実測ログに閾値の
        # 数値そのものは載せない——`crates/engine/benches/simd_bench.rs` が
        # 標準出力へ実測値と pass/fail のみを書き出す設計になっている）。BENCH_MAX_P95_MS・
        # BENCH_MIN_RECALL が未設定の場合は `simd_bench.rs` 側が fail-closed で
        # 非ゼロ終了する。BENCH_CORE5 は repo variable が未設定（既定）なら CORE-5 を
        # 対象外として扱う opt-in フラグで、CORE-5 接続後（Issue #176）にリポジトリ
        # 管理者が `gh variable set BENCH_CORE5 1` で有効化する想定（`simd_bench.rs`
        # 冒頭コメント参照）。
        env:
          BENCH_MAX_P95_MS: ${{ vars.BENCH_MAX_P95_MS }}
          BENCH_MIN_RECALL: ${{ vars.BENCH_MIN_RECALL }}
          BENCH_CORE5: ${{ vars.BENCH_CORE5 }}
        run: make bench-simd

  # --------------------------------------------------
  # bench-batch（TASK-130 回帰ベンチ実行）
  # --------------------------------------------------
  # CORE-7（動的窓の劣化上限）はアクティブなゲートとして実測する。CORE-6（GPU vs
  # CPU-SIMD）・CORE-16（f16 常駐 vs f32 常駐）は実 GPU バックエンド未接続のため
  # opt-in fail-closed で扱う（`batch_bench.rs` 冒頭コメント参照。Issue #178 で
  # 追跡）。bench-simd ジョブと同様、schedule / workflow_dispatch の両方で
  # 実行する（#168）。
  bench-batch:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      - name: Install Rust toolchain (rust-toolchain.toml)
        run: rustup show
      - name: Run bench-batch
        # 数値基準（劣化率上限）は spec（TASK-130）が SSOT のため本ワークフローには
        # ハードコードしない。リポジトリの Actions variables（`vars.*`）から注入する。
        # BENCH_BATCH_MAX_DEGRADATION_PCT が未設定の場合は `batch_bench.rs` 側が
        # fail-closed で非ゼロ終了する。BENCH_CORE6/BENCH_CORE16 は repo variable が
        # 未設定（既定）なら対象外として扱う opt-in フラグで、実 GPU 接続後（Issue
        # #178）にリポジトリ管理者が `gh variable set BENCH_CORE6 1` 等で有効化する
        # 想定（`batch_bench.rs` 冒頭コメント参照）。
        env:
          BENCH_BATCH_MAX_DEGRADATION_PCT: ${{ vars.BENCH_BATCH_MAX_DEGRADATION_PCT }}
          BENCH_CORE6: ${{ vars.BENCH_CORE6 }}
          BENCH_CORE16: ${{ vars.BENCH_CORE16 }}
        run: make bench-batch

  # --------------------------------------------------
  # bench-c1（TASK-83 Conditional Go 条件7 の参考実測。workflow_dispatch 限定）
  # --------------------------------------------------
  # SQL 表層（`EngineCore::execute_sql`）経由の C1（純粋 Top-k）p95 を再測定する。
  # 上記 2 ジョブと異なり schedule には含めない（ワークフロー冒頭コメント参照）。
  # GitHub ホステッド runner は専有環境ではないため `BENCH_DEDICATED_ENV` は注入せず、
  # `sql_c1_bench.rs` 側が「専有環境未宣言」として条件7 の判定対象から明示的に除外する
  # （参考値としての p95/Recall 実測とゲートの土台整備が本ジョブの目的で、条件7 の
  # 最終判定はオーナーが専有環境で `make bench-c1` を実行して行う）。
  bench-c1:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      - name: Install Rust toolchain (rust-toolchain.toml)
        run: rustup show
      - name: Run bench-c1
        # 数値基準（p95 上限・Recall 下限）は spec（TASK-83・SQL-1）が SSOT のため
        # 本ワークフローにはハードコードしない。bench-simd の
        # BENCH_MAX_P95_MS・BENCH_MIN_RECALL は provider 単体（CORE-3・SEARCH-4・
        # CORE-4）の基準であり SQL-1 とは出所が異なるため流用せず、SQL-1 専用の
        # repo variables を使う。未設定の場合は `sql_c1_bench.rs` 側が fail-closed で
        # 非ゼロ終了する。
        env:
          BENCH_SQL_C1_MAX_P95_MS: ${{ vars.BENCH_SQL_C1_MAX_P95_MS }}
          BENCH_SQL_C1_MIN_RECALL: ${{ vars.BENCH_SQL_C1_MIN_RECALL }}
        run: make bench-c1
