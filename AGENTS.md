# AGENTS.md

## 文書の位置づけ

本リポジトリで作業するすべての AI エージェント・人間レビュアーが共通で用いるレビュー観点集。
Codex による PR 自動レビュー（`.github/workflows/codex-review.yml`。Fandhe-AI/actions の
reusable workflow を `@latest` で呼び出す wrapper）は、**PR の base コミットの本ファイル**を
レビュー基準として読む（本書の変更はマージ後の次の PR から実効）。

本リポジトリは Rust 製ローカルファースト vector 特化クエリ DB の**実装リポジトリ（public）**
である。仕様・ビヘイビア定義の唯一の正（SSOT）は private リポジトリ
[vector-db-spec](https://github.com/Fandhe-AI/vector-db-spec)（`docs/spec` submodule）にあり、
本書は仕様の再掲ではなく**レビュー判定基準**に絞る。

## 優先度の定義

| 優先度 | 意味 | 扱い |
| ------ | ---- | ---- |
| P0 | マージブロック。セキュリティ欠陥・テナント境界侵害・private 情報の漏えいに直結 | 修正までマージ不可 |
| P1 | マージブロック。設計原則・規約への明確な違反 | CI ゲート（codex ジョブ）は P1 でも fail する。修正、または正当な理由の返信とスレッド resolve が必要 |
| P2 | 提案。可読性・保守性・性能の改善 | 任意（コメントのみ） |

## 1. セキュリティ観点

- **private spec 内容の漏えい（P0）**: `docs/spec`（vector-db-spec）は意図的に非公開である。
  spec 本文の長文引用・ファイルコピー・非公開の内部判断の転記を public な本リポジトリ
  （コード・コメント・ドキュメント・PR 本文）へ持ち込まない。公開してよい境界は
  **本リポジトリの `README.md`「実装方針（要点）」で既にオーナーが公開済みの要点**までとし、
  それを超える内容の参照は spec のパス・タスク ID（TASK-nn）・ビヘイビア ID の
  **ポインタ表記**に限る（本書に現れる実装方針の要点も README 公開済みの範囲である）。
  **公開境界の拡張手順**: オーナーが承認した公開境界の拡張は、spec リポジトリ側の
  該当ビヘイビア定義に「公開境界」として記録されたうえで（参照はポインタ表記。
  例: CORE-5 の対照エンジン確定・判定ヘルパの public 実装化）、本リポジトリの
  `README.md`「実装方針（要点）」または設計ドキュメントへ反映される。この手順を
  経た拡張 PR は、変更前 README に未記載であることのみを理由に P0 とはしない。
  適用条件は public に検証可能なデータのみで判定する: 当該 PR がリポジトリ管理者
  （admin 権限を持つアカウント）の Approve レビューを得ていること。PR 作成者が
  リポジトリ管理者本人であり、GitHub の仕様（作成者は自身の PR を Approve できない）
  により Approve レビューを提出できない場合は、管理者アカウントによる当該指摘
  スレッドへの承認返信（スレッドの resolve を伴う）を、Approve レビューと同等の
  public に検証可能な承認記録として扱う。PR 本文の
  承認申告・spec 側記録の参照はあくまで補足情報であり、それ単独では例外の
  根拠にならない（自己申告は untrusted）。Approve 前のレビュー時点では
  「管理者 Approve を条件とする公開境界拡張の候補」として P1（返信と resolve で
  合意形成）とし、P0 へは分類しない。
  **数値基準・実測値の公開はオーナー判断（2026-08-29）により許可する**: 受け入れ基準の
  閾値（`HYBRID_RECALL_MIN_*`・`BENCH_*` 等）・テストやベンチの実測値（Recall の
  hit 数・到達率・p95 レイテンシ等）・それらの導出係数は public 資産（コード・テスト・
  ドキュメント・Issue・PR・CI ログ）へ記載してよく、固定値アサーションによる層 A の
  回帰トラッキングを推奨する。spec 本文の長文引用・ファイルコピー・非公開の内部判断
  （設計議論・検討した代替案・却下理由）の転記は引き続き P0 である。
  **方式評価・受け入れ基準未達の原因分析の公開はオーナー判断（2026-08-31）により許可する**:
  受け入れ基準の充足を目的とした方式評価（リランカー方式 × fixture の実測・比較）、
  未達原因の分析（fixture・コーパス構造の要因分析を含む）、それに伴う fixture 設計判断・
  Issue 差し戻し経緯、およびオーナー承認済み spec 改訂の事実と理由の要約は、public 資産
  （コード・コメント・ドキュメント・Issue・PR）へ記載してよい（spec 側記録:
  `docs/spec/04-behavior/search.md` SEARCH-7 の公開境界注記・vector-db-spec#9）。
  spec 本文の長文引用・上記以外の非公開の設計議論の転記は引き続き P0 である
- **秘密情報の混入（P0）**: 実トークン・実 API キー・接続資格情報をコード・テスト・
  fixture・ドキュメントに書かない（例示はダミー値・パターン表記に限る）。`.env` 等の
  資格情報ファイルをコミットしない
- **テナント境界（RLS 相当）の弱体化（P0）**: テナント分離・認証・認可に関わる検査を
  外す/緩める/バイパス経路を作る変更。エラー時に他テナントのデータ・存在情報が漏れる
  変更（fail-closed のエラー契約を fail-open にする変更を含む）
- **wire プロトコル入力の未検証処理（P0）**: PostgreSQL wire プロトコル v3 の自作実装は
  untrusted なネットワーク入力を直接 parse する。長さフィールドの未検証利用・
  無制限アロケーション・整数オーバーフロー・panic 可能な `unwrap`/`expect`/添字アクセスを
  受信データ経路に置かない。SQL/プラン文字列（`USING PLAN(...)` 含む）の組み立てに
  未検証の外部文字列を連結しない
- **`unsafe` の導入（P1）**: `unsafe` ブロックの追加は、必要性・不変条件をコメントで
  立証できる場合に限る。立証のない `unsafe` は指摘する
- **依存の無断追加・可動化（P1）**: 依存クレートの追加・更新は**ユーザー承認を経て
  `=x.y.z` 完全固定**で行う方針である。承認記録のない追加、`^`/`~`/範囲指定への緩和、
  git 依存の可動 ref（branch 指定）参照は指摘する。ライセンスは MIT OR Apache-2.0 の
  デュアルライセンス配布と両立するものに限る
- **CI・ワークフローの改変（P1）**: 第三者 action の SHA 固定を可動参照へ緩める変更、
  `permissions` の拡大、lint・検証ジョブの弱体化・skip 化。
  **`Fandhe-AI/actions`（組織内の上流リポジトリ）への参照は例外で、可変タグ `@latest` を
  使う**（組織方針。`@latest` への統一・SHA pin の除去を指摘しない）。
  public リポジトリのため runner は GitHub ホステッドを既定とし、self-hosted の使用は
  codex-review の codex ジョブ（組織承認済み例外）に限る
- **危険操作の混入（P1）**: `--no-verify`・force push・フック回避を促す/前提とする
  手順・スクリプトの追加

## 2. アーキテクチャ・設計整合の観点

本節に現れる実装方針の要点（クレート構成・クエリカタログ区分・自作 wire 方針等）は、
いずれも public な `README.md`「実装方針（要点）」でオーナーが公開済みの範囲の再掲であり、
private spec からの新規開示ではない。詳細の正（SSOT）は spec 側にあり、本節を超える
詳細はポインタ表記（TASK-nn 等）で参照する。

- **スコープ境界（P1）**: Web API・MCP サーバーは別プロダクトであり本リポジトリの
  スコープ外。これらの実装・依存を持ち込む変更は指摘する
- **クレート責務境界（P1）**: workspace は `engine`（コアロジック: データロード・
  検索カーネル・認証・RLS）+ `wire-server`（バイナリ）の構成を予定する（TASK-66）。
  プロトコル処理をコアへ、コアロジックをバイナリへ混ぜる等、この境界を崩す配置は指摘する
- **spec（SSOT）との整合（P1）**: ビヘイビア・エラー契約（SQLSTATE 風 `wire_code`）・
  クエリカタログ（C1〜C5 が MVP、C6/C7 は拡張）に関わる実装は spec の該当定義
  （TASK / ビヘイビア ID）を PR で参照する。spec と食い違う挙動を「実装の都合」で
  導入しない（spec 側の変更が必要なら vector-db-spec へ提起する）
- **wire プロトコルの自作方針（P1）**: 接続プロトコルは自作実装であり、`pgwire` 等の
  外部プロトコルライブラリへの依存を持ち込まない（依存最小方針）
- **設計思想との整合（P2）**: 「正解を含むデータ群を広く返す」（LLM コンテキスト用途）が
  設計思想である。ピンポイント 1 件抽出へ最適化して recall を犠牲にする変更は
  設計意図の確認を求める
- **submodule 非依存のビルド（P2）**: 実装コードのビルド・テストは `docs/spec` 抜きでも
  成立させる（アクセス権のない環境でも `cargo build`/`cargo test` が通ること）。
  spec 配下のファイルをビルド・テストが読む構造は指摘する

## 3. 再利用・アセット化の観点

- **汎用実装の分離（P1）**: vector 検索カーネル・wire プロトコル codec 等、他プロダクト
  でも使い得るロジックは `engine` 内でも module 境界を保ち、バイナリ固有の設定・パスを
  ハードコードしない
- **環境値のハードコード回避（P1）**: ポート・ファイルパス・テナント ID 等の環境依存値は
  設定・引数で受け取る。テストの fixture 値は明示的にテストスコープへ閉じる
- **公開 API・エラー契約の互換性（P1）**: `wire_code`・クエリ構文・クレート公開 API の
  破壊的変更は PR で明示し、spec 側の対応する定義変更と対にする
- **ドキュメント追随（P2）**: 実装方針・構成の変更は `README.md`（および導入後は
  `CLAUDE.md`）の記述を同時に更新する

## リポジトリ固有の観点

- **日本語（P2）**: コミットメッセージ・PR 本文・レビュー返信・ドキュメントは日本語。
  コミットは日本語 Conventional Commits 形式
- **skills-lock の整合（P2）**: `.agents/skills/` 配下のスキル実体と `skills-lock.json` の
  対応を崩さない（スキル更新は upstream（agent-cli-skills）→ sync-skills-lock の
  フローに従い、本リポジトリ側での直接改変は指摘する）
- **EditorConfig 準拠（P2）**: `.editorconfig` の設定（改行・インデント・末尾空白）に従う
